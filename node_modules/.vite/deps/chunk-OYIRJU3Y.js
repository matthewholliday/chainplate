import {
  useShallow
} from "./chunk-XZLMIPKT.js";
import {
  require_jsx_runtime
} from "./chunk-LPULNKJF.js";
import {
  require_react
} from "./chunk-42LCURWG.js";
import {
  AssistantMessageStream,
  AssistantMetaTransformStream,
  AssistantStream,
  ExportedMessageRepository,
  InMemoryThreadListAdapter,
  MessageRepository,
  PlainTextDecoder,
  ToolResponse,
  attachmentsEqual,
  bindExternalStoreMessage,
  createAssistantStreamController,
  fileMatchesAccept,
  fromThreadMessageLike,
  generateErrorMessageId,
  generateId,
  getAutoStatus,
  getExternalStoreMessages,
  getPartialJsonObjectFieldState,
  getPartialJsonObjectMeta,
  isAutoStatus,
  isMcpAppUri,
  liftNonTextParts,
  mergeModelContexts,
  symbolInnerMessage,
  toJSONSchema,
  toPartialJSONSchema,
  toolResultStream
} from "./chunk-QZ4OSTC6.js";
import {
  __toESM
} from "./chunk-G3PMV62Z.js";

// node_modules/@assistant-ui/tap/dist/core/helpers/callResourceFn.js
function callResourceFn(resource2, props) {
  const fn = resource2[fnSymbol];
  if (!fn) throw new Error("ResourceElement.type is not a valid Resource");
  return fn(props);
}
var fnSymbol = /* @__PURE__ */ Symbol("fnSymbol");

// node_modules/@assistant-ui/tap/dist/core/resource.js
function resource(fn) {
  const type = (props) => {
    return {
      type,
      props
    };
  };
  type[fnSymbol] = fn;
  return type;
}

// node_modules/@assistant-ui/tap/dist/core/withKey.js
function withKey(key, element) {
  return {
    ...element,
    key
  };
}

// node_modules/@assistant-ui/tap/dist/core/helpers/env.js
var isDevelopment = typeof process !== "undefined" && true;

// node_modules/@assistant-ui/tap/dist/core/helpers/execution-context.js
var currentResourceFiber = null;
function withResourceFiber(fiber, fn) {
  fiber.currentIndex = 0;
  const previousContext = currentResourceFiber;
  currentResourceFiber = fiber;
  try {
    fn();
    fiber.isFirstRender = false;
    if (fiber.cells.length !== fiber.currentIndex) throw new Error(`Rendered ${fiber.currentIndex} hooks but expected ${fiber.cells.length}. Hooks must be called in the exact same order in every render.`);
  } finally {
    currentResourceFiber = previousContext;
  }
}
function getCurrentResourceFiber() {
  if (!currentResourceFiber) throw new Error("No resource fiber available");
  return currentResourceFiber;
}
function getDevStrictMode(enable) {
  if (!isDevelopment) return null;
  if (currentResourceFiber?.devStrictMode) return currentResourceFiber.isFirstRender ? "child" : "root";
  return enable ? "root" : null;
}

// node_modules/@assistant-ui/tap/dist/core/helpers/root.js
var createResourceFiberRoot = (dispatchUpdate) => {
  return {
    version: 0,
    committedVersion: 0,
    dispatchUpdate,
    changelog: [],
    dirtyCells: []
  };
};
var commitRoot = (root) => {
  for (const cell of root.dirtyCells) {
    cell.dirty = false;
    cell.queue.clear();
    cell.current = cell.workInProgress;
  }
  root.committedVersion = root.version;
  root.changelog.length = 0;
  root.dirtyCells.length = 0;
};
var setRootVersion = (root, version) => {
  const rollback = root.version > version;
  root.version = version;
  if (rollback) {
    for (const cell of root.dirtyCells) {
      cell.dirty = false;
      cell.queue.clear();
      cell.workInProgress = cell.current;
    }
    root.dirtyCells.length = 0;
    if (version === root.committedVersion) root.changelog.length = 0;
    else {
      if (root.committedVersion > version) throw new Error("Version is less than committed version");
      while (root.committedVersion + root.changelog.length > version) root.changelog.pop();
      root.changelog.forEach((apply) => apply());
      commitRoot(root);
    }
  }
};
var markCellDirty = (fiber, cell) => {
  if (!cell.dirty) {
    cell.dirty = true;
    fiber.markDirty?.();
    fiber.root.dirtyCells.push(cell);
  }
};

// node_modules/@assistant-ui/tap/dist/hooks/utils/tapHook.js
var tapHook = (type, init) => {
  const fiber = getCurrentResourceFiber();
  const index = fiber.currentIndex++;
  if (!fiber.isFirstRender && index >= fiber.cells.length) throw new Error("Rendered more hooks than during the previous render. Hooks must be called in the exact same order in every render.");
  let cell = fiber.cells[index];
  if (!cell) {
    cell = init();
    fiber.cells[index] = cell;
  }
  if (cell.type !== type) throw new Error("Hook order changed between renders");
  return cell;
};
var registerRenderMountTask = (task) => {
  getCurrentResourceFiber().renderContext.effectTasks.push(task);
};

// node_modules/@assistant-ui/tap/dist/hooks/tap-reducer.js
var dispatchOnFiber = (fiber, callback) => {
  if (fiber.renderContext) throw new Error("Resource updated during render");
  if (fiber.isNeverMounted) throw new Error("Resource updated before mount");
  fiber.root.dispatchUpdate(() => {
    const result = callback();
    if (result) {
      result();
      fiber.root.changelog.push(result);
      return true;
    }
    return false;
  });
};
function tapReducerImpl(reducer, getDerivedState, initialArg, initFn) {
  const cell = tapHook("reducer", () => {
    const fiber2 = getCurrentResourceFiber();
    const initialState = initFn ? initFn(initialArg) : initialArg;
    if (isDevelopment && fiber2.devStrictMode && initFn) initFn(initialArg);
    return {
      type: "reducer",
      queue: /* @__PURE__ */ new Set(),
      dirty: false,
      workInProgress: initialState,
      current: initialState,
      reducer,
      dispatch: (action) => {
        const entry = {
          action,
          hasEagerState: false,
          eagerState: void 0
        };
        dispatchOnFiber(fiber2, () => {
          if (fiber2.root.dirtyCells.length === 0 && !entry.hasEagerState) {
            entry.eagerState = reducer(cell.workInProgress, action);
            entry.hasEagerState = true;
            if (Object.is(cell.current, entry.eagerState)) return null;
          }
          return () => {
            markCellDirty(fiber2, cell);
            cell.queue.add(entry);
          };
        });
      }
    };
  });
  const fiber = getCurrentResourceFiber();
  const sameReducer = reducer === cell.reducer;
  cell.reducer = reducer;
  for (const item of cell.queue) {
    if (!item.hasEagerState || !sameReducer) {
      item.eagerState = reducer(cell.workInProgress, item.action);
      item.hasEagerState = true;
    }
    if (isDevelopment && fiber.devStrictMode) reducer(cell.workInProgress, item.action);
    cell.workInProgress = item.eagerState;
  }
  cell.queue.clear();
  if (getDerivedState) {
    const derived = getDerivedState(cell.workInProgress);
    if (!Object.is(derived, cell.workInProgress)) {
      markCellDirty(fiber, cell);
      cell.workInProgress = derived;
    }
  }
  return [cell.workInProgress, cell.dispatch];
}
function tapReducer(reducer, initialArg, init) {
  return tapReducerImpl(reducer, void 0, initialArg, init);
}
function tapReducerWithDerivedState(reducer, getDerivedState, initialArg, init) {
  return tapReducerImpl(reducer, getDerivedState, initialArg, init);
}

// node_modules/@assistant-ui/tap/dist/hooks/tap-state.js
var stateReducer = (state, action) => typeof action === "function" ? action(state) : action;
var stateInit = (initial) => typeof initial === "function" ? initial() : initial;
function tapState(initial) {
  return tapReducer(stateReducer, initial, stateInit);
}

// node_modules/@assistant-ui/tap/dist/hooks/utils/depsShallowEqual.js
var depsShallowEqual = (a, b) => {
  if (a.length !== b.length) return false;
  for (let i = 0; i < a.length; i++) if (!Object.is(a[i], b[i])) return false;
  return true;
};

// node_modules/@assistant-ui/tap/dist/hooks/tap-effect.js
var newEffect = () => ({
  type: "effect",
  cleanup: void 0,
  deps: null
});
function tapEffect(effect, deps) {
  const cell = tapHook("effect", newEffect);
  if (deps && cell.deps && depsShallowEqual(cell.deps, deps)) return;
  if (cell.deps !== null && !!deps !== !!cell.deps) throw new Error("tapEffect called with and without dependencies across re-renders");
  registerRenderMountTask(() => {
    const errors = [];
    try {
      cell.cleanup?.();
    } catch (error) {
      errors.push(error);
    } finally {
      cell.cleanup = void 0;
    }
    try {
      const cleanup = effect();
      if (cleanup !== void 0 && typeof cleanup !== "function") throw new Error(`An effect function must either return a cleanup function or nothing. Received: ${typeof cleanup}`);
      cell.cleanup = cleanup;
    } catch (error) {
      errors.push(error);
    }
    cell.deps = deps;
    if (errors.length > 0) if (errors.length === 1) throw errors[0];
    else {
      for (const error of errors) console.error(error);
      throw new AggregateError(errors, "Errors during commit");
    }
  });
}

// node_modules/@assistant-ui/tap/dist/hooks/tap-ref.js
function tapRef(initialValue) {
  const [state] = tapState(() => ({ current: initialValue }));
  return state;
}

// node_modules/@assistant-ui/tap/dist/hooks/tap-const.js
function tapConst(getValue, _deps) {
  const [state] = tapState(getValue);
  return state;
}

// node_modules/@assistant-ui/tap/dist/hooks/tap-memo.js
var memoReducer = () => {
  throw new Error("Memo reducer should not be called");
};
var tapMemo = (fn, deps) => {
  const fiber = getCurrentResourceFiber();
  const [state] = tapReducerWithDerivedState(memoReducer, (state2) => {
    if (state2 && depsShallowEqual(state2.deps, deps)) return state2;
    const value = fn();
    if (isDevelopment && fiber.devStrictMode) fn();
    return {
      value,
      deps
    };
  }, null);
  return state.value;
};

// node_modules/@assistant-ui/tap/dist/hooks/tap-callback.js
var tapCallback = (fn, deps) => {
  return tapMemo(() => fn, deps);
};

// node_modules/@assistant-ui/tap/dist/hooks/tap-effect-event.js
function tapEffectEvent(callback) {
  const callbackRef = tapRef(callback);
  tapEffect(() => {
    callbackRef.current = callback;
  });
  const fiber = getCurrentResourceFiber();
  return tapCallback(((...args) => {
    if (isDevelopment && fiber.renderContext) throw new Error("tapEffectEvent cannot be called during render");
    return callbackRef.current(...args);
  }), [fiber]);
}

// node_modules/@assistant-ui/tap/dist/core/helpers/commit.js
function commitAllEffects(renderResult) {
  const errors = [];
  for (const task of renderResult.effectTasks) try {
    task();
  } catch (error) {
    errors.push(error);
  }
  if (errors.length > 0) if (errors.length === 1) throw errors[0];
  else {
    for (const error of errors) console.error(error);
    throw new AggregateError(errors, "Errors during commit");
  }
}
function cleanupAllEffects(executionContext) {
  const errors = [];
  for (const cell of executionContext.cells) if (cell?.type === "effect") {
    cell.deps = null;
    if (cell.cleanup) try {
      cell.cleanup?.();
    } catch (e) {
      errors.push(e);
    } finally {
      cell.cleanup = void 0;
    }
  }
  if (errors.length > 0) if (errors.length === 1) throw errors[0];
  else {
    for (const error of errors) console.error(error);
    throw new AggregateError(errors, "Errors during cleanup");
  }
}

// node_modules/@assistant-ui/tap/dist/core/ResourceFiber.js
function createResourceFiber(type, root, markDirty = void 0, strictMode = getDevStrictMode(false)) {
  return {
    type,
    root,
    markDirty,
    devStrictMode: strictMode,
    cells: [],
    currentIndex: 0,
    renderContext: void 0,
    isFirstRender: true,
    isMounted: false,
    isNeverMounted: true
  };
}
function unmountResourceFiber(fiber) {
  if (!fiber.isMounted) throw new Error("Tried to unmount a fiber that is already unmounted");
  fiber.isMounted = false;
  cleanupAllEffects(fiber);
}
function renderResourceFiber(fiber, props) {
  const result = {
    effectTasks: [],
    props,
    output: void 0
  };
  withResourceFiber(fiber, () => {
    fiber.renderContext = result;
    try {
      result.output = callResourceFn(fiber.type, props);
    } finally {
      fiber.renderContext = void 0;
    }
  });
  return result;
}
function commitResourceFiber(fiber, result) {
  fiber.isMounted = true;
  if (isDevelopment && fiber.isNeverMounted && fiber.devStrictMode === "root") {
    fiber.isNeverMounted = false;
    commitAllEffects(result);
    cleanupAllEffects(fiber);
  }
  fiber.isNeverMounted = false;
  commitAllEffects(result);
}

// node_modules/@assistant-ui/tap/dist/hooks/tap-resource.js
function tapResource(element, propsDeps) {
  const parentFiber = getCurrentResourceFiber();
  const versionRef = tapRef(0);
  const fiber = tapMemo(() => {
    element.key;
    return createResourceFiber(element.type, parentFiber.root, () => {
      versionRef.current++;
      parentFiber.markDirty?.();
    });
  }, [
    element.type,
    element.key,
    parentFiber
  ]);
  const result = propsDeps ? tapMemo(() => renderResourceFiber(fiber, element.props), [
    fiber,
    ...propsDeps,
    versionRef.current
  ]) : renderResourceFiber(fiber, element.props);
  tapEffect(() => () => unmountResourceFiber(fiber), [fiber]);
  tapEffect(() => {
    commitResourceFiber(fiber, result);
  }, [fiber, result]);
  return result.output;
}

// node_modules/@assistant-ui/tap/dist/hooks/tap-resources.js
function tapResources(getElements, getElementsDeps) {
  const versionRef = tapRef(0);
  const version = versionRef.current;
  const parentFiber = tapConst(getCurrentResourceFiber, []);
  const markDirty = tapConst(() => () => {
    versionRef.current++;
    parentFiber.markDirty?.();
  }, []);
  const fibers = tapConst(() => /* @__PURE__ */ new Map(), []);
  const getElementsMemo = getElementsDeps ? tapCallback(getElements, getElementsDeps) : getElements;
  const res = tapMemo(() => {
    const elementsArray = getElementsMemo();
    const seenKeys = /* @__PURE__ */ new Set();
    const results = [];
    let newCount = 0;
    for (let i = 0; i < elementsArray.length; i++) {
      const element = elementsArray[i];
      const elementKey = element.key;
      if (elementKey === void 0) throw new Error(`tapResources did not provide a key for array at index ${i}`);
      if (seenKeys.has(elementKey)) throw new Error(`Duplicate key ${elementKey} in tapResources`);
      seenKeys.add(elementKey);
      let state = fibers.get(elementKey);
      if (!state) {
        const fiber = createResourceFiber(element.type, parentFiber.root, markDirty);
        const result = renderResourceFiber(fiber, element.props);
        state = {
          fiber,
          next: result
        };
        newCount++;
        fibers.set(elementKey, state);
        results.push(result.output);
      } else if (state.fiber.type !== element.type) {
        const fiber = createResourceFiber(element.type, parentFiber.root, markDirty);
        const result = renderResourceFiber(fiber, element.props);
        state.next = [fiber, result];
        results.push(result.output);
      } else {
        state.next = renderResourceFiber(state.fiber, element.props);
        results.push(state.next.output);
      }
    }
    if (fibers.size > results.length - newCount) {
      for (const key of fibers.keys()) if (!seenKeys.has(key)) fibers.get(key).next = "delete";
    }
    return results;
  }, [getElementsMemo, version]);
  tapEffect(() => {
    return () => {
      for (const key of fibers.keys()) {
        const fiber = fibers.get(key).fiber;
        unmountResourceFiber(fiber);
      }
    };
  }, []);
  tapEffect(() => {
    for (const [key, state] of fibers.entries()) if (state.next === "delete") {
      if (state.fiber.isMounted) unmountResourceFiber(state.fiber);
      fibers.delete(key);
    } else if (Array.isArray(state.next)) {
      unmountResourceFiber(state.fiber);
      state.fiber = state.next[0];
      commitResourceFiber(state.fiber, state.next[1]);
    } else commitResourceFiber(state.fiber, state.next);
  }, [res]);
  return res;
}

// node_modules/@assistant-ui/tap/dist/core/scheduler.js
var MAX_FLUSH_LIMIT = 50;
var flushState = {
  schedulers: /* @__PURE__ */ new Set([]),
  isScheduled: false
};
var UpdateScheduler = class {
  _task;
  _isDirty = false;
  constructor(_task) {
    this._task = _task;
  }
  get isDirty() {
    return this._isDirty;
  }
  markDirty() {
    this._isDirty = true;
    flushState.schedulers.add(this);
    scheduleFlush();
  }
  runTask() {
    this._isDirty = false;
    this._task();
  }
};
var scheduleFlush = () => {
  if (flushState.isScheduled) return;
  flushState.isScheduled = true;
  scheduleMacrotask();
};
var flushScheduled = () => {
  try {
    const errors = [];
    let flushDepth = 0;
    for (const scheduler of flushState.schedulers) {
      flushState.schedulers.delete(scheduler);
      if (!scheduler.isDirty) continue;
      flushDepth++;
      if (flushDepth > MAX_FLUSH_LIMIT) throw new Error("Maximum update depth exceeded. This can happen when a resource repeatedly calls setState inside tapEffect.");
      try {
        scheduler.runTask();
      } catch (error) {
        errors.push(error);
      }
    }
    if (errors.length > 0) if (errors.length === 1) throw errors[0];
    else {
      for (const error of errors) console.error(error);
      throw new AggregateError(errors, "Errors occurred during flushSync");
    }
  } finally {
    flushState.schedulers.clear();
    flushState.isScheduled = false;
  }
};
var scheduleMacrotask = (() => {
  if (typeof MessageChannel !== "undefined") {
    const channel = new MessageChannel();
    channel.port1.onmessage = flushScheduled;
    return () => channel.port2.postMessage(null);
  }
  return () => setTimeout(flushScheduled, 0);
})();
var flushResourcesSync = (callback) => {
  const prev = flushState;
  flushState = {
    schedulers: /* @__PURE__ */ new Set([]),
    isScheduled: true
  };
  try {
    const result = callback();
    flushScheduled();
    return result;
  } finally {
    flushState = prev;
  }
};

// node_modules/@assistant-ui/tap/dist/tapResourceRoot.js
var tapResourceRoot = (element) => {
  const scheduler = tapConst(() => new UpdateScheduler(() => handleUpdate(null)), []);
  const queue = tapConst(() => [], []);
  const fiber = tapMemo(() => {
    element.key;
    return createResourceFiber(element.type, createResourceFiberRoot((callback) => {
      if (!scheduler.isDirty && !callback()) return;
      queue.push(callback);
      scheduler.markDirty();
    }));
  }, [element.type, element.key]);
  setRootVersion(fiber.root, fiber.root.committedVersion);
  const render = renderResourceFiber(fiber, element.props);
  const isMountedRef = tapRef(false);
  const committedPropsRef = tapRef(element.props);
  const valueRef = tapRef(render.output);
  const subscribers = tapConst(() => /* @__PURE__ */ new Set(), []);
  const handleUpdate = tapEffectEvent((render2) => {
    if (render2 === null) {
      setRootVersion(fiber.root, 2);
      setRootVersion(fiber.root, 1);
      queue.forEach((callback) => {
        if (isDevelopment && fiber.devStrictMode) callback();
        callback();
      });
      if (isDevelopment && fiber.devStrictMode) renderResourceFiber(fiber, committedPropsRef.current);
      render2 = renderResourceFiber(fiber, committedPropsRef.current);
    }
    if (scheduler.isDirty) throw new Error("Scheduler is dirty, this should never happen");
    commitRoot(fiber.root);
    queue.length = 0;
    if (isMountedRef.current) commitResourceFiber(fiber, render2);
    if (scheduler.isDirty || valueRef.current === render2.output) return;
    valueRef.current = render2.output;
    subscribers.forEach((callback) => callback());
  });
  tapEffect(() => {
    isMountedRef.current = true;
    return () => {
      isMountedRef.current = false;
      unmountResourceFiber(fiber);
    };
  }, [fiber]);
  tapEffect(() => {
    committedPropsRef.current = render.props;
    commitRoot(fiber.root);
    commitResourceFiber(fiber, render);
    if (scheduler.isDirty || valueRef.current === render.output) return;
    valueRef.current = render.output;
    subscribers.forEach((callback) => callback());
  });
  return tapMemo(() => ({
    getValue: () => valueRef.current,
    subscribe: (listener) => {
      subscribers.add(listener);
      return () => subscribers.delete(listener);
    }
  }), []);
};

// node_modules/@assistant-ui/tap/dist/core/createResourceRoot.js
var SubscribableResource = resource(tapResourceRoot);

// node_modules/@assistant-ui/tap/dist/core/context.js
var contextValue = /* @__PURE__ */ Symbol("tap.Context");
var createResourceContext = (defaultValue) => {
  return { [contextValue]: defaultValue };
};
var withContextProvider = (context2, value, fn) => {
  const previousValue = context2[contextValue];
  context2[contextValue] = value;
  try {
    return fn();
  } finally {
    context2[contextValue] = previousValue;
  }
};
var tap = (context2) => {
  return context2[contextValue];
};

// node_modules/@assistant-ui/core/dist/react/client/DataRenderers.js
var DataRenderers = resource(() => {
  const [state, setState] = tapState(() => ({
    renderers: {},
    fallbacks: []
  }));
  return {
    getState: () => state,
    setDataUI: tapCallback((name, render) => {
      setState((prev) => {
        return {
          ...prev,
          renderers: {
            ...prev.renderers,
            [name]: [...prev.renderers[name] ?? [], render]
          }
        };
      });
      return () => {
        setState((prev) => {
          return {
            ...prev,
            renderers: {
              ...prev.renderers,
              [name]: prev.renderers[name]?.filter((r) => r !== render) ?? []
            }
          };
        });
      };
    }, []),
    setFallbackDataUI: tapCallback((render) => {
      setState((prev) => ({
        ...prev,
        fallbacks: [...prev.fallbacks, render]
      }));
      return () => {
        setState((prev) => ({
          ...prev,
          fallbacks: prev.fallbacks.filter((r) => r !== render)
        }));
      };
    }, [])
  };
});

// node_modules/@assistant-ui/core/dist/utils/composite-context-provider.js
var CompositeContextProvider = class {
  _providers = /* @__PURE__ */ new Set();
  getModelContext() {
    return mergeModelContexts(this._providers);
  }
  registerModelContextProvider(provider) {
    this._providers.add(provider);
    const unsubscribe = provider.subscribe?.(() => {
      this.notifySubscribers();
    });
    this.notifySubscribers();
    return () => {
      this._providers.delete(provider);
      unsubscribe?.();
      this.notifySubscribers();
    };
  }
  _subscribers = /* @__PURE__ */ new Set();
  notifySubscribers() {
    for (const callback of this._subscribers) callback();
  }
  subscribe(callback) {
    this._subscribers.add(callback);
    return () => this._subscribers.delete(callback);
  }
};

// node_modules/@assistant-ui/core/dist/store/clients/model-context-client.js
var EMPTY_TOOL_NAMES = [];
var INITIAL_STATE = {
  modelName: void 0,
  toolNames: EMPTY_TOOL_NAMES
};
var toolNamesEqual = (a, b) => a === b || a.length === b.length && a.every((v, i) => v === b[i]);
var deriveState = (composite, prev) => {
  const ctx = composite.getModelContext();
  const modelName = ctx.config?.modelName;
  const keys = ctx.tools ? Object.keys(ctx.tools).sort() : EMPTY_TOOL_NAMES;
  const toolNames = keys.length ? keys : EMPTY_TOOL_NAMES;
  if (modelName === prev.modelName && toolNamesEqual(toolNames, prev.toolNames)) return prev;
  return {
    modelName,
    toolNames
  };
};
var ModelContext = resource(() => {
  const composite = tapMemo(() => new CompositeContextProvider(), []);
  const [state, setState] = tapState(() => deriveState(composite, INITIAL_STATE));
  tapEffect(() => {
    setState((prev) => deriveState(composite, prev));
    return composite.subscribe(() => {
      setState((prev) => deriveState(composite, prev));
    });
  }, [composite]);
  return {
    getState: () => deriveState(composite, state),
    getModelContext: () => composite.getModelContext(),
    subscribe: (callback) => composite.subscribe(callback),
    register: (provider) => composite.registerModelContextProvider(provider)
  };
});

// node_modules/@assistant-ui/store/dist/utils/BaseProxyHandler.js
var INTROSPECTION_PROPS = /* @__PURE__ */ new Set([
  "$$typeof",
  "nodeType",
  "then"
]);
var handleIntrospectionProp = (prop, name) => {
  if (prop === Symbol.toStringTag) return name;
  if (typeof prop === "symbol") return void 0;
  if (prop === "toJSON") return () => name;
  if (INTROSPECTION_PROPS.has(prop)) return void 0;
  return false;
};
var BaseProxyHandler = class {
  getOwnPropertyDescriptor(_, prop) {
    const value = this.get(_, prop);
    if (value === void 0) return void 0;
    return {
      value,
      writable: false,
      enumerable: true,
      configurable: false
    };
  }
  set() {
    return false;
  }
  setPrototypeOf() {
    return false;
  }
  defineProperty() {
    return false;
  }
  deleteProperty() {
    return false;
  }
  preventExtensions() {
    return false;
  }
};

// node_modules/@assistant-ui/store/dist/utils/tap-client-stack-context.js
var SYMBOL_CLIENT_INDEX = /* @__PURE__ */ Symbol("assistant-ui.store.clientIndex");
var getClientIndex = (client) => {
  return client[SYMBOL_CLIENT_INDEX];
};
var ClientStackContext = createResourceContext([]);
var tapClientStack = () => {
  return tap(ClientStackContext);
};
var tapWithClientStack = (client, callback) => {
  const currentStack = tapClientStack();
  return withContextProvider(ClientStackContext, tapMemo(() => [...currentStack, client], [currentStack, client]), callback);
};

// node_modules/@assistant-ui/store/dist/wrapperResource.js
var wrapperResource = (fn) => {
  const res = resource(fn);
  return (props) => {
    const el = res(props);
    if (props.key === void 0) return el;
    return withKey(props.key, el);
  };
};

// node_modules/@assistant-ui/store/dist/tapClientResource.js
var SYMBOL_GET_OUTPUT = /* @__PURE__ */ Symbol("assistant-ui.store.getValue");
var getClientState = (client) => {
  const output = client[SYMBOL_GET_OUTPUT];
  if (!output) throw new Error("Client scope contains a non-client resource. Ensure your Derived get() returns a client created with tapClientResource(), not a plain resource.");
  return output.getState?.();
};
var fieldAccessFns = /* @__PURE__ */ new Map();
function getOrCreateProxyFn(prop) {
  let template = fieldAccessFns.get(prop);
  if (!template) {
    template = function(...args) {
      if (!this || typeof this !== "object") throw new Error(`Method "${String(prop)}" called without proper context. This may indicate the function was called incorrectly.`);
      const output = this[SYMBOL_GET_OUTPUT];
      if (!output) throw new Error(`Method "${String(prop)}" called on invalid client proxy. Ensure you are calling this method on a valid client instance.`);
      const method = output[prop];
      if (!method) throw new Error(`Method "${String(prop)}" is not implemented.`);
      if (typeof method !== "function") throw new Error(`"${String(prop)}" is not a function.`);
      return method(...args);
    };
    fieldAccessFns.set(prop, template);
  }
  return template;
}
var ClientProxyHandler = class extends BaseProxyHandler {
  outputRef;
  index;
  boundFns;
  cachedReceiver;
  constructor(outputRef, index) {
    super();
    this.outputRef = outputRef;
    this.index = index;
  }
  get(_, prop, receiver) {
    if (prop === SYMBOL_GET_OUTPUT) return this.outputRef.current;
    if (prop === SYMBOL_CLIENT_INDEX) return this.index;
    const introspection = handleIntrospectionProp(prop, "ClientProxy");
    if (introspection !== false) return introspection;
    const value = this.outputRef.current[prop];
    if (typeof value === "function") {
      if (this.cachedReceiver !== receiver) {
        this.boundFns = /* @__PURE__ */ new Map();
        this.cachedReceiver = receiver;
      }
      let bound = this.boundFns.get(prop);
      if (!bound) {
        bound = getOrCreateProxyFn(prop).bind(receiver);
        this.boundFns.set(prop, bound);
      }
      return bound;
    }
    return value;
  }
  ownKeys() {
    return Object.keys(this.outputRef.current);
  }
  has(_, prop) {
    if (prop === SYMBOL_GET_OUTPUT) return true;
    if (prop === SYMBOL_CLIENT_INDEX) return true;
    return prop in this.outputRef.current;
  }
};
var ClientResource = wrapperResource((element) => {
  const valueRef = tapRef(null);
  const index = tapClientStack().length;
  const methods = tapMemo(() => new Proxy({}, new ClientProxyHandler(valueRef, index)), [index]);
  const value = tapWithClientStack(methods, () => tapResource(element));
  if (!valueRef.current) valueRef.current = value;
  tapEffect(() => {
    valueRef.current = value;
  });
  return {
    methods,
    state: value.getState?.(),
    key: element.key
  };
});
var tapClientResource = (element) => {
  return tapResource(ClientResource(element));
};

// node_modules/@assistant-ui/store/dist/utils/proxied-assistant-state.js
var PROXIED_ASSISTANT_STATE_SYMBOL = /* @__PURE__ */ Symbol("assistant-ui.store.proxiedAssistantState");
var isIgnoredKey = (key) => {
  return key === "on" || key === "subscribe" || typeof key === "symbol";
};
var createProxiedAssistantState = (client) => {
  class ProxiedAssistantStateProxyHandler extends BaseProxyHandler {
    get(_, prop) {
      const introspection = handleIntrospectionProp(prop, "AssistantState");
      if (introspection !== false) return introspection;
      const scope = prop;
      if (isIgnoredKey(scope)) return void 0;
      return getClientState(client[scope]());
    }
    ownKeys() {
      return Object.keys(client).filter((key) => !isIgnoredKey(key));
    }
    has(_, prop) {
      return !isIgnoredKey(prop) && prop in client;
    }
  }
  return new Proxy({}, new ProxiedAssistantStateProxyHandler());
};
var getProxiedAssistantState = (client) => {
  return client[PROXIED_ASSISTANT_STATE_SYMBOL];
};

// node_modules/@assistant-ui/store/dist/utils/react-assistant-context.js
var import_react = __toESM(require_react(), 1);
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var NO_OP_SUBSCRIBE = () => () => {
};
var createErrorClientField = (message) => {
  const fn = (() => {
    throw new Error(message);
  });
  fn.source = null;
  fn.query = null;
  return fn;
};
var DefaultAssistantClientProxyHandler = class extends BaseProxyHandler {
  get(_, prop) {
    if (prop === "subscribe") return NO_OP_SUBSCRIBE;
    if (prop === "on") return NO_OP_SUBSCRIBE;
    if (prop === PROXIED_ASSISTANT_STATE_SYMBOL) return DefaultAssistantClientProxiedAssistantState;
    const introspection = handleIntrospectionProp(prop, "DefaultAssistantClient");
    if (introspection !== false) return introspection;
    return createErrorClientField("You are using a component or hook that requires an AuiProvider. Wrap your component in an <AuiProvider> component.");
  }
  ownKeys() {
    return [
      "subscribe",
      "on",
      PROXIED_ASSISTANT_STATE_SYMBOL
    ];
  }
  has(_, prop) {
    return prop === "subscribe" || prop === "on" || prop === PROXIED_ASSISTANT_STATE_SYMBOL;
  }
};
var DefaultAssistantClient = new Proxy({}, new DefaultAssistantClientProxyHandler());
var DefaultAssistantClientProxiedAssistantState = createProxiedAssistantState(DefaultAssistantClient);
var createRootAssistantClient = () => new Proxy({}, { get(_, prop) {
  const introspection = handleIntrospectionProp(prop, "AssistantClient");
  if (introspection !== false) return introspection;
  return createErrorClientField(`The current scope does not have a "${String(prop)}" property.`);
} });
var AssistantContext = (0, import_react.createContext)(DefaultAssistantClient);
var useAssistantContextValue = () => {
  return (0, import_react.useContext)(AssistantContext);
};
var AuiProvider = ({ value, children }) => {
  return (0, import_jsx_runtime.jsx)(AssistantContext.Provider, {
    value,
    children
  });
};

// node_modules/@assistant-ui/store/dist/Derived.js
var Derived = resource((_config) => {
  return null;
});

// node_modules/@assistant-ui/store/dist/attachTransformScopes.js
var TRANSFORM_SCOPES = /* @__PURE__ */ Symbol("assistant-ui.transform-scopes");
function attachTransformScopes(resource2, transform) {
  const r = resource2;
  if (r[TRANSFORM_SCOPES]) throw new Error("transformScopes is already attached to this resource");
  r[TRANSFORM_SCOPES] = transform;
}
function getTransformScopes(resource2) {
  return resource2[TRANSFORM_SCOPES];
}

// node_modules/@assistant-ui/store/dist/utils/splitClients.js
function splitClients(clients, baseClient) {
  const scopes = { ...clients };
  const visited = /* @__PURE__ */ new Set();
  let changed = true;
  while (changed) {
    changed = false;
    for (const clientElement of Object.values(scopes)) {
      if (clientElement.type === Derived) continue;
      if (visited.has(clientElement.type)) continue;
      visited.add(clientElement.type);
      const transform = getTransformScopes(clientElement.type);
      if (transform) {
        transform(scopes, baseClient);
        changed = true;
        break;
      }
    }
  }
  const rootClients = {};
  const derivedClients = {};
  for (const [key, clientElement] of Object.entries(scopes)) if (clientElement.type === Derived) derivedClients[key] = clientElement;
  else rootClients[key] = clientElement;
  return {
    rootClients,
    derivedClients
  };
}
var tapShallowMemoObject = (object) => {
  return tapMemo(() => object, [...Object.entries(object).flat()]);
};
var tapSplitClients = (clients, baseClient) => {
  const { rootClients, derivedClients } = splitClients(clients, baseClient);
  return {
    rootClients: tapShallowMemoObject(rootClients),
    derivedClients: tapShallowMemoObject(derivedClients)
  };
};

// node_modules/@assistant-ui/store/dist/types/events.js
var normalizeEventSelector = (selector) => {
  if (typeof selector === "string") return {
    scope: selector.split(".")[0],
    event: selector
  };
  return {
    scope: selector.scope,
    event: selector.event
  };
};

// node_modules/@assistant-ui/store/dist/utils/NotificationManager.js
var NotificationManager = resource(() => {
  return tapConst(() => {
    const listeners = /* @__PURE__ */ new Map();
    const wildcardListeners = /* @__PURE__ */ new Set();
    const subscribers = /* @__PURE__ */ new Set();
    return {
      on(event, callback) {
        const cb = callback;
        if (event === "*") {
          wildcardListeners.add(cb);
          return () => wildcardListeners.delete(cb);
        }
        let set = listeners.get(event);
        if (!set) {
          set = /* @__PURE__ */ new Set();
          listeners.set(event, set);
        }
        set.add(cb);
        return () => {
          set.delete(cb);
          if (set.size === 0) listeners.delete(event);
        };
      },
      emit(event, payload, clientStack) {
        const eventListeners = listeners.get(event);
        if (!eventListeners && wildcardListeners.size === 0) return;
        queueMicrotask(() => {
          const errors = [];
          if (eventListeners) for (const cb of eventListeners) try {
            cb(payload, clientStack);
          } catch (e) {
            errors.push(e);
          }
          if (wildcardListeners.size > 0) {
            const wrapped = {
              event,
              payload
            };
            for (const cb of wildcardListeners) try {
              cb(wrapped, clientStack);
            } catch (e) {
              errors.push(e);
            }
          }
          if (errors.length > 0) if (errors.length === 1) throw errors[0];
          else {
            for (const error of errors) console.error(error);
            throw new AggregateError(errors, "Errors occurred during event emission");
          }
        });
      },
      subscribe(callback) {
        subscribers.add(callback);
        return () => subscribers.delete(callback);
      },
      notifySubscribers() {
        for (const cb of subscribers) try {
          cb();
        } catch (e) {
          console.error("NotificationManager: subscriber callback error", e);
        }
      }
    };
  }, []);
});

// node_modules/@assistant-ui/store/dist/utils/tap-assistant-context.js
var AssistantTapContext = createResourceContext(null);
var withAssistantTapContextProvider = (value, fn) => {
  return withContextProvider(AssistantTapContext, value, fn);
};
var tapAssistantTapContext = () => {
  const ctx = tap(AssistantTapContext);
  if (!ctx) throw new Error("AssistantTapContext is not available");
  return ctx;
};
var tapAssistantClientRef = () => {
  return tapAssistantTapContext().clientRef;
};
var tapAssistantEmit = () => {
  const { emit } = tapAssistantTapContext();
  const clientStack = tapClientStack();
  return tapEffectEvent((event, payload) => {
    emit(event, payload, clientStack);
  });
};

// node_modules/@assistant-ui/tap/dist/react/use-resource.js
var import_react2 = __toESM(require_react(), 1);
var useDevStrictMode = () => {
  if (!isDevelopment) return null;
  const count = (0, import_react2.useRef)(0);
  const isFirstRender = count.current === 0;
  (0, import_react2.useState)(() => count.current++);
  if (count.current !== 2) return null;
  return isFirstRender ? "child" : "root";
};
function useResource(element) {
  const root = (0, import_react2.useMemo)(() => {
    return createResourceFiberRoot((cb) => dispatch(cb));
  }, []);
  const [version, dispatch] = (0, import_react2.useReducer)((v, cb) => {
    setRootVersion(root, v);
    return v + (cb() ? 1 : 0);
  }, 0);
  setRootVersion(root, version);
  const devStrictMode = useDevStrictMode();
  const fiber = (0, import_react2.useMemo)(() => {
    element.key;
    return createResourceFiber(element.type, root, void 0, devStrictMode);
  }, [
    element.type,
    element.key,
    root,
    devStrictMode
  ]);
  const result = renderResourceFiber(fiber, element.props);
  (0, import_react2.useLayoutEffect)(() => {
    return () => unmountResourceFiber(fiber);
  }, [fiber]);
  (0, import_react2.useLayoutEffect)(() => {
    commitRoot(root);
    commitResourceFiber(fiber, result);
  });
  return result.output;
}

// node_modules/@assistant-ui/store/dist/useAui.js
var tapShallowMemoArray = (array) => {
  return tapMemo(() => array, array);
};
var RootClientResource = resource(({ element, emit, clientRef }) => {
  const { methods, state } = withAssistantTapContextProvider({
    clientRef,
    emit
  }, () => tapClientResource(element));
  return tapMemo(() => ({
    state,
    methods
  }), [methods, state]);
});
var RootClientAccessorResource = resource(({ element, notifications, clientRef, name }) => {
  const store = tapResourceRoot(RootClientResource({
    element,
    emit: notifications.emit,
    clientRef
  }));
  tapEffect(() => {
    return store.subscribe(notifications.notifySubscribers);
  }, [store, notifications]);
  return tapMemo(() => {
    const clientFunction = () => store.getValue().methods;
    Object.defineProperties(clientFunction, {
      source: {
        value: "root",
        writable: false
      },
      query: {
        value: {},
        writable: false
      },
      name: {
        value: name,
        configurable: true
      }
    });
    return clientFunction;
  }, [store, name]);
});
var NoOpRootClientsAccessorsResource = resource(() => {
  return tapMemo(() => ({
    clients: [],
    subscribe: void 0,
    on: void 0
  }), []);
});
var RootClientsAccessorsResource = resource(({ clients: inputClients, clientRef }) => {
  const notifications = tapResource(NotificationManager());
  tapEffect(() => clientRef.parent.subscribe(notifications.notifySubscribers), [clientRef, notifications]);
  const results = tapShallowMemoArray(tapResources(() => Object.keys(inputClients).map((key) => withKey(key, RootClientAccessorResource({
    element: inputClients[key],
    notifications,
    clientRef,
    name: key
  }))), [
    inputClients,
    notifications,
    clientRef
  ]));
  return tapMemo(() => {
    return {
      clients: results,
      subscribe: notifications.subscribe,
      on: function(selector, callback) {
        if (!this) throw new Error("const { on } = useAui() is not supported. Use aui.on() instead.");
        const { scope, event } = normalizeEventSelector(selector);
        if (scope !== "*") {
          if (this[scope].source === null) throw new Error(`Scope "${scope}" is not available. Use { scope: "*", event: "${event}" } to listen globally.`);
        }
        const localUnsub = notifications.on(event, (payload, clientStack) => {
          if (scope === "*") {
            callback(payload);
            return;
          }
          const scopeClient = this[scope]();
          if (scopeClient === clientStack[getClientIndex(scopeClient)]) callback(payload);
        });
        if (scope !== "*" && clientRef.parent[scope].source === null) return localUnsub;
        const parentUnsub = clientRef.parent.on(selector, callback);
        return () => {
          localUnsub();
          parentUnsub();
        };
      }
    };
  }, [
    results,
    notifications,
    clientRef
  ]);
});
var DerivedClientAccessorResource = resource(({ element, clientRef, name }) => {
  const propsRef = tapRef(element.props);
  propsRef.current = element.props;
  return tapMemo(() => {
    const clientFunction = () => propsRef.current.get(clientRef.current);
    Object.defineProperties(clientFunction, {
      source: { value: propsRef.current.source },
      query: { value: propsRef.current.query },
      name: {
        value: name,
        configurable: true
      }
    });
    return clientFunction;
  }, [clientRef, name]);
});
var serializeMeta = (name, meta) => {
  let queryKey;
  try {
    const sorted = {};
    for (const k of Object.keys(meta.query).sort()) sorted[k] = meta.query[k];
    queryKey = JSON.stringify(sorted);
  } catch {
    queryKey = String(meta.query);
  }
  return `${name}::${meta.source}::${queryKey}`;
};
var DerivedClientsAccessorsResource = resource(({ clients, clientRef }) => {
  return tapShallowMemoArray(tapResources(() => Object.keys(clients).map((key) => {
    const name = key;
    const element = clients[name];
    return withKey(serializeMeta(name, element.props), DerivedClientAccessorResource({
      element,
      clientRef,
      name
    }));
  }), [clients, clientRef]));
});
var AssistantClientResource = resource(({ parent, clients }) => {
  const { rootClients, derivedClients } = tapSplitClients(clients, parent);
  const clientRef = tapRef({
    parent,
    current: null
  }).current;
  tapEffect(() => {
    clientRef.current = client;
  });
  const rootFields = tapResource(Object.keys(rootClients).length > 0 ? RootClientsAccessorsResource({
    clients: rootClients,
    clientRef
  }) : NoOpRootClientsAccessorsResource());
  const derivedFields = tapResource(DerivedClientsAccessorsResource({
    clients: derivedClients,
    clientRef
  }));
  const client = tapMemo(() => {
    const proto = parent === DefaultAssistantClient ? createRootAssistantClient() : parent;
    const client2 = Object.create(proto);
    Object.assign(client2, {
      subscribe: rootFields.subscribe ?? parent.subscribe,
      on: rootFields.on ?? parent.on,
      [PROXIED_ASSISTANT_STATE_SYMBOL]: createProxiedAssistantState(client2)
    });
    for (const field of rootFields.clients) client2[field.name] = field;
    for (const field of derivedFields) client2[field.name] = field;
    return client2;
  }, [
    parent,
    rootFields,
    derivedFields
  ]);
  if (clientRef.current === null) clientRef.current = client;
  return client;
});
function useAui(clients, { parent } = { parent: useAssistantContextValue() }) {
  if (clients) return useResource(AssistantClientResource({
    parent: parent ?? DefaultAssistantClient,
    clients
  }));
  if (parent === null) throw new Error("received null parent, this usage is not allowed");
  return parent;
}

// node_modules/@assistant-ui/store/dist/useAuiState.js
var import_react4 = __toESM(require_react(), 1);
var useAuiState = (selector) => {
  const aui = useAui();
  const proxiedState = getProxiedAssistantState(aui);
  const slice = (0, import_react4.useSyncExternalStore)(aui.subscribe, () => selector(proxiedState), () => selector(proxiedState));
  if (slice === proxiedState) throw new Error("You tried to return the entire AssistantState. This is not supported due to technical limitations.");
  (0, import_react4.useDebugValue)(slice);
  return slice;
};

// node_modules/@assistant-ui/store/dist/AuiIf.js
var AuiIf = ({ children, condition }) => {
  return useAuiState(condition) ? children : null;
};
AuiIf.displayName = "AuiIf";

// node_modules/@assistant-ui/store/dist/useAuiEvent.js
var import_react6 = __toESM(require_react(), 1);

// node_modules/use-effect-event/dist/index.js
var import_react5 = __toESM(require_react());
var context = import_react5.default.createContext(true);
function forbiddenInRender() {
  throw new Error("A function wrapped in useEffectEvent can't be called during rendering.");
}
var isInvalidExecutionContextForEventFunction = "use" in import_react5.default ? () => {
  try {
    return import_react5.default.use(context);
  } catch {
    return false;
  }
} : () => false;
function useEffectEvent(fn) {
  const ref = import_react5.default.useRef(forbiddenInRender);
  return import_react5.default.useInsertionEffect(() => {
    ref.current = fn;
  }, [fn]), (...args) => {
    isInvalidExecutionContextForEventFunction() && forbiddenInRender();
    const latestFn = ref.current;
    return latestFn(...args);
  };
}

// node_modules/@assistant-ui/store/dist/useAuiEvent.js
var useAuiEvent = (selector, callback) => {
  const aui = useAui();
  const callbackRef = useEffectEvent(callback);
  const { scope, event } = normalizeEventSelector(selector);
  (0, import_react6.useEffect)(() => aui.on({
    scope,
    event
  }, callbackRef), [
    aui,
    scope,
    event
  ]);
};

// node_modules/@assistant-ui/store/dist/RenderChildrenWithAccessor.js
var import_react7 = __toESM(require_react(), 1);
var useGetItemAccessor = (getItemState) => {
  const aui = useAui();
  const accessedRef = (0, import_react7.useRef)(false);
  const currentValue = accessedRef.current ? null : getItemState(aui);
  useAuiState(() => {
    if (!accessedRef.current) return currentValue;
    return getItemState(aui);
  });
  return () => {
    accessedRef.current = true;
    return getItemState(aui);
  };
};
var EMPTY_OBJECT = Object.freeze({});
function RenderChildrenWithAccessor({ getItemState, children }) {
  return useMemoizedProplessComponent(children(useGetItemAccessor(getItemState)));
}
var useMemoizedProplessComponent = (node) => {
  const el = typeof node === "object" && node != null && "type" in node ? node : null;
  const resultType = el?.type;
  const resultKey = el?.key;
  return (0, import_react7.useMemo)(() => el, [
    resultType,
    resultKey,
    typeof el?.props === "object" && el.props != null && Object.entries(el.props).length === 0 ? EMPTY_OBJECT : el?.props
  ]) ?? node;
};

// node_modules/@assistant-ui/store/dist/tapClientLookup.js
var ClientResourceWithKey = wrapperResource((el) => {
  if (el.key === void 0) throw new Error("tapClientResource: Element has no key");
  return tapResource(ClientResource(el));
});
function tapClientLookup(getElements, getElementsDeps) {
  const resources = tapResources(() => getElements().map((el) => ClientResourceWithKey(el)), getElementsDeps);
  const keys = tapMemo(() => Object.keys(resources), [resources]);
  const keyToIndex = tapMemo(() => {
    return resources.reduce((acc, resource2, index) => {
      acc[resource2.key] = index;
      return acc;
    }, {});
  }, [resources]);
  return {
    state: tapMemo(() => {
      return resources.map((r) => r.state);
    }, [resources]),
    get: (lookup) => {
      if ("index" in lookup) {
        if (lookup.index < 0 || lookup.index >= keys.length) throw new Error(`tapClientLookup: Index ${lookup.index} out of bounds (length: ${keys.length})`);
        return resources[lookup.index].methods;
      }
      const index = keyToIndex[lookup.key];
      if (index === void 0) throw new Error(`tapClientLookup: Key "${lookup.key}" not found`);
      return resources[index].methods;
    }
  };
}

// node_modules/@assistant-ui/core/dist/react/model-context/toolbox.js
var isStandaloneToolDisplay = (tool) => {
  if (tool.display !== void 0) return tool.display === "standalone";
  return tool.type === "human";
};
var resolveToolCallText = (text, part) => {
  if (!(part.status?.type === "running" || part.status?.type === "requires-action")) {
    const value2 = text.complete;
    if (typeof value2 !== "function") return value2 ?? null;
    return value2({
      args: part.args,
      result: part.result
    });
  }
  const value = text.running;
  if (typeof value !== "function") return value ?? null;
  return value({ args: part.args });
};
var makeToolCallTextComponent = (text) => {
  return function ToolCallTextComponent(part) {
    return resolveToolCallText(text, part);
  };
};

// node_modules/@assistant-ui/core/dist/react/client/Tools.js
var Tools = resource(({ toolkit, mcpApp }) => {
  const mcpAppOutput = tapResources(() => mcpApp ? [withKey("mcpApp", mcpApp)] : [], [mcpApp])[0];
  const [toolUIs, setToolUIs] = tapState(() => ({}));
  const state = tapMemo(() => ({
    toolUIs,
    mcpApp: mcpAppOutput,
    tools: Object.fromEntries(Object.entries(toolUIs).map(([name, regs]) => [name, regs.map((r) => r.render)]))
  }), [toolUIs, mcpAppOutput]);
  const clientRef = tapAssistantClientRef();
  const setToolUI = tapCallback((toolName, render, options) => {
    const registration = {
      render,
      standalone: options?.standalone ?? false
    };
    setToolUIs((prev) => ({
      ...prev,
      [toolName]: [...prev[toolName] ?? [], registration]
    }));
    return () => {
      setToolUIs((prev) => {
        const next = prev[toolName]?.filter((r) => r !== registration) ?? [];
        if (next.length > 0) return {
          ...prev,
          [toolName]: next
        };
        const { [toolName]: _removed, ...rest } = prev;
        return rest;
      });
    };
  }, []);
  tapEffect(() => {
    if (!toolkit) return;
    const unsubscribes = [];
    for (const [toolName, tool] of Object.entries(toolkit)) {
      const toolRender = "render" in tool ? tool.render : void 0;
      const toolRenderText = "renderText" in tool ? tool.renderText : void 0;
      const render = toolRender ?? (toolRenderText ? makeToolCallTextComponent(toolRenderText) : void 0);
      if (render) unsubscribes.push(setToolUI(toolName, render, { standalone: isStandaloneToolDisplay(tool) }));
    }
    const toolsWithoutRender = Object.entries(toolkit).reduce((acc, [name, tool]) => {
      if (tool.type === "mcp") return acc;
      const { display: _display, render: _render, renderText: _renderText, ...rest } = tool;
      acc[name] = rest;
      return acc;
    }, {});
    unsubscribes.push(clientRef.current.modelContext().register({ getModelContext: () => ({ tools: toolsWithoutRender }) }));
    return () => {
      unsubscribes.forEach((fn) => fn());
    };
  }, [
    toolkit,
    setToolUI,
    clientRef
  ]);
  return {
    getState: () => state,
    setToolUI
  };
});
attachTransformScopes(Tools, (scopes, parent) => {
  if (!scopes.modelContext && parent.modelContext.source === null) scopes.modelContext = ModelContext();
});

// node_modules/@assistant-ui/core/dist/store/clients/suggestions.js
var SuggestionClient = resource((state) => {
  return { getState: () => state };
});
var Suggestions = resource((suggestions) => {
  const [state] = tapState(() => {
    return { suggestions: (suggestions ?? []).map((s) => {
      if (typeof s === "string") return {
        title: s,
        label: "",
        prompt: s
      };
      return {
        title: s.title,
        label: s.label,
        prompt: s.prompt
      };
    }) };
  });
  const suggestionClients = tapClientLookup(() => state.suggestions.map((suggestion, index) => withKey(index, SuggestionClient(suggestion))), [state.suggestions]);
  return {
    getState: () => state,
    suggestion: ({ index }) => {
      return suggestionClients.get({ index });
    }
  };
});

// node_modules/@assistant-ui/core/dist/react/model-context/useAssistantTool.js
var import_react8 = __toESM(require_react(), 1);
var useAssistantTool = (tool) => {
  const aui = useAui();
  const standalone = isStandaloneToolDisplay(tool);
  const renderTextComponent = (0, import_react8.useMemo)(() => tool.renderText ? makeToolCallTextComponent(tool.renderText) : void 0, [tool.renderText]);
  const render = tool.render ?? renderTextComponent;
  (0, import_react8.useEffect)(() => {
    if (!render) return void 0;
    return aui.tools().setToolUI(tool.toolName, render, { standalone });
  }, [
    aui,
    tool.toolName,
    render,
    standalone
  ]);
  (0, import_react8.useEffect)(() => {
    const { toolName, render: render2, renderText, display, ...rest } = tool;
    const context2 = { tools: { [toolName]: rest } };
    return aui.modelContext().register({ getModelContext: () => context2 });
  }, [aui, tool]);
};

// node_modules/@assistant-ui/core/dist/react/model-context/makeAssistantTool.js
var makeAssistantTool = (tool) => {
  const Tool = () => {
    useAssistantTool(tool);
    return null;
  };
  Tool.unstable_tool = tool;
  return Tool;
};

// node_modules/@assistant-ui/core/dist/react/model-context/useAssistantToolUI.js
var import_react9 = __toESM(require_react(), 1);
var useAssistantToolUI = (tool) => {
  const aui = useAui();
  const standalone = tool?.display === "standalone";
  (0, import_react9.useEffect)(() => {
    if (!tool?.toolName || !tool?.render) return void 0;
    return aui.tools().setToolUI(tool.toolName, tool.render, { standalone });
  }, [
    aui,
    tool?.toolName,
    tool?.render,
    standalone
  ]);
};

// node_modules/@assistant-ui/core/dist/react/model-context/makeAssistantToolUI.js
var makeAssistantToolUI = (tool) => {
  const ToolUI = () => {
    useAssistantToolUI(tool);
    return null;
  };
  ToolUI.unstable_tool = tool;
  return ToolUI;
};

// node_modules/@assistant-ui/core/dist/react/model-context/useAssistantDataUI.js
var import_react10 = __toESM(require_react(), 1);
var useAssistantDataUI = (dataUI) => {
  const aui = useAui();
  (0, import_react10.useEffect)(() => {
    if (!dataUI?.name || !dataUI?.render) return void 0;
    return aui.dataRenderers().setDataUI(dataUI.name, dataUI.render);
  }, [
    aui,
    dataUI?.name,
    dataUI?.render
  ]);
};

// node_modules/@assistant-ui/core/dist/react/model-context/makeAssistantDataUI.js
var makeAssistantDataUI = (dataUI) => {
  const DataUI = () => {
    useAssistantDataUI(dataUI);
    return null;
  };
  DataUI.unstable_data = dataUI;
  return DataUI;
};

// node_modules/@assistant-ui/core/dist/react/model-context/useAssistantInstructions.js
var import_react11 = __toESM(require_react(), 1);
var getInstructions = (instruction) => {
  if (typeof instruction === "string") return { instruction };
  return instruction;
};
var useAssistantInstructions = (config) => {
  const { instruction, disabled = false } = getInstructions(config);
  const aui = useAui();
  (0, import_react11.useEffect)(() => {
    if (disabled) return;
    const config2 = { system: instruction };
    return aui.modelContext().register({ getModelContext: () => config2 });
  }, [
    aui,
    instruction,
    disabled
  ]);
};

// node_modules/@assistant-ui/core/dist/react/model-context/useAssistantContext.js
var import_react12 = __toESM(require_react(), 1);
var useAssistantContext = (config) => {
  const { getContext, disabled = false } = config;
  const aui = useAui();
  const getContextRef = (0, import_react12.useRef)(getContext);
  getContextRef.current = getContext;
  (0, import_react12.useEffect)(() => {
    if (disabled) return;
    return aui.modelContext().register({ getModelContext: () => ({ system: getContextRef.current() }) });
  }, [aui, disabled]);
};

// node_modules/@assistant-ui/core/dist/react/model-context/useInlineRender.js
var import_react14 = __toESM(require_react(), 1);

// node_modules/zustand/esm/vanilla.mjs
var createStoreImpl = (createState) => {
  let state;
  const listeners = /* @__PURE__ */ new Set();
  const setState = (partial, replace) => {
    const nextState = typeof partial === "function" ? partial(state) : partial;
    if (!Object.is(nextState, state)) {
      const previousState = state;
      state = (replace != null ? replace : typeof nextState !== "object" || nextState === null) ? nextState : Object.assign({}, state, nextState);
      listeners.forEach((listener) => listener(state, previousState));
    }
  };
  const getState = () => state;
  const getInitialState = () => initialState;
  const subscribe = (listener) => {
    listeners.add(listener);
    return () => listeners.delete(listener);
  };
  const api = { setState, getState, getInitialState, subscribe };
  const initialState = state = createState(setState, getState, api);
  return api;
};
var createStore = ((createState) => createState ? createStoreImpl(createState) : createStoreImpl);

// node_modules/zustand/esm/react.mjs
var import_react13 = __toESM(require_react(), 1);
var identity = (arg) => arg;
function useStore(api, selector = identity) {
  const slice = import_react13.default.useSyncExternalStore(
    api.subscribe,
    import_react13.default.useCallback(() => selector(api.getState()), [api, selector]),
    import_react13.default.useCallback(() => selector(api.getInitialState()), [api, selector])
  );
  import_react13.default.useDebugValue(slice);
  return slice;
}
var createImpl = (createState) => {
  const api = createStore(createState);
  const useBoundStore = (selector) => useStore(api, selector);
  Object.assign(useBoundStore, api);
  return useBoundStore;
};
var create = ((createState) => createState ? createImpl(createState) : createImpl);

// node_modules/@assistant-ui/core/dist/react/model-context/useInlineRender.js
var useInlineRender = (toolUI) => {
  const [useToolUIStore] = (0, import_react14.useState)(() => create(() => ({ toolUI })));
  (0, import_react14.useEffect)(() => {
    useToolUIStore.setState({ toolUI });
  }, [toolUI, useToolUIStore]);
  return (0, import_react14.useCallback)(function ToolUI(args) {
    return useToolUIStore().toolUI(args);
  }, [useToolUIStore]);
};

// node_modules/@assistant-ui/core/dist/react/model-context/define-toolkit.js
function defineToolkit(_definition) {
  throw new Error('[assistant-ui] defineToolkit() has no runtime implementation — it is stripped at build time by the use-generative compiler. Reaching it means this module was not compiled (e.g. defineToolkit used outside a "use generative" file). Add the directive, or do not use defineToolkit here.');
}

// node_modules/@assistant-ui/core/dist/react/model-context/stub-tool.js
function stubTool() {
  throw new Error('[assistant-ui] stubTool() has no runtime implementation - it marks a tool executor that must be supplied via useAuiToolOverrides(...). Make sure this module is compiled as "use generative".');
}

// node_modules/@assistant-ui/core/dist/react/model-context/useAuiToolOverrides.js
var import_react15 = __toESM(require_react(), 1);
function useAuiToolOverrides(overrides) {
  const aui = useAui();
  const overridesRef = (0, import_react15.useRef)(overrides);
  overridesRef.current = overrides;
  (0, import_react15.useEffect)(() => {
    return aui.modelContext().register({ getModelContext: () => ({
      priority: 1e3,
      tools: overridesRef.current
    }) });
  }, [aui]);
}

// node_modules/@assistant-ui/core/dist/react/model-context/hitl.js
function hitlTool() {
  throw new Error('[assistant-ui] hitlTool() has no runtime implementation — it marks a human-in-the-loop tool and is stripped at build time by the use-generative compiler. Reaching it means this module was not compiled (e.g. hitlTool() used outside a "use generative" file).');
}
var hitl = hitlTool;

// node_modules/@assistant-ui/core/dist/react/model-context/provider-tool.js
function providerTool(_config) {
  throw new Error('[assistant-ui] providerTool() has no runtime implementation — it marks a provider-executed tool and is stripped at build time by the use-generative compiler. Reaching it means this module was not compiled (e.g. providerTool() used outside a "use generative" file).');
}

// node_modules/@assistant-ui/core/dist/react/model-context/define-mcp-toolkit.js
function defineMcpToolkit(definition) {
  return Object.fromEntries(Object.entries(definition).map(([name, server]) => [name, {
    type: "mcp",
    server
  }]));
}

// node_modules/@assistant-ui/core/dist/react/model-context/useAssistantInteractable.js
var import_react16 = __toESM(require_react(), 1);
var useAssistantInteractable = (name, config) => {
  const aui = useAui();
  const autoId = (0, import_react16.useId)().replace(/[^a-zA-Z0-9]/g, "");
  const id = config.id ?? autoId;
  const stateSchemaRef = (0, import_react16.useRef)(config.stateSchema);
  stateSchemaRef.current = config.stateSchema;
  const initialStateRef = (0, import_react16.useRef)(config.initialState);
  initialStateRef.current = config.initialState;
  (0, import_react16.useEffect)(() => {
    return aui.interactables().register({
      id,
      name,
      description: config.description,
      stateSchema: stateSchemaRef.current,
      initialState: initialStateRef.current,
      selected: config.selected
    });
  }, [
    aui,
    id,
    name,
    config.description,
    config.selected
  ]);
  return id;
};

// node_modules/@assistant-ui/core/dist/react/model-context/useInteractableState.js
var import_react17 = __toESM(require_react(), 1);
var useInteractableState = (id, fallback) => {
  const aui = useAui();
  const state = useAuiState((s) => s.interactables.definitions[id]?.state) ?? fallback;
  const persistenceStatus = useAuiState((s) => s.interactables.persistence[id]);
  const setState = (0, import_react17.useCallback)((updater) => {
    aui.interactables().setState(id, (prev) => {
      if (typeof updater === "function") return updater(prev);
      return updater;
    });
  }, [aui, id]);
  const setSelected = (0, import_react17.useCallback)((selected) => {
    aui.interactables().setSelected(id, selected);
  }, [aui, id]);
  const flush = (0, import_react17.useCallback)(() => aui.interactables().flush(), [aui]);
  return [state, {
    setState,
    setSelected,
    isPending: persistenceStatus?.isPending ?? false,
    error: persistenceStatus?.error,
    flush
  }];
};

// node_modules/@assistant-ui/core/dist/react/model-context/useToolArgsStatus.js
var import_react18 = __toESM(require_react(), 1);
var useToolArgsStatus = () => {
  const part = useAuiState((s) => s.part);
  return (0, import_react18.useMemo)(() => {
    const statusType = part.status.type;
    if (part.type !== "tool-call") throw new Error("useToolArgsStatus can only be used inside tool-call message parts");
    const isStreaming = statusType === "running";
    const args = part.args;
    const meta = getPartialJsonObjectMeta(args);
    const propStatus = {};
    for (const key of Object.keys(args)) if (meta) propStatus[key] = getPartialJsonObjectFieldState(args, [key]) === "complete" || !isStreaming ? "complete" : "streaming";
    else propStatus[key] = isStreaming ? "streaming" : "complete";
    return {
      status: statusType,
      propStatus
    };
  }, [part]);
};

// node_modules/@assistant-ui/core/dist/react/client/interactable-model-context.js
function shallowMerge(prev, partial) {
  if (typeof prev !== "object" || prev === null || typeof partial !== "object" || partial === null || Array.isArray(prev) || Array.isArray(partial)) return partial;
  return {
    ...prev,
    ...partial
  };
}
function buildInteractableModelContext(definitions, partialSchemaCache, setDefState) {
  const entries = Object.values(definitions);
  if (entries.length === 0) return void 0;
  const byName = /* @__PURE__ */ new Map();
  for (const def of entries) {
    const list = byName.get(def.name) ?? [];
    list.push(def);
    byName.set(def.name, list);
  }
  const systemParts = [];
  const tools = {};
  for (const [name, instances] of byName) {
    const isMulti = instances.length > 1;
    for (const def of instances) {
      const selectedTag = def.selected ? " (SELECTED)" : "";
      const idTag = isMulti ? ` [id="${def.id}"]` : "";
      systemParts.push(`Interactable component "${name}"${idTag}${selectedTag} (${def.description}). Current state: ${JSON.stringify(def.state)}`);
      const safeName = name.replace(/[^a-zA-Z0-9_-]/g, "_");
      const safeId = def.id.replace(/[^a-zA-Z0-9_-]/g, "_");
      const toolName = isMulti ? `update_${safeName}_${safeId}` : `update_${safeName}`;
      const partialSchema = partialSchemaCache.get(def.id) ?? def.stateSchema;
      tools[toolName] = {
        type: "frontend",
        description: `Update the state of interactable component "${name}"${isMulti ? ` (id: ${def.id})` : ""}. Only include the fields you want to change; omitted fields keep their current values. ${def.description}`,
        parameters: partialSchema,
        streamCall: async (reader) => {
          try {
            for await (const partialArgs of reader.args.streamValues()) setDefState(def.id, (prev) => shallowMerge(prev, partialArgs));
          } catch {
          }
        },
        execute: async (partialState) => {
          setDefState(def.id, (prev) => shallowMerge(prev, partialState));
          return { success: true };
        }
      };
    }
  }
  return {
    system: systemParts.join("\n"),
    tools
  };
}

// node_modules/@assistant-ui/core/dist/react/client/Interactables.js
var PERSISTENCE_DEBOUNCE_MS = 500;
var Interactables = resource(() => {
  const [state, setState] = tapState(() => ({
    definitions: {},
    persistence: {}
  }));
  const clientRef = tapAssistantClientRef();
  const stateRef = tapRef(state);
  tapEffect(() => {
    stateRef.current = state;
  }, [state]);
  const subscribersRef = tapRef(/* @__PURE__ */ new Set());
  const partialSchemaCacheRef = tapRef(/* @__PURE__ */ new Map());
  const detachedStateRef = tapRef(/* @__PURE__ */ new Map());
  const adapterRef = tapRef(void 0);
  const debounceTimerRef = tapRef(void 0);
  const syncSeqRef = tapRef(0);
  const hasPendingLocalChangeRef = tapRef(false);
  const flushResolversRef = tapRef([]);
  const dirtyIdsRef = tapRef(/* @__PURE__ */ new Set());
  const runPersistence = tapCallback(async () => {
    const adapter = adapterRef.current;
    if (!adapter) {
      for (const resolve of flushResolversRef.current) resolve();
      flushResolversRef.current = [];
      return;
    }
    const seq = ++syncSeqRef.current;
    const dirtyIds = new Set(dirtyIdsRef.current);
    dirtyIdsRef.current.clear();
    hasPendingLocalChangeRef.current = true;
    const exported = stateRef.current.definitions;
    const payload = {};
    for (const [id, def] of Object.entries(exported)) payload[id] = {
      name: def.name,
      state: def.state
    };
    setState((prev) => ({
      ...prev,
      persistence: {
        ...prev.persistence,
        ...Object.fromEntries([...dirtyIds].map((id) => [id, {
          isPending: true,
          error: void 0
        }]))
      }
    }));
    try {
      await adapter.save(payload);
      if (syncSeqRef.current === seq) {
        hasPendingLocalChangeRef.current = false;
        setState((prev) => {
          const persistence = { ...prev.persistence };
          for (const id of dirtyIds) delete persistence[id];
          return {
            ...prev,
            persistence
          };
        });
      }
    } catch (e) {
      if (syncSeqRef.current === seq) {
        hasPendingLocalChangeRef.current = false;
        setState((prev) => ({
          ...prev,
          persistence: {
            ...prev.persistence,
            ...Object.fromEntries([...dirtyIds].map((id) => [id, {
              isPending: false,
              error: e
            }]))
          }
        }));
      }
    } finally {
      if (dirtyIdsRef.current.size > 0 && adapterRef.current) runPersistence();
      else {
        for (const resolve of flushResolversRef.current) resolve();
        flushResolversRef.current = [];
      }
    }
  }, []);
  const schedulePersistence = tapCallback((id) => {
    if (!adapterRef.current) return;
    dirtyIdsRef.current.add(id);
    if (debounceTimerRef.current !== void 0) clearTimeout(debounceTimerRef.current);
    debounceTimerRef.current = setTimeout(() => {
      debounceTimerRef.current = void 0;
      if (!hasPendingLocalChangeRef.current) runPersistence();
      else debounceTimerRef.current = setTimeout(() => {
        debounceTimerRef.current = void 0;
        runPersistence();
      }, PERSISTENCE_DEBOUNCE_MS);
    }, PERSISTENCE_DEBOUNCE_MS);
  }, [runPersistence]);
  const exportState = tapCallback(() => {
    const result = {};
    for (const [id, def] of Object.entries(stateRef.current.definitions)) result[id] = {
      name: def.name,
      state: def.state
    };
    return result;
  }, []);
  const importState = tapCallback((saved) => {
    for (const [id, entry] of Object.entries(saved)) detachedStateRef.current.set(id, entry.state);
    setState((prev) => {
      let changed = false;
      const definitions = { ...prev.definitions };
      for (const [id, entry] of Object.entries(saved)) if (definitions[id]) {
        definitions[id] = {
          ...definitions[id],
          state: entry.state
        };
        changed = true;
      }
      return changed ? {
        ...prev,
        definitions
      } : prev;
    });
  }, []);
  const setPersistenceAdapter = tapCallback((adapter) => {
    adapterRef.current = adapter;
  }, []);
  const flush = tapCallback(async () => {
    if (debounceTimerRef.current !== void 0) {
      clearTimeout(debounceTimerRef.current);
      debounceTimerRef.current = void 0;
    }
    if (!adapterRef.current) return;
    if (!hasPendingLocalChangeRef.current && dirtyIdsRef.current.size === 0) return;
    const p = new Promise((resolve) => {
      flushResolversRef.current.push(resolve);
    });
    if (!hasPendingLocalChangeRef.current) runPersistence();
    return p;
  }, [runPersistence]);
  const flushIfPending = tapCallback(() => {
    if (adapterRef.current && debounceTimerRef.current !== void 0) {
      clearTimeout(debounceTimerRef.current);
      debounceTimerRef.current = void 0;
      runPersistence();
    }
  }, [runPersistence]);
  const setDefState = tapCallback((id, updater) => {
    setState((prev) => {
      const existing = prev.definitions[id];
      if (!existing) return prev;
      return {
        ...prev,
        definitions: {
          ...prev.definitions,
          [id]: {
            ...existing,
            state: updater(existing.state)
          }
        }
      };
    });
    if (stateRef.current.definitions[id]) schedulePersistence(id);
  }, [schedulePersistence]);
  const setDefSelected = tapCallback((id, selected) => {
    setState((prev) => {
      const existing = prev.definitions[id];
      if (!existing) return prev;
      return {
        ...prev,
        definitions: {
          ...prev.definitions,
          [id]: {
            ...existing,
            selected
          }
        }
      };
    });
  }, []);
  const provider = tapMemo(() => ({
    getModelContext: () => {
      const defs = stateRef.current.definitions;
      return buildInteractableModelContext(defs, partialSchemaCacheRef.current, setDefState) ?? {};
    },
    subscribe: (callback) => {
      subscribersRef.current.add(callback);
      return () => {
        subscribersRef.current.delete(callback);
      };
    }
  }), [setDefState]);
  tapEffect(() => {
    for (const cb of subscribersRef.current) cb();
  }, [state]);
  tapEffect(() => {
    return clientRef.current.modelContext().register(provider);
  }, [clientRef, provider]);
  return {
    getState: () => state,
    register: tapCallback((def) => {
      try {
        const jsonSchema = toJSONSchema(def.stateSchema);
        partialSchemaCacheRef.current.set(def.id, toPartialJSONSchema(jsonSchema));
      } catch (e) {
        console.warn(`[Interactables] Failed to create partial schema for "${def.name}". The update tool will require all fields.`, e);
      }
      const detached = detachedStateRef.current.get(def.id);
      detachedStateRef.current.delete(def.id);
      setState((prev) => ({
        ...prev,
        definitions: {
          ...prev.definitions,
          [def.id]: {
            id: def.id,
            name: def.name,
            description: def.description,
            stateSchema: def.stateSchema,
            state: prev.definitions[def.id]?.state ?? detached ?? def.initialState,
            selected: def.selected
          }
        }
      }));
      return () => {
        flushIfPending();
        setState((prev) => {
          const existing = prev.definitions[def.id];
          if (existing) detachedStateRef.current.set(def.id, existing.state);
          partialSchemaCacheRef.current.delete(def.id);
          const { [def.id]: _, ...rest } = prev.definitions;
          const { [def.id]: __, ...restPersistence } = prev.persistence;
          return {
            ...prev,
            definitions: rest,
            persistence: restPersistence
          };
        });
      };
    }, [flushIfPending]),
    setState: setDefState,
    setSelected: setDefSelected,
    exportState,
    importState,
    setPersistenceAdapter,
    flush
  };
});
attachTransformScopes(Interactables, (scopes, parent) => {
  if (!scopes.modelContext && parent.modelContext.source === null) scopes.modelContext = ModelContext();
});

// node_modules/@assistant-ui/core/dist/react/providers/AttachmentByIndexProvider.js
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var MessageAttachmentByIndexProvider = ({ index, children }) => {
  return (0, import_jsx_runtime2.jsx)(AuiProvider, {
    value: useAui({ attachment: Derived({
      source: "message",
      query: {
        type: "index",
        index
      },
      get: (aui) => aui.message().attachment({ index })
    }) }),
    children
  });
};
var ComposerAttachmentByIndexProvider = ({ index, children }) => {
  return (0, import_jsx_runtime2.jsx)(AuiProvider, {
    value: useAui({ attachment: Derived({
      source: "composer",
      query: {
        type: "index",
        index
      },
      get: (aui) => aui.composer().attachment({ index })
    }) }),
    children
  });
};

// node_modules/@assistant-ui/core/dist/store/runtime-clients/tap-subscribable.js
var tapSubscribable = (subscribable) => {
  const [, setState] = tapState(subscribable.getState);
  tapEffect(() => {
    setState(subscribable.getState());
    return subscribable.subscribe(() => {
      setState(subscribable.getState());
    });
  }, [subscribable]);
  return subscribable.getState();
};

// node_modules/@assistant-ui/core/dist/store/runtime-clients/thread-list-item-runtime-client.js
var ThreadListItemClient = resource(({ runtime }) => {
  const state = tapSubscribable(runtime);
  const emit = tapAssistantEmit();
  tapEffect(() => {
    const unsubscribers = [];
    for (const event of ["switchedTo", "switchedAway"]) {
      const unsubscribe = runtime.unstable_on(event, () => {
        emit(`threadListItem.${event}`, { threadId: runtime.getState().id });
      });
      unsubscribers.push(unsubscribe);
    }
    return () => {
      for (const unsub of unsubscribers) unsub();
    };
  }, [runtime, emit]);
  return {
    getState: () => state,
    switchTo: runtime.switchTo,
    rename: runtime.rename,
    updateCustom: runtime.updateCustom,
    archive: runtime.archive,
    unarchive: runtime.unarchive,
    delete: runtime.delete,
    generateTitle: runtime.generateTitle,
    initialize: runtime.initialize,
    detach: runtime.detach,
    __internal_getRuntime: () => runtime
  };
});

// node_modules/@assistant-ui/core/dist/react/providers/ThreadListItemRuntimeProvider.js
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var ThreadListItemRuntimeProvider = ({ runtime, children }) => {
  return (0, import_jsx_runtime3.jsx)(AuiProvider, {
    value: useAui({ threadListItem: ThreadListItemClient({ runtime }) }),
    children
  });
};

// node_modules/@assistant-ui/core/dist/react/providers/MessageByIndexProvider.js
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var MessageByIndexProvider = ({ index, children }) => {
  return (0, import_jsx_runtime4.jsx)(AuiProvider, {
    value: useAui({
      message: Derived({
        source: "thread",
        query: {
          type: "index",
          index
        },
        get: (aui) => aui.thread().message({ index })
      }),
      composer: Derived({
        source: "message",
        query: {},
        get: (aui) => aui.thread().message({ index }).composer()
      })
    }),
    children
  });
};

// node_modules/@assistant-ui/core/dist/react/providers/PartByIndexProvider.js
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
var PartByIndexProvider = ({ index, children }) => {
  return (0, import_jsx_runtime5.jsx)(AuiProvider, {
    value: useAui({ part: Derived({
      source: "message",
      query: {
        type: "index",
        index
      },
      get: (aui) => aui.message().part({ index })
    }) }),
    children
  });
};

// node_modules/@assistant-ui/core/dist/react/providers/TextMessagePartProvider.js
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
var TextMessagePartClient = resource(({ text, isRunning }) => {
  const state = tapMemo(() => ({
    type: "text",
    text,
    status: isRunning ? { type: "running" } : { type: "complete" }
  }), [text, isRunning]);
  return {
    getState: () => state,
    addToolResult: () => {
      throw new Error("Not supported");
    },
    resumeToolCall: () => {
      throw new Error("Not supported");
    },
    respondToToolApproval: () => {
      throw new Error("Not supported");
    }
  };
});
var TextMessagePartProvider = ({ text, isRunning = false, children }) => {
  return (0, import_jsx_runtime6.jsx)(AuiProvider, {
    value: useAui({ part: TextMessagePartClient({
      text,
      isRunning
    }) }),
    children
  });
};

// node_modules/@assistant-ui/core/dist/store/clients/chain-of-thought-client.js
var COMPLETE_STATUS = Object.freeze({ type: "complete" });
var ChainOfThoughtClient = resource(({ parts, getMessagePart }) => {
  const [collapsed, setCollapsed] = tapState(true);
  const status = tapMemo(() => {
    return parts[parts.length - 1]?.status ?? COMPLETE_STATUS;
  }, [parts]);
  const state = tapMemo(() => ({
    parts,
    collapsed,
    status
  }), [
    parts,
    collapsed,
    status
  ]);
  return {
    getState: () => state,
    setCollapsed,
    part: getMessagePart
  };
});

// node_modules/@assistant-ui/core/dist/react/providers/ChainOfThoughtByIndicesProvider.js
var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
var ChainOfThoughtByIndicesProvider = ({ startIndex, endIndex, children }) => {
  const parts = useAuiState((s) => s.message.parts).slice(startIndex, endIndex + 1);
  const parentAui = useAui();
  return (0, import_jsx_runtime7.jsx)(AuiProvider, {
    value: useAui({ chainOfThought: ChainOfThoughtClient({
      parts,
      getMessagePart: ({ index }) => {
        if (index < 0 || index >= parts.length) throw new Error(`ChainOfThought part index ${index} is out of bounds (0..${parts.length - 1})`);
        return parentAui.message().part({ index: startIndex + index });
      }
    }) }),
    children
  });
};

// node_modules/@assistant-ui/core/dist/react/providers/ThreadListItemByIndexProvider.js
var import_jsx_runtime8 = __toESM(require_jsx_runtime(), 1);
var ThreadListItemByIndexProvider = ({ index, archived, children }) => {
  return (0, import_jsx_runtime8.jsx)(AuiProvider, {
    value: useAui({ threadListItem: Derived({
      source: "threads",
      query: {
        type: "index",
        index,
        archived
      },
      get: (aui) => aui.threads().item({
        index,
        archived
      })
    }) }),
    children
  });
};

// node_modules/@assistant-ui/core/dist/react/providers/SuggestionByIndexProvider.js
var import_jsx_runtime9 = __toESM(require_jsx_runtime(), 1);
var SuggestionByIndexProvider = ({ index, children }) => {
  return (0, import_jsx_runtime9.jsx)(AuiProvider, {
    value: useAui({ suggestion: Derived({
      source: "suggestions",
      query: { index },
      get: (aui) => aui.suggestions().suggestion({ index })
    }) }),
    children
  });
};

// node_modules/@assistant-ui/core/dist/subscribable/subscribable.js
var SKIP_UPDATE = /* @__PURE__ */ Symbol("skip-update");
function shallowEqual(objA, objB) {
  if (objA === void 0 && objB === void 0) return true;
  if (objA === void 0) return false;
  if (objB === void 0) return false;
  for (const key of Object.keys(objA)) {
    const valueA = objA[key];
    const valueB = objB[key];
    if (!Object.is(valueA, valueB)) return false;
  }
  return true;
}
var BaseSubscribable = class {
  _subscribers = /* @__PURE__ */ new Set();
  subscribe(callback) {
    this._subscribers.add(callback);
    return () => this._subscribers.delete(callback);
  }
  waitForUpdate() {
    return new Promise((resolve) => {
      const unsubscribe = this.subscribe(() => {
        unsubscribe();
        resolve();
      });
    });
  }
  _notifySubscribers() {
    const errors = [];
    for (const callback of this._subscribers) try {
      callback();
    } catch (error) {
      errors.push(error);
    }
    if (errors.length > 0) if (errors.length === 1) throw errors[0];
    else {
      for (const error of errors) console.error(error);
      throw new AggregateError(errors);
    }
  }
};
var BaseSubject = class {
  _subscriptions = /* @__PURE__ */ new Set();
  _connection;
  get isConnected() {
    return !!this._connection;
  }
  notifySubscribers(payload) {
    for (const callback of this._subscriptions) callback(payload);
  }
  _updateConnection() {
    if (this._subscriptions.size > 0) {
      if (this._connection) return;
      this._connection = this._connect();
    } else {
      this._connection?.();
      this._connection = void 0;
    }
  }
  subscribe(callback) {
    this._subscriptions.add(callback);
    this._updateConnection();
    return () => {
      this._subscriptions.delete(callback);
      this._updateConnection();
    };
  }
};
var ShallowMemoizeSubject = class extends BaseSubject {
  binding;
  get path() {
    return this.binding.path;
  }
  constructor(binding) {
    super();
    this.binding = binding;
    const state = binding.getState();
    if (state === SKIP_UPDATE) throw new Error("Entry not available in the store");
    this._previousState = state;
  }
  _previousState;
  getState = () => {
    if (!this.isConnected) this._syncState();
    return this._previousState;
  };
  _syncState() {
    const state = this.binding.getState();
    if (state === SKIP_UPDATE) return false;
    if (shallowEqual(state, this._previousState)) return false;
    this._previousState = state;
    return true;
  }
  _connect() {
    const callback = () => {
      if (this._syncState()) this.notifySubscribers();
    };
    return this.binding.subscribe(callback);
  }
};
var LazyMemoizeSubject = class extends BaseSubject {
  binding;
  get path() {
    return this.binding.path;
  }
  constructor(binding) {
    super();
    this.binding = binding;
  }
  _previousStateDirty = true;
  _previousState;
  getState = () => {
    if (!this.isConnected || this._previousStateDirty) {
      const newState = this.binding.getState();
      if (newState !== SKIP_UPDATE) this._previousState = newState;
      this._previousStateDirty = false;
    }
    if (this._previousState === void 0) throw new Error("Entry not available in the store");
    return this._previousState;
  };
  _connect() {
    const callback = () => {
      this._previousStateDirty = true;
      this.notifySubscribers();
    };
    return this.binding.subscribe(callback);
  }
};
var NestedSubscriptionSubject = class extends BaseSubject {
  binding;
  get path() {
    return this.binding.path;
  }
  constructor(binding) {
    super();
    this.binding = binding;
  }
  getState() {
    return this.binding.getState();
  }
  outerSubscribe(callback) {
    return this.binding.subscribe(callback);
  }
  _connect() {
    const callback = () => {
      this.notifySubscribers();
    };
    let lastState = this.binding.getState();
    let innerUnsubscribe = lastState?.subscribe(callback);
    const onRuntimeUpdate = () => {
      const newState = this.binding.getState();
      if (newState === lastState) return;
      lastState = newState;
      innerUnsubscribe?.();
      innerUnsubscribe = newState?.subscribe(callback);
      callback();
    };
    const outerUnsubscribe = this.outerSubscribe(onRuntimeUpdate);
    return () => {
      outerUnsubscribe?.();
      innerUnsubscribe?.();
    };
  }
};
var EventSubscriptionSubject = class extends BaseSubject {
  config;
  constructor(config) {
    super();
    this.config = config;
  }
  getState() {
    return this.config.binding.getState();
  }
  outerSubscribe(callback) {
    return this.config.binding.subscribe(callback);
  }
  _connect() {
    const callback = (payload) => {
      this.notifySubscribers(payload);
    };
    let lastState = this.config.binding.getState();
    let innerUnsubscribe = lastState?.unstable_on(this.config.event, callback);
    const onRuntimeUpdate = () => {
      const newState = this.config.binding.getState();
      if (newState === lastState) return;
      lastState = newState;
      innerUnsubscribe?.();
      innerUnsubscribe = newState?.unstable_on(this.config.event, callback);
    };
    const outerUnsubscribe = this.outerSubscribe(onRuntimeUpdate);
    return () => {
      outerUnsubscribe?.();
      innerUnsubscribe?.();
    };
  }
};

// node_modules/@assistant-ui/core/dist/runtime/api/attachment-runtime.js
var AttachmentRuntimeImpl = class {
  _core;
  get path() {
    return this._core.path;
  }
  constructor(_core) {
    this._core = _core;
    this.__internal_bindMethods();
  }
  __internal_bindMethods() {
    this.getState = this.getState.bind(this);
    this.remove = this.remove.bind(this);
    this.subscribe = this.subscribe.bind(this);
  }
  getState() {
    return this._core.getState();
  }
  subscribe(callback) {
    return this._core.subscribe(callback);
  }
};
var ComposerAttachmentRuntime = class extends AttachmentRuntimeImpl {
  _composerApi;
  constructor(core, _composerApi) {
    super(core);
    this._composerApi = _composerApi;
  }
  remove() {
    const core = this._composerApi.getState();
    if (!core) throw new Error("Composer is not available");
    return core.removeAttachment(this.getState().id);
  }
};
var ThreadComposerAttachmentRuntimeImpl = class extends ComposerAttachmentRuntime {
  get source() {
    return "thread-composer";
  }
};
var EditComposerAttachmentRuntimeImpl = class extends ComposerAttachmentRuntime {
  get source() {
    return "edit-composer";
  }
};
var MessageAttachmentRuntimeImpl = class extends AttachmentRuntimeImpl {
  get source() {
    return "message";
  }
  remove() {
    throw new Error("Message attachments cannot be removed");
  }
};

// node_modules/@assistant-ui/core/dist/runtime/api/composer-runtime.js
var EMPTY_ARRAY = Object.freeze([]);
var EMPTY_OBJECT2 = Object.freeze({});
var getThreadComposerState = (runtime) => {
  return Object.freeze({
    type: "thread",
    isEditing: runtime?.isEditing ?? false,
    canCancel: runtime?.canCancel ?? false,
    canSend: runtime?.canSend ?? false,
    isEmpty: runtime?.isEmpty ?? true,
    attachments: runtime?.attachments ?? EMPTY_ARRAY,
    text: runtime?.text ?? "",
    role: runtime?.role ?? "user",
    runConfig: runtime?.runConfig ?? EMPTY_OBJECT2,
    attachmentAccept: runtime?.attachmentAccept ?? "",
    dictation: runtime?.dictation,
    quote: runtime?.quote,
    value: runtime?.text ?? ""
  });
};
var getEditComposerState = (runtime) => {
  return Object.freeze({
    type: "edit",
    isEditing: runtime?.isEditing ?? false,
    canCancel: runtime?.canCancel ?? false,
    canSend: runtime?.canSend ?? false,
    isEmpty: runtime?.isEmpty ?? true,
    text: runtime?.text ?? "",
    role: runtime?.role ?? "user",
    attachments: runtime?.attachments ?? EMPTY_ARRAY,
    runConfig: runtime?.runConfig ?? EMPTY_OBJECT2,
    attachmentAccept: runtime?.attachmentAccept ?? "",
    dictation: runtime?.dictation,
    quote: runtime?.quote,
    parentId: runtime?.parentId ?? null,
    sourceId: runtime?.sourceId ?? null,
    value: runtime?.text ?? ""
  });
};
var ComposerRuntimeImpl = class {
  _core;
  get path() {
    return this._core.path;
  }
  constructor(_core) {
    this._core = _core;
  }
  __internal_bindMethods() {
    this.setText = this.setText.bind(this);
    this.setRunConfig = this.setRunConfig.bind(this);
    this.getState = this.getState.bind(this);
    this.subscribe = this.subscribe.bind(this);
    this.addAttachment = this.addAttachment.bind(this);
    this.reset = this.reset.bind(this);
    this.clearAttachments = this.clearAttachments.bind(this);
    this.send = this.send.bind(this);
    this.cancel = this.cancel.bind(this);
    this.setRole = this.setRole.bind(this);
    this.getAttachmentByIndex = this.getAttachmentByIndex.bind(this);
    this.startDictation = this.startDictation.bind(this);
    this.stopDictation = this.stopDictation.bind(this);
    this.setQuote = this.setQuote.bind(this);
    this.unstable_on = this.unstable_on.bind(this);
  }
  setText(text) {
    const core = this._core.getState();
    if (!core) throw new Error("Composer is not available");
    core.setText(text);
  }
  setRunConfig(runConfig) {
    const core = this._core.getState();
    if (!core) throw new Error("Composer is not available");
    core.setRunConfig(runConfig);
  }
  addAttachment(fileOrAttachment) {
    const core = this._core.getState();
    if (!core) throw new Error("Composer is not available");
    return core.addAttachment(fileOrAttachment);
  }
  reset() {
    const core = this._core.getState();
    if (!core) throw new Error("Composer is not available");
    return core.reset();
  }
  clearAttachments() {
    const core = this._core.getState();
    if (!core) throw new Error("Composer is not available");
    return core.clearAttachments();
  }
  send(options) {
    const core = this._core.getState();
    if (!core) throw new Error("Composer is not available");
    core.send(options);
  }
  cancel() {
    const core = this._core.getState();
    if (!core) throw new Error("Composer is not available");
    core.cancel();
  }
  setRole(role) {
    const core = this._core.getState();
    if (!core) throw new Error("Composer is not available");
    core.setRole(role);
  }
  startDictation() {
    const core = this._core.getState();
    if (!core) throw new Error("Composer is not available");
    core.startDictation();
  }
  stopDictation() {
    const core = this._core.getState();
    if (!core) throw new Error("Composer is not available");
    core.stopDictation();
  }
  setQuote(quote) {
    const core = this._core.getState();
    if (!core) throw new Error("Composer is not available");
    core.setQuote(quote);
  }
  subscribe(callback) {
    return this._core.subscribe(callback);
  }
  _eventSubscriptionSubjects = /* @__PURE__ */ new Map();
  unstable_on(event, callback) {
    let subject = this._eventSubscriptionSubjects.get(event);
    if (!subject) {
      subject = new EventSubscriptionSubject({
        event,
        binding: this._core
      });
      this._eventSubscriptionSubjects.set(event, subject);
    }
    return subject.subscribe(callback);
  }
};
var ThreadComposerRuntimeImpl = class extends ComposerRuntimeImpl {
  get path() {
    return this._core.path;
  }
  get type() {
    return "thread";
  }
  _getState;
  constructor(core) {
    const stateBinding = new LazyMemoizeSubject({
      path: core.path,
      getState: () => getThreadComposerState(core.getState()),
      subscribe: (callback) => core.subscribe(callback)
    });
    super({
      path: core.path,
      getState: () => core.getState(),
      subscribe: (callback) => stateBinding.subscribe(callback)
    });
    this._getState = stateBinding.getState.bind(stateBinding);
    this.__internal_bindMethods();
  }
  getState() {
    return this._getState();
  }
  getAttachmentByIndex(idx) {
    return new ThreadComposerAttachmentRuntimeImpl(new ShallowMemoizeSubject({
      path: {
        ...this.path,
        attachmentSource: "thread-composer",
        attachmentSelector: {
          type: "index",
          index: idx
        },
        ref: `${this.path.ref}.attachments[${idx}]`
      },
      getState: () => {
        const attachment = this.getState().attachments[idx];
        if (!attachment) return SKIP_UPDATE;
        return {
          ...attachment,
          source: "thread-composer"
        };
      },
      subscribe: (callback) => this._core.subscribe(callback)
    }), this._core);
  }
};
var EditComposerRuntimeImpl = class extends ComposerRuntimeImpl {
  _beginEdit;
  get path() {
    return this._core.path;
  }
  get type() {
    return "edit";
  }
  _getState;
  constructor(core, _beginEdit) {
    const stateBinding = new LazyMemoizeSubject({
      path: core.path,
      getState: () => getEditComposerState(core.getState()),
      subscribe: (callback) => core.subscribe(callback)
    });
    super({
      path: core.path,
      getState: () => core.getState(),
      subscribe: (callback) => stateBinding.subscribe(callback)
    });
    this._beginEdit = _beginEdit;
    this._getState = stateBinding.getState.bind(stateBinding);
    this.__internal_bindMethods();
  }
  __internal_bindMethods() {
    super.__internal_bindMethods();
    this.beginEdit = this.beginEdit.bind(this);
  }
  getState() {
    return this._getState();
  }
  beginEdit() {
    this._beginEdit();
  }
  getAttachmentByIndex(idx) {
    return new EditComposerAttachmentRuntimeImpl(new ShallowMemoizeSubject({
      path: {
        ...this.path,
        attachmentSource: "edit-composer",
        attachmentSelector: {
          type: "index",
          index: idx
        },
        ref: `${this.path.ref}.attachments[${idx}]`
      },
      getState: () => {
        const attachment = this.getState().attachments[idx];
        if (!attachment) return SKIP_UPDATE;
        return {
          ...attachment,
          source: "edit-composer"
        };
      },
      subscribe: (callback) => this._core.subscribe(callback)
    }), this._core);
  }
};

// node_modules/@assistant-ui/core/dist/utils/text.js
var getThreadMessageText = (message) => {
  return message.content.filter((part) => part.type === "text").map((part) => part.text).join("\n\n");
};

// node_modules/@assistant-ui/core/dist/runtime/api/message-part-runtime.js
var MessagePartRuntimeImpl = class {
  contentBinding;
  messageApi;
  threadApi;
  get path() {
    return this.contentBinding.path;
  }
  constructor(contentBinding, messageApi, threadApi) {
    this.contentBinding = contentBinding;
    this.messageApi = messageApi;
    this.threadApi = threadApi;
    this.__internal_bindMethods();
  }
  __internal_bindMethods() {
    this.addToolResult = this.addToolResult.bind(this);
    this.resumeToolCall = this.resumeToolCall.bind(this);
    this.respondToToolApproval = this.respondToToolApproval.bind(this);
    this.getState = this.getState.bind(this);
    this.subscribe = this.subscribe.bind(this);
  }
  getState() {
    return this.contentBinding.getState();
  }
  addToolResult(result) {
    const state = this.contentBinding.getState();
    if (!state) throw new Error("Message part is not available");
    if (state.type !== "tool-call") throw new Error("Tried to add tool result to non-tool message part");
    if (!this.messageApi) throw new Error("Message API is not available. This is likely a bug in assistant-ui.");
    if (!this.threadApi) throw new Error("Thread API is not available");
    const message = this.messageApi.getState();
    if (!message) throw new Error("Message is not available");
    const toolName = state.toolName;
    const toolCallId = state.toolCallId;
    const response = ToolResponse.toResponse(result);
    this.threadApi.getState().addToolResult({
      messageId: message.id,
      toolName,
      toolCallId,
      result: response.result,
      artifact: response.artifact,
      isError: response.isError
    });
  }
  resumeToolCall(payload) {
    const state = this.contentBinding.getState();
    if (!state) throw new Error("Message part is not available");
    if (state.type !== "tool-call") throw new Error("Tried to resume tool call on non-tool message part");
    if (!this.threadApi) throw new Error("Thread API is not available");
    const toolCallId = state.toolCallId;
    this.threadApi.getState().resumeToolCall({
      toolCallId,
      payload
    });
  }
  respondToToolApproval(response) {
    const state = this.contentBinding.getState();
    if (!state) throw new Error("Message part is not available");
    if (state.type !== "tool-call") throw new Error("Tried to respond to tool approval on non-tool message part");
    if (!state.approval || state.approval.approved !== void 0) throw new Error("Tool call has no pending approval");
    if (!this.threadApi) throw new Error("Thread API is not available");
    this.threadApi.getState().respondToToolApproval({
      approvalId: state.approval.id,
      approved: response.approved,
      ...response.reason != null && { reason: response.reason }
    });
  }
  subscribe(callback) {
    return this.contentBinding.subscribe(callback);
  }
};

// node_modules/@assistant-ui/core/dist/runtime/api/message-runtime.js
var COMPLETE_STATUS2 = Object.freeze({ type: "complete" });
var toMessagePartStatus = (message, partIndex, part) => {
  if (message.role !== "assistant") return COMPLETE_STATUS2;
  if (part.type === "tool-call") if (!part.result) return message.status;
  else return COMPLETE_STATUS2;
  const isLastPart = partIndex === Math.max(0, message.content.length - 1);
  if (message.status.type === "requires-action") return COMPLETE_STATUS2;
  return isLastPart ? message.status : COMPLETE_STATUS2;
};
var getMessagePartState = (message, partIndex) => {
  const part = message.content[partIndex];
  if (!part) return SKIP_UPDATE;
  const status = toMessagePartStatus(message, partIndex, part);
  return Object.freeze({
    ...part,
    [symbolInnerMessage]: part[symbolInnerMessage],
    status
  });
};
var MessageRuntimeImpl = class {
  _core;
  _threadBinding;
  get path() {
    return this._core.path;
  }
  constructor(_core, _threadBinding) {
    this._core = _core;
    this._threadBinding = _threadBinding;
    this.composer = new EditComposerRuntimeImpl(new NestedSubscriptionSubject({
      path: {
        ...this.path,
        ref: `${this.path.ref}${this.path.ref}.composer`,
        composerSource: "edit"
      },
      getState: this._getEditComposerRuntimeCore,
      subscribe: (callback) => this._threadBinding.subscribe(callback)
    }), () => this._threadBinding.getState().beginEdit(this._core.getState().id));
    this.__internal_bindMethods();
  }
  __internal_bindMethods() {
    this.reload = this.reload.bind(this);
    this.getState = this.getState.bind(this);
    this.subscribe = this.subscribe.bind(this);
    this.getMessagePartByIndex = this.getMessagePartByIndex.bind(this);
    this.getMessagePartByToolCallId = this.getMessagePartByToolCallId.bind(this);
    this.getAttachmentByIndex = this.getAttachmentByIndex.bind(this);
    this.unstable_getCopyText = this.unstable_getCopyText.bind(this);
    this.speak = this.speak.bind(this);
    this.stopSpeaking = this.stopSpeaking.bind(this);
    this.submitFeedback = this.submitFeedback.bind(this);
    this.switchToBranch = this.switchToBranch.bind(this);
  }
  composer;
  _getEditComposerRuntimeCore = () => {
    return this._threadBinding.getState().getEditComposer(this._core.getState().id);
  };
  getState() {
    return this._core.getState();
  }
  reload(reloadConfig = {}) {
    const editComposerRuntimeCore = this._getEditComposerRuntimeCore();
    const composerRuntimeCore = editComposerRuntimeCore ?? this._threadBinding.getState().composer;
    const composer = editComposerRuntimeCore ?? composerRuntimeCore;
    const { runConfig = composer.runConfig } = reloadConfig;
    const state = this._core.getState();
    if (state.role !== "assistant") throw new Error("Can only reload assistant messages");
    this._threadBinding.getState().startRun({
      parentId: state.parentId,
      sourceId: state.id,
      runConfig
    });
  }
  speak() {
    const state = this._core.getState();
    return this._threadBinding.getState().speak(state.id);
  }
  stopSpeaking() {
    const state = this._core.getState();
    if (this._threadBinding.getState().speech?.messageId === state.id) this._threadBinding.getState().stopSpeaking();
    else throw new Error("Message is not being spoken");
  }
  submitFeedback({ type }) {
    const state = this._core.getState();
    this._threadBinding.getState().submitFeedback({
      messageId: state.id,
      type
    });
  }
  switchToBranch({ position, branchId }) {
    const state = this._core.getState();
    if (branchId && position) throw new Error("May not specify both branchId and position");
    else if (!branchId && !position) throw new Error("Must specify either branchId or position");
    const branches = this._threadBinding.getState().getBranches(state.id);
    let targetBranch = branchId;
    if (position === "previous") targetBranch = branches[state.branchNumber - 2];
    else if (position === "next") targetBranch = branches[state.branchNumber];
    if (!targetBranch) throw new Error("Branch not found");
    this._threadBinding.getState().switchToBranch(targetBranch);
  }
  unstable_getCopyText() {
    return getThreadMessageText(this.getState());
  }
  subscribe(callback) {
    return this._core.subscribe(callback);
  }
  getMessagePartByIndex(idx) {
    if (idx < 0) throw new Error("Message part index must be >= 0");
    return new MessagePartRuntimeImpl(new ShallowMemoizeSubject({
      path: {
        ...this.path,
        ref: `${this.path.ref}${this.path.ref}.content[${idx}]`,
        messagePartSelector: {
          type: "index",
          index: idx
        }
      },
      getState: () => {
        return getMessagePartState(this.getState(), idx);
      },
      subscribe: (callback) => this._core.subscribe(callback)
    }), this._core, this._threadBinding);
  }
  getMessagePartByToolCallId(toolCallId) {
    return new MessagePartRuntimeImpl(new ShallowMemoizeSubject({
      path: {
        ...this.path,
        ref: this.path.ref + `${this.path.ref}.content[toolCallId=${JSON.stringify(toolCallId)}]`,
        messagePartSelector: {
          type: "toolCallId",
          toolCallId
        }
      },
      getState: () => {
        const state = this._core.getState();
        const idx = state.content.findIndex((part) => part.type === "tool-call" && part.toolCallId === toolCallId);
        if (idx === -1) return SKIP_UPDATE;
        return getMessagePartState(state, idx);
      },
      subscribe: (callback) => this._core.subscribe(callback)
    }), this._core, this._threadBinding);
  }
  getAttachmentByIndex(idx) {
    return new MessageAttachmentRuntimeImpl(new ShallowMemoizeSubject({
      path: {
        ...this.path,
        ref: `${this.path.ref}${this.path.ref}.attachments[${idx}]`,
        attachmentSource: "message",
        attachmentSelector: {
          type: "index",
          index: idx
        }
      },
      getState: () => {
        const attachment = this.getState().attachments?.[idx];
        if (!attachment) return SKIP_UPDATE;
        return {
          ...attachment,
          source: "message"
        };
      },
      subscribe: (callback) => this._core.subscribe(callback)
    }));
  }
};

// node_modules/@assistant-ui/core/dist/runtime/api/thread-runtime.js
var toResumeRunConfig = (message) => {
  return {
    parentId: message.parentId ?? null,
    sourceId: message.sourceId ?? null,
    runConfig: message.runConfig ?? {},
    ...message.stream ? { stream: message.stream } : {}
  };
};
var toStartRunConfig = (message) => {
  return {
    parentId: message.parentId ?? null,
    sourceId: message.sourceId ?? null,
    runConfig: message.runConfig ?? {}
  };
};
var toAppendMessage = (messages, message) => {
  if (typeof message === "string") return {
    createdAt: /* @__PURE__ */ new Date(),
    parentId: messages.at(-1)?.id ?? null,
    sourceId: null,
    runConfig: {},
    role: "user",
    content: [{
      type: "text",
      text: message
    }],
    attachments: [],
    metadata: { custom: {} }
  };
  return {
    createdAt: message.createdAt ?? /* @__PURE__ */ new Date(),
    parentId: message.parentId ?? messages.at(-1)?.id ?? null,
    sourceId: message.sourceId ?? null,
    role: message.role ?? "user",
    content: message.content,
    attachments: message.attachments ?? [],
    metadata: message.metadata ?? { custom: {} },
    runConfig: message.runConfig ?? {},
    startRun: message.startRun
  };
};
var getThreadState = (runtime, threadListItemState) => {
  const lastMessage = runtime.messages.at(-1);
  return Object.freeze({
    threadId: threadListItemState.id,
    metadata: threadListItemState,
    capabilities: runtime.capabilities,
    isDisabled: runtime.isDisabled,
    isLoading: runtime.isLoading,
    isRunning: runtime.isRunning ?? (lastMessage?.role !== "assistant" ? false : lastMessage.status.type === "running"),
    messages: runtime.messages,
    state: runtime.state,
    suggestions: runtime.suggestions,
    extras: runtime.extras,
    speech: runtime.speech,
    voice: runtime.voice
  });
};
var ThreadRuntimeImpl = class {
  get path() {
    return this._threadBinding.path;
  }
  get __internal_threadBinding() {
    return this._threadBinding;
  }
  _threadBinding;
  constructor(threadBinding, threadListItemBinding) {
    const stateBinding = new ShallowMemoizeSubject({
      path: threadBinding.path,
      getState: () => getThreadState(threadBinding.getState(), threadListItemBinding.getState()),
      subscribe: (callback) => {
        const sub1 = threadBinding.subscribe(callback);
        const sub2 = threadListItemBinding.subscribe(callback);
        return () => {
          sub1();
          sub2();
        };
      }
    });
    this._threadBinding = {
      path: threadBinding.path,
      getState: () => threadBinding.getState(),
      getStateState: () => stateBinding.getState(),
      outerSubscribe: (callback) => threadBinding.outerSubscribe(callback),
      subscribe: (callback) => threadBinding.subscribe(callback)
    };
    this.composer = new ThreadComposerRuntimeImpl(new NestedSubscriptionSubject({
      path: {
        ...this.path,
        ref: `${this.path.ref}.composer`,
        composerSource: "thread"
      },
      getState: () => this._threadBinding.getState().composer,
      subscribe: (callback) => this._threadBinding.subscribe(callback)
    }));
    this.__internal_bindMethods();
  }
  __internal_bindMethods() {
    this.append = this.append.bind(this);
    this.resumeRun = this.resumeRun.bind(this);
    this.importExternalState = this.importExternalState.bind(this);
    this.exportExternalState = this.exportExternalState.bind(this);
    this.startRun = this.startRun.bind(this);
    this.cancelRun = this.cancelRun.bind(this);
    this.stopSpeaking = this.stopSpeaking.bind(this);
    this.connectVoice = this.connectVoice.bind(this);
    this.disconnectVoice = this.disconnectVoice.bind(this);
    this.muteVoice = this.muteVoice.bind(this);
    this.unmuteVoice = this.unmuteVoice.bind(this);
    this.getVoiceVolume = this.getVoiceVolume.bind(this);
    this.subscribeVoiceVolume = this.subscribeVoiceVolume.bind(this);
    this.export = this.export.bind(this);
    this.import = this.import.bind(this);
    this.reset = this.reset.bind(this);
    this.getMessageByIndex = this.getMessageByIndex.bind(this);
    this.getMessageById = this.getMessageById.bind(this);
    this.subscribe = this.subscribe.bind(this);
    this.unstable_on = this.unstable_on.bind(this);
    this.getModelContext = this.getModelContext.bind(this);
    this.getState = this.getState.bind(this);
  }
  composer;
  getState() {
    return this._threadBinding.getStateState();
  }
  append(message) {
    this._threadBinding.getState().append(toAppendMessage(this._threadBinding.getState().messages, message));
  }
  subscribe(callback) {
    return this._threadBinding.subscribe(callback);
  }
  getModelContext() {
    return this._threadBinding.getState().getModelContext();
  }
  startRun(config) {
    return this._threadBinding.getState().startRun(toStartRunConfig(config));
  }
  resumeRun(config) {
    return this._threadBinding.getState().resumeRun(toResumeRunConfig(config));
  }
  exportExternalState() {
    return this._threadBinding.getState().exportExternalState();
  }
  importExternalState(state) {
    this._threadBinding.getState().importExternalState(state);
  }
  cancelRun() {
    this._threadBinding.getState().cancelRun();
  }
  stopSpeaking() {
    return this._threadBinding.getState().stopSpeaking();
  }
  connectVoice() {
    this._threadBinding.getState().connectVoice();
  }
  disconnectVoice() {
    this._threadBinding.getState().disconnectVoice();
  }
  getVoiceVolume() {
    return this._threadBinding.getState().getVoiceVolume();
  }
  subscribeVoiceVolume(callback) {
    return this._threadBinding.getState().subscribeVoiceVolume(callback);
  }
  muteVoice() {
    this._threadBinding.getState().muteVoice();
  }
  unmuteVoice() {
    this._threadBinding.getState().unmuteVoice();
  }
  export() {
    return this._threadBinding.getState().export();
  }
  import(data) {
    this._threadBinding.getState().import(data);
  }
  reset(initialMessages) {
    this._threadBinding.getState().reset(initialMessages);
  }
  getMessageByIndex(idx) {
    if (idx < 0) throw new Error("Message index must be >= 0");
    return this._getMessageRuntime({
      ...this.path,
      ref: `${this.path.ref}.messages[${idx}]`,
      messageSelector: {
        type: "index",
        index: idx
      }
    }, () => {
      const messages = this._threadBinding.getState().messages;
      const message = messages[idx];
      if (!message) return void 0;
      return {
        message,
        parentId: messages[idx - 1]?.id ?? null,
        index: idx
      };
    });
  }
  getMessageById(messageId) {
    return this._getMessageRuntime({
      ...this.path,
      ref: `${this.path.ref}.messages[messageId=${JSON.stringify(messageId)}]`,
      messageSelector: {
        type: "messageId",
        messageId
      }
    }, () => this._threadBinding.getState().getMessageById(messageId));
  }
  _getMessageRuntime(path, callback) {
    return new MessageRuntimeImpl(new ShallowMemoizeSubject({
      path,
      getState: () => {
        const { message, parentId, index } = callback() ?? {};
        const { messages, speech: speechState } = this._threadBinding.getState();
        if (!message || parentId === void 0 || index === void 0) return SKIP_UPDATE;
        const branches = this._threadBinding.getState().getBranches(message.id);
        return {
          ...message,
          [symbolInnerMessage]: message[symbolInnerMessage],
          index,
          isLast: messages.at(-1)?.id === message.id,
          parentId,
          branchNumber: branches.indexOf(message.id) + 1,
          branchCount: branches.length,
          speech: speechState?.messageId === message.id ? speechState : void 0
        };
      },
      subscribe: (callback2) => this._threadBinding.subscribe(callback2)
    }), this._threadBinding);
  }
  _eventSubscriptionSubjects = /* @__PURE__ */ new Map();
  unstable_on(event, callback) {
    let subject = this._eventSubscriptionSubjects.get(event);
    if (!subject) {
      subject = new EventSubscriptionSubject({
        event,
        binding: this._threadBinding
      });
      this._eventSubscriptionSubjects.set(event, subject);
    }
    return subject.subscribe(callback);
  }
};

// node_modules/@assistant-ui/core/dist/runtimes/readonly/ReadonlyThreadRuntimeCore.js
var READONLY_THREAD_ERROR = new Error("This is a readonly thread. You cannot perform mutations on readonly threads.");
var ReadonlyThreadRuntimeCore = class extends BaseSubscribable {
  _messages = [];
  get messages() {
    return this._messages;
  }
  setMessages(messages) {
    if (this._messages === messages) return;
    this._messages = messages;
    this._notifySubscribers();
  }
  getMessageById(messageId) {
    const idx = this._messages.findIndex((m) => m.id === messageId);
    if (idx === -1) return void 0;
    return {
      parentId: this._messages[idx - 1]?.id ?? null,
      message: this._messages[idx],
      index: idx
    };
  }
  getBranches(messageId) {
    if (this._messages.findIndex((m) => m.id === messageId) === -1) return [];
    return [messageId];
  }
  switchToBranch() {
    throw READONLY_THREAD_ERROR;
  }
  append() {
    throw READONLY_THREAD_ERROR;
  }
  startRun() {
    throw READONLY_THREAD_ERROR;
  }
  resumeRun() {
    throw READONLY_THREAD_ERROR;
  }
  cancelRun() {
  }
  addToolResult() {
    throw READONLY_THREAD_ERROR;
  }
  resumeToolCall() {
    throw READONLY_THREAD_ERROR;
  }
  respondToToolApproval() {
    throw READONLY_THREAD_ERROR;
  }
  speak() {
    throw READONLY_THREAD_ERROR;
  }
  stopSpeaking() {
  }
  connectVoice() {
    throw READONLY_THREAD_ERROR;
  }
  disconnectVoice() {
  }
  getVoiceVolume = () => 0;
  subscribeVoiceVolume = () => () => {
  };
  muteVoice() {
    throw READONLY_THREAD_ERROR;
  }
  unmuteVoice() {
    throw READONLY_THREAD_ERROR;
  }
  submitFeedback() {
    throw READONLY_THREAD_ERROR;
  }
  getModelContext() {
    return {};
  }
  exportExternalState() {
    throw READONLY_THREAD_ERROR;
  }
  importExternalState() {
    throw READONLY_THREAD_ERROR;
  }
  composer = {
    attachments: [],
    attachmentAccept: "*",
    async addAttachment() {
      throw READONLY_THREAD_ERROR;
    },
    async removeAttachment() {
      throw READONLY_THREAD_ERROR;
    },
    isEditing: false,
    canCancel: false,
    canSend: false,
    isEmpty: true,
    text: "",
    setText() {
      throw READONLY_THREAD_ERROR;
    },
    role: "user",
    setRole() {
      throw READONLY_THREAD_ERROR;
    },
    runConfig: {},
    setRunConfig() {
      throw READONLY_THREAD_ERROR;
    },
    async reset() {
    },
    async clearAttachments() {
    },
    send() {
      throw READONLY_THREAD_ERROR;
    },
    cancel() {
    },
    dictation: void 0,
    startDictation() {
      throw READONLY_THREAD_ERROR;
    },
    stopDictation() {
    },
    quote: void 0,
    setQuote() {
      throw READONLY_THREAD_ERROR;
    },
    subscribe() {
      return () => {
      };
    },
    unstable_on() {
      return () => {
      };
    }
  };
  getEditComposer() {
  }
  beginEdit() {
    throw READONLY_THREAD_ERROR;
  }
  speech = void 0;
  voice = void 0;
  capabilities = {
    switchToBranch: false,
    switchBranchDuringRun: false,
    edit: false,
    reload: false,
    cancel: false,
    unstable_copy: false,
    speech: false,
    dictation: false,
    voice: false,
    attachments: false,
    feedback: false,
    queue: false
  };
  isDisabled = false;
  isSendDisabled = false;
  isLoading = false;
  state = null;
  suggestions = [];
  extras = void 0;
  import() {
    throw READONLY_THREAD_ERROR;
  }
  export() {
    return { messages: this._messages.map((message, idx) => ({
      message,
      parentId: this._messages[idx - 1]?.id ?? null
    })) };
  }
  reset() {
    throw READONLY_THREAD_ERROR;
  }
  unstable_on() {
    return () => {
    };
  }
};

// node_modules/@assistant-ui/core/dist/store/runtime-clients/attachment-runtime-client.js
var AttachmentRuntimeClient = resource(({ runtime }) => {
  const state = tapSubscribable(runtime);
  return {
    getState: () => state,
    remove: runtime.remove,
    __internal_getRuntime: () => runtime
  };
});

// node_modules/@assistant-ui/core/dist/store/runtime-clients/composer-runtime-client.js
var ComposerAttachmentClientByIndex = resource(({ runtime, index }) => {
  return tapResource(AttachmentRuntimeClient({ runtime: tapMemo(() => runtime.getAttachmentByIndex(index), [runtime, index]) }));
});
var ComposerClient = resource(({ threadIdRef, messageIdRef, runtime }) => {
  const runtimeState = tapSubscribable(runtime);
  const emit = tapAssistantEmit();
  tapEffect(() => {
    const unsubscribers = [];
    for (const event of ["send", "attachmentAdd"]) {
      const unsubscribe = runtime.unstable_on(event, () => {
        emit(`composer.${event}`, {
          threadId: threadIdRef.current,
          ...messageIdRef && { messageId: messageIdRef.current }
        });
      });
      unsubscribers.push(unsubscribe);
    }
    unsubscribers.push(runtime.unstable_on("attachmentAddError", (payload) => {
      emit("composer.attachmentAddError", {
        threadId: threadIdRef.current,
        ...messageIdRef && { messageId: messageIdRef.current },
        ...payload.attachmentId && { attachmentId: payload.attachmentId },
        reason: payload.reason,
        message: payload.message
      });
    }));
    return () => {
      for (const unsub of unsubscribers) unsub();
    };
  }, [
    runtime,
    emit,
    threadIdRef,
    messageIdRef
  ]);
  const attachments = tapClientLookup(() => runtimeState.attachments.map((attachment, idx) => withKey(attachment.id, ComposerAttachmentClientByIndex({
    runtime,
    index: idx
  }))), [runtimeState.attachments, runtime]);
  const state = tapMemo(() => {
    return {
      text: runtimeState.text,
      role: runtimeState.role,
      attachments: attachments.state,
      runConfig: runtimeState.runConfig,
      isEditing: runtimeState.isEditing,
      canCancel: runtimeState.canCancel,
      canSend: runtimeState.canSend,
      attachmentAccept: runtimeState.attachmentAccept,
      isEmpty: runtimeState.isEmpty,
      type: runtimeState.type ?? "thread",
      dictation: runtimeState.dictation,
      quote: runtimeState.quote,
      queue: []
    };
  }, [runtimeState, attachments.state]);
  return {
    getState: () => state,
    setText: runtime.setText,
    setRole: runtime.setRole,
    setRunConfig: runtime.setRunConfig,
    addAttachment: runtime.addAttachment,
    reset: runtime.reset,
    clearAttachments: runtime.clearAttachments,
    send: runtime.send,
    cancel: runtime.cancel,
    beginEdit: runtime.beginEdit ?? (() => {
      throw new Error("beginEdit is not supported in this runtime");
    }),
    startDictation: runtime.startDictation,
    stopDictation: runtime.stopDictation,
    setQuote: runtime.setQuote,
    attachment: (selector) => {
      if ("id" in selector) return attachments.get({ key: selector.id });
      else return attachments.get(selector);
    },
    queueItem: () => {
      throw new Error("Queue is not supported in this runtime");
    },
    __internal_getRuntime: () => runtime
  };
});

// node_modules/@assistant-ui/core/dist/store/runtime-clients/message-part-runtime-client.js
var MessagePartClient = resource(({ runtime }) => {
  const state = tapSubscribable(runtime);
  return {
    getState: () => state,
    addToolResult: (result) => runtime.addToolResult(result),
    resumeToolCall: (payload) => runtime.resumeToolCall(payload),
    respondToToolApproval: (response) => runtime.respondToToolApproval(response),
    __internal_getRuntime: () => runtime
  };
});

// node_modules/@assistant-ui/core/dist/store/runtime-clients/message-runtime-client.js
var MessageAttachmentClientByIndex = resource(({ runtime, index }) => {
  return tapResource(AttachmentRuntimeClient({ runtime: tapMemo(() => runtime.getAttachmentByIndex(index), [runtime, index]) }));
});
var MessagePartByIndex = resource(({ runtime, index }) => {
  return tapResource(MessagePartClient({ runtime: tapMemo(() => runtime.getMessagePartByIndex(index), [runtime, index]) }));
});
var MessageClient = resource(({ runtime, threadIdRef }) => {
  const runtimeState = tapSubscribable(runtime);
  const [isCopiedState, setIsCopied] = tapState(false);
  const [isHoveringState, setIsHovering] = tapState(false);
  const messageIdRef = tapMemo(() => ({ get current() {
    return runtime.getState().id;
  } }), [runtime]);
  const composer = tapClientResource(ComposerClient({
    runtime: runtime.composer,
    threadIdRef,
    messageIdRef
  }));
  const parts = tapClientLookup(() => runtimeState.content.map((part, idx) => withKey("toolCallId" in part && part.toolCallId != null ? `toolCallId-${part.toolCallId}` : `index-${idx}`, MessagePartByIndex({
    runtime,
    index: idx
  }))), [runtimeState.content, runtime]);
  const attachments = tapClientLookup(() => (runtimeState.attachments ?? []).map((attachment, idx) => withKey(attachment.id, MessageAttachmentClientByIndex({
    runtime,
    index: idx
  }))), [runtimeState.attachments, runtime]);
  const state = tapMemo(() => {
    return {
      ...runtimeState,
      parts: parts.state,
      composer: composer.state,
      isCopied: isCopiedState,
      isHovering: isHoveringState
    };
  }, [
    runtimeState,
    parts.state,
    composer.state,
    isCopiedState,
    isHoveringState
  ]);
  return {
    getState: () => state,
    composer: () => composer.methods,
    reload: (config) => runtime.reload(config),
    speak: () => runtime.speak(),
    stopSpeaking: () => runtime.stopSpeaking(),
    submitFeedback: (feedback) => runtime.submitFeedback(feedback),
    switchToBranch: (options) => runtime.switchToBranch(options),
    getCopyText: () => runtime.unstable_getCopyText(),
    part: (selector) => {
      if ("index" in selector) return parts.get({ index: selector.index });
      else return parts.get({ key: `toolCallId-${selector.toolCallId}` });
    },
    attachment: (selector) => {
      if ("id" in selector) return attachments.get({ key: selector.id });
      else return attachments.get(selector);
    },
    setIsCopied,
    setIsHovering,
    __internal_getRuntime: () => runtime
  };
});

// node_modules/@assistant-ui/core/dist/store/runtime-clients/thread-runtime-client.js
var MessageClientById = resource(({ runtime, id, threadIdRef }) => {
  return tapResource(MessageClient({
    runtime: tapMemo(() => runtime.getMessageById(id), [runtime, id]),
    threadIdRef
  }));
});
var ThreadClient = resource(({ runtime }) => {
  const runtimeState = tapSubscribable(runtime);
  const emit = tapAssistantEmit();
  tapEffect(() => {
    const unsubscribers = [];
    for (const event of [
      "runStart",
      "runEnd",
      "initialize",
      "modelContextUpdate"
    ]) {
      const unsubscribe = runtime.unstable_on(event, () => {
        const threadId = runtime.getState()?.threadId || "unknown";
        emit(`thread.${event}`, { threadId });
      });
      unsubscribers.push(unsubscribe);
    }
    return () => {
      for (const unsub of unsubscribers) unsub();
    };
  }, [runtime, emit]);
  const threadIdRef = tapMemo(() => ({ get current() {
    return runtime.getState().threadId;
  } }), [runtime]);
  const composer = tapClientResource(ComposerClient({
    runtime: runtime.composer,
    threadIdRef
  }));
  const messages = tapClientLookup(() => runtimeState.messages.map((m) => withKey(m.id, MessageClientById({
    runtime,
    id: m.id,
    threadIdRef
  }))), [
    runtimeState.messages,
    runtime,
    threadIdRef
  ]);
  const state = tapMemo(() => {
    return {
      isEmpty: messages.state.length === 0 && !runtimeState.isLoading,
      isDisabled: runtimeState.isDisabled,
      isLoading: runtimeState.isLoading,
      isRunning: runtimeState.isRunning,
      capabilities: runtimeState.capabilities,
      state: runtimeState.state,
      suggestions: runtimeState.suggestions,
      extras: runtimeState.extras,
      speech: runtimeState.speech,
      voice: runtimeState.voice,
      composer: composer.state,
      messages: messages.state
    };
  }, [
    runtimeState,
    messages,
    composer.state
  ]);
  return {
    getState: () => state,
    composer: () => composer.methods,
    append: runtime.append,
    startRun: runtime.startRun,
    resumeRun: runtime.resumeRun,
    cancelRun: runtime.cancelRun,
    getModelContext: runtime.getModelContext,
    export: runtime.export,
    import: runtime.import,
    reset: runtime.reset,
    stopSpeaking: runtime.stopSpeaking,
    connectVoice: runtime.connectVoice,
    disconnectVoice: runtime.disconnectVoice,
    getVoiceVolume: runtime.getVoiceVolume,
    subscribeVoiceVolume: runtime.subscribeVoiceVolume,
    muteVoice: runtime.muteVoice,
    unmuteVoice: runtime.unmuteVoice,
    message: (selector) => {
      if ("id" in selector) return messages.get({ key: selector.id });
      else return messages.get(selector);
    },
    __internal_getRuntime: () => runtime
  };
});

// node_modules/@assistant-ui/core/dist/react/providers/ReadonlyThreadProvider.js
var import_react19 = __toESM(require_react(), 1);
var import_jsx_runtime10 = __toESM(require_jsx_runtime(), 1);
var READONLY_THREAD_PATH = Object.freeze({
  ref: "readonly-thread",
  threadSelector: { type: "main" }
});
var READONLY_THREAD_LIST_ITEM = Object.freeze({
  id: "readonly",
  remoteId: void 0,
  externalId: void 0,
  isMain: true,
  status: "regular",
  title: void 0
});
var READONLY_THREAD_LIST_ITEM_BINDING = Object.freeze({
  path: READONLY_THREAD_PATH,
  getState: () => READONLY_THREAD_LIST_ITEM,
  subscribe: () => () => {
  }
});
var ReadonlyThreadProvider = ({ messages, children }) => {
  const [core] = (0, import_react19.useState)(() => {
    const c = new ReadonlyThreadRuntimeCore();
    c.setMessages(messages);
    return c;
  });
  (0, import_react19.useEffect)(() => {
    core.setMessages(messages);
  }, [core, messages]);
  return (0, import_jsx_runtime10.jsx)(AuiProvider, {
    value: useAui({
      thread: ThreadClient({ runtime: (0, import_react19.useMemo)(() => {
        return new ThreadRuntimeImpl({
          path: READONLY_THREAD_PATH,
          getState: () => core,
          subscribe: (callback) => core.subscribe(callback),
          outerSubscribe: (callback) => core.subscribe(callback)
        }, READONLY_THREAD_LIST_ITEM_BINDING);
      }, [core]) }),
      composer: Derived({
        source: "thread",
        query: {},
        get: (aui) => aui.thread().composer()
      })
    }),
    children
  });
};

// node_modules/@assistant-ui/core/dist/react/runtimes/RuntimeAdapterProvider.js
var import_react20 = __toESM(require_react(), 1);
var import_jsx_runtime11 = __toESM(require_jsx_runtime(), 1);
var RuntimeAdaptersContext = (0, import_react20.createContext)(null);
var RuntimeAdapterProvider = ({ adapters, children }) => {
  const context2 = (0, import_react20.useContext)(RuntimeAdaptersContext);
  const value = (0, import_react20.useMemo)(() => ({
    ...context2,
    ...adapters
  }), [context2, adapters]);
  return (0, import_jsx_runtime11.jsx)(RuntimeAdaptersContext.Provider, {
    value,
    children
  });
};
var useRuntimeAdapters = () => {
  return (0, import_react20.useContext)(RuntimeAdaptersContext);
};

// node_modules/@assistant-ui/core/dist/runtime/api/thread-list-item-runtime.js
var ThreadListItemRuntimeImpl = class {
  _core;
  _threadListBinding;
  get path() {
    return this._core.path;
  }
  constructor(_core, _threadListBinding) {
    this._core = _core;
    this._threadListBinding = _threadListBinding;
    this.__internal_bindMethods();
  }
  __internal_bindMethods() {
    this.switchTo = this.switchTo.bind(this);
    this.rename = this.rename.bind(this);
    this.updateCustom = this.updateCustom.bind(this);
    this.archive = this.archive.bind(this);
    this.unarchive = this.unarchive.bind(this);
    this.delete = this.delete.bind(this);
    this.initialize = this.initialize.bind(this);
    this.generateTitle = this.generateTitle.bind(this);
    this.subscribe = this.subscribe.bind(this);
    this.unstable_on = this.unstable_on.bind(this);
    this.getState = this.getState.bind(this);
    this.detach = this.detach.bind(this);
  }
  getState() {
    return this._core.getState();
  }
  switchTo(options) {
    const state = this._core.getState();
    return this._threadListBinding.switchToThread(state.id, options);
  }
  rename(newTitle) {
    const state = this._core.getState();
    return this._threadListBinding.rename(state.id, newTitle);
  }
  updateCustom(custom) {
    const state = this._core.getState();
    if (!this._threadListBinding.updateCustom) throw new Error("Thread list runtime does not support updating custom metadata");
    return this._threadListBinding.updateCustom(state.id, custom);
  }
  archive() {
    const state = this._core.getState();
    return this._threadListBinding.archive(state.id);
  }
  unarchive() {
    const state = this._core.getState();
    return this._threadListBinding.unarchive(state.id);
  }
  delete() {
    const state = this._core.getState();
    return this._threadListBinding.delete(state.id);
  }
  initialize() {
    const state = this._core.getState();
    return this._threadListBinding.initialize(state.id);
  }
  generateTitle() {
    const state = this._core.getState();
    return this._threadListBinding.generateTitle(state.id);
  }
  unstable_on(event, callback) {
    let prevIsMain = this._core.getState().isMain;
    let prevThreadId = this._core.getState().id;
    return this.subscribe(() => {
      const currentState = this._core.getState();
      const newIsMain = currentState.isMain;
      const newThreadId = currentState.id;
      if (prevIsMain === newIsMain && prevThreadId === newThreadId) return;
      prevIsMain = newIsMain;
      prevThreadId = newThreadId;
      if (event === "switchedTo" && !newIsMain) return;
      if (event === "switchedAway" && newIsMain) return;
      callback({});
    });
  }
  subscribe(callback) {
    return this._core.subscribe(callback);
  }
  detach() {
    const state = this._core.getState();
    this._threadListBinding.detach(state.id);
  }
  __internal_getRuntime() {
    return this;
  }
};

// node_modules/@assistant-ui/core/dist/runtime/api/thread-list-runtime.js
var RESOLVED_PROMISE = Promise.resolve();
var getThreadListState = (threadList) => {
  return {
    mainThreadId: threadList.mainThreadId,
    newThreadId: threadList.newThreadId,
    threadIds: threadList.threadIds,
    archivedThreadIds: threadList.archivedThreadIds,
    isLoading: threadList.isLoading,
    isLoadingMore: threadList.isLoadingMore ?? false,
    hasMore: threadList.hasMore ?? false,
    threadItems: threadList.threadItems
  };
};
var getThreadListItemState = (threadList, threadId) => {
  if (threadId === void 0) return SKIP_UPDATE;
  const threadData = threadList.getItemById(threadId);
  if (!threadData) return SKIP_UPDATE;
  return {
    id: threadData.id,
    remoteId: threadData.remoteId,
    externalId: threadData.externalId,
    title: threadData.title,
    status: threadData.status,
    custom: threadData.custom,
    isMain: threadData.id === threadList.mainThreadId
  };
};
var ThreadListRuntimeImpl = class {
  _core;
  _runtimeFactory;
  _getState;
  constructor(_core, _runtimeFactory = ThreadRuntimeImpl) {
    this._core = _core;
    this._runtimeFactory = _runtimeFactory;
    const stateBinding = new LazyMemoizeSubject({
      path: {},
      getState: () => getThreadListState(_core),
      subscribe: (callback) => _core.subscribe(callback)
    });
    this._getState = stateBinding.getState.bind(stateBinding);
    this._mainThreadListItemRuntime = new ThreadListItemRuntimeImpl(new ShallowMemoizeSubject({
      path: {
        ref: `threadItems[main]`,
        threadSelector: { type: "main" }
      },
      getState: () => {
        return getThreadListItemState(this._core, this._core.mainThreadId);
      },
      subscribe: (callback) => this._core.subscribe(callback)
    }), this._core);
    this.main = new _runtimeFactory(new NestedSubscriptionSubject({
      path: {
        ref: "threads.main",
        threadSelector: { type: "main" }
      },
      getState: () => _core.getMainThreadRuntimeCore(),
      subscribe: (callback) => _core.subscribe(callback)
    }), this._mainThreadListItemRuntime);
    this.__internal_bindMethods();
  }
  __internal_bindMethods() {
    this.switchToThread = this.switchToThread.bind(this);
    this.switchToNewThread = this.switchToNewThread.bind(this);
    this.getLoadThreadsPromise = this.getLoadThreadsPromise.bind(this);
    this.reload = this.reload.bind(this);
    this.loadMore = this.loadMore.bind(this);
    this.getState = this.getState.bind(this);
    this.subscribe = this.subscribe.bind(this);
    this.getById = this.getById.bind(this);
    this.getItemById = this.getItemById.bind(this);
    this.getItemByIndex = this.getItemByIndex.bind(this);
    this.getArchivedItemByIndex = this.getArchivedItemByIndex.bind(this);
  }
  switchToThread(threadId, options) {
    return this._core.switchToThread(threadId, options);
  }
  switchToNewThread() {
    return this._core.switchToNewThread();
  }
  getLoadThreadsPromise() {
    return this._core.getLoadThreadsPromise();
  }
  reload() {
    return this._core.reload?.() ?? RESOLVED_PROMISE;
  }
  loadMore() {
    return this._core.loadMore?.() ?? RESOLVED_PROMISE;
  }
  getState() {
    return this._getState();
  }
  subscribe(callback) {
    return this._core.subscribe(callback);
  }
  _mainThreadListItemRuntime;
  main;
  get mainItem() {
    return this._mainThreadListItemRuntime;
  }
  getById(threadId) {
    return new this._runtimeFactory(new NestedSubscriptionSubject({
      path: {
        ref: `threads[threadId=${JSON.stringify(threadId)}]`,
        threadSelector: {
          type: "threadId",
          threadId
        }
      },
      getState: () => this._core.getThreadRuntimeCore(threadId),
      subscribe: (callback) => this._core.subscribe(callback)
    }), this.mainItem);
  }
  getItemByIndex(idx) {
    return new ThreadListItemRuntimeImpl(new ShallowMemoizeSubject({
      path: {
        ref: `threadItems[${idx}]`,
        threadSelector: {
          type: "index",
          index: idx
        }
      },
      getState: () => {
        return getThreadListItemState(this._core, this._core.threadIds[idx]);
      },
      subscribe: (callback) => this._core.subscribe(callback)
    }), this._core);
  }
  getArchivedItemByIndex(idx) {
    return new ThreadListItemRuntimeImpl(new ShallowMemoizeSubject({
      path: {
        ref: `archivedThreadItems[${idx}]`,
        threadSelector: {
          type: "archiveIndex",
          index: idx
        }
      },
      getState: () => {
        return getThreadListItemState(this._core, this._core.archivedThreadIds[idx]);
      },
      subscribe: (callback) => this._core.subscribe(callback)
    }), this._core);
  }
  getItemById(threadId) {
    return new ThreadListItemRuntimeImpl(new ShallowMemoizeSubject({
      path: {
        ref: `threadItems[threadId=${threadId}]`,
        threadSelector: {
          type: "threadId",
          threadId
        }
      },
      getState: () => {
        return getThreadListItemState(this._core, threadId);
      },
      subscribe: (callback) => this._core.subscribe(callback)
    }), this._core);
  }
};

// node_modules/@assistant-ui/core/dist/runtime/api/assistant-runtime.js
var AssistantRuntimeImpl = class {
  _core;
  threads;
  _thread;
  constructor(_core) {
    this._core = _core;
    this.threads = new ThreadListRuntimeImpl(_core.threads);
    this._thread = this.threads.main;
    this.__internal_bindMethods();
  }
  __internal_bindMethods() {
    this.registerModelContextProvider = this.registerModelContextProvider.bind(this);
  }
  get thread() {
    return this._thread;
  }
  registerModelContextProvider(provider) {
    return this._core.registerModelContextProvider(provider);
  }
};

// node_modules/@assistant-ui/core/dist/runtime/base/base-assistant-runtime-core.js
var BaseAssistantRuntimeCore = class {
  _contextProvider = new CompositeContextProvider();
  registerModelContextProvider(provider) {
    return this._contextProvider.registerModelContextProvider(provider);
  }
  getModelContextProvider() {
    return this._contextProvider;
  }
};

// node_modules/@assistant-ui/core/dist/runtimes/external-store/external-store-thread-list-runtime-core.js
var EMPTY_ARRAY2 = Object.freeze([]);
var DEFAULT_THREAD_ID = "DEFAULT_THREAD_ID";
var DEFAULT_THREADS = Object.freeze([DEFAULT_THREAD_ID]);
var DEFAULT_THREAD = Object.freeze({
  id: DEFAULT_THREAD_ID,
  remoteId: void 0,
  externalId: void 0,
  status: "regular"
});
var RESOLVED_PROMISE2 = Promise.resolve();
var DEFAULT_THREAD_DATA = Object.freeze({ [DEFAULT_THREAD_ID]: DEFAULT_THREAD });
var ExternalStoreThreadListRuntimeCore = class {
  threadFactory;
  _mainThreadId = DEFAULT_THREAD_ID;
  _threads = DEFAULT_THREADS;
  _archivedThreads = EMPTY_ARRAY2;
  _threadData = DEFAULT_THREAD_DATA;
  adapter = {};
  get isLoading() {
    return this.adapter.isLoading ?? false;
  }
  get newThreadId() {
  }
  get threadIds() {
    return this._threads;
  }
  get archivedThreadIds() {
    return this._archivedThreads;
  }
  get threadItems() {
    return this._threadData;
  }
  getLoadThreadsPromise() {
    return RESOLVED_PROMISE2;
  }
  _mainThread;
  get mainThreadId() {
    return this._mainThreadId;
  }
  constructor(adapter = {}, threadFactory) {
    this.threadFactory = threadFactory;
    this.__internal_setAdapter(adapter, true);
  }
  getMainThreadRuntimeCore() {
    return this._mainThread;
  }
  getThreadRuntimeCore() {
    throw new Error("Method not implemented.");
  }
  getItemById(threadId) {
    return this._threadData[threadId];
  }
  __internal_setAdapter(adapter, initialLoad = false) {
    const previousAdapter = this.adapter;
    this.adapter = adapter;
    const newThreadId = adapter.threadId ?? DEFAULT_THREAD_ID;
    const newThreads = adapter.threads ?? EMPTY_ARRAY2;
    const newArchivedThreads = adapter.archivedThreads ?? EMPTY_ARRAY2;
    const previousThreadId = previousAdapter.threadId ?? DEFAULT_THREAD_ID;
    const previousThreads = previousAdapter.threads ?? EMPTY_ARRAY2;
    const previousArchivedThreads = previousAdapter.archivedThreads ?? EMPTY_ARRAY2;
    if (!initialLoad && previousThreadId === newThreadId && previousThreads === newThreads && previousArchivedThreads === newArchivedThreads) return;
    if (previousThreads !== newThreads || previousArchivedThreads !== newArchivedThreads || previousThreadId !== newThreadId) this._threadData = {
      ...DEFAULT_THREAD_DATA,
      ...Object.fromEntries(adapter.threads?.map((t) => [t.id, {
        ...t,
        remoteId: t.remoteId,
        externalId: t.externalId,
        status: "regular"
      }]) ?? []),
      ...Object.fromEntries(adapter.archivedThreads?.map((t) => [t.id, {
        ...t,
        remoteId: t.remoteId,
        externalId: t.externalId,
        status: "archived"
      }]) ?? [])
    };
    if (previousThreads !== newThreads) this._threads = this.adapter.threads?.map((t) => t.id) ?? EMPTY_ARRAY2;
    if (previousArchivedThreads !== newArchivedThreads) this._archivedThreads = this.adapter.archivedThreads?.map((t) => t.id) ?? EMPTY_ARRAY2;
    if (initialLoad || previousThreadId !== newThreadId) {
      this._mainThreadId = newThreadId;
      this._mainThread = this.threadFactory();
    }
    if (!this._threadData[this._mainThreadId]) this._threadData = {
      ...this._threadData,
      [this._mainThreadId]: {
        id: this._mainThreadId,
        remoteId: void 0,
        externalId: void 0,
        status: "regular"
      }
    };
    this._notifySubscribers();
  }
  async switchToThread(threadId, _options) {
    if (this._mainThreadId === threadId) return;
    const onSwitchToThread = this.adapter.onSwitchToThread;
    if (!onSwitchToThread) throw new Error("External store adapter does not support switching to thread");
    await onSwitchToThread(threadId);
  }
  async switchToNewThread() {
    const onSwitchToNewThread = this.adapter.onSwitchToNewThread;
    if (!onSwitchToNewThread) throw new Error("External store adapter does not support switching to new thread");
    await onSwitchToNewThread();
  }
  async rename(threadId, newTitle) {
    const onRename = this.adapter.onRename;
    if (!onRename) throw new Error("External store adapter does not support renaming");
    await onRename(threadId, newTitle);
  }
  async updateCustom(threadId, custom) {
    const onUpdateCustom = this.adapter.onUpdateCustom;
    if (!onUpdateCustom) throw new Error("External store adapter does not support updating custom metadata");
    await onUpdateCustom(threadId, custom);
  }
  async detach() {
  }
  async archive(threadId) {
    const onArchive = this.adapter.onArchive;
    if (!onArchive) throw new Error("External store adapter does not support archiving");
    await onArchive(threadId);
  }
  async unarchive(threadId) {
    const onUnarchive = this.adapter.onUnarchive;
    if (!onUnarchive) throw new Error("External store adapter does not support unarchiving");
    await onUnarchive(threadId);
  }
  async delete(threadId) {
    const onDelete = this.adapter.onDelete;
    if (!onDelete) throw new Error("External store adapter does not support deleting");
    await onDelete(threadId);
  }
  initialize(threadId) {
    return Promise.resolve({
      remoteId: threadId,
      externalId: void 0
    });
  }
  generateTitle() {
    throw new Error("Method not implemented.");
  }
  _subscriptions = /* @__PURE__ */ new Set();
  subscribe(callback) {
    this._subscriptions.add(callback);
    return () => this._subscriptions.delete(callback);
  }
  _notifySubscribers() {
    for (const callback of this._subscriptions) callback();
  }
};

// node_modules/@assistant-ui/core/dist/runtime/base/base-composer-runtime-core.js
var isAttachmentComplete = (a) => a.status.type === "complete";
var BaseComposerRuntimeCore = class extends BaseSubscribable {
  isEditing = true;
  get attachmentAccept() {
    return this.getAttachmentAdapter()?.accept ?? "*";
  }
  _attachments = [];
  get attachments() {
    return this._attachments;
  }
  setAttachments(value) {
    this._attachments = value;
    this._notifySubscribers();
  }
  get isEmpty() {
    return !this.text.trim() && !this.attachments.length;
  }
  _text = "";
  get text() {
    return this._text;
  }
  _role = "user";
  get role() {
    return this._role;
  }
  _runConfig = {};
  get runConfig() {
    return this._runConfig;
  }
  _quote = void 0;
  get quote() {
    return this._quote;
  }
  setQuote(quote) {
    if (this._quote === quote) return;
    this._quote = quote;
    this._notifySubscribers();
  }
  setText(value) {
    if (this._text === value) return;
    this._text = value;
    if (this._dictation) {
      this._dictationBaseText = value;
      this._currentInterimText = "";
      const { status, inputDisabled } = this._dictation;
      this._dictation = inputDisabled ? {
        status,
        inputDisabled
      } : { status };
    }
    this._notifySubscribers();
  }
  setRole(role) {
    if (this._role === role) return;
    this._role = role;
    this._notifySubscribers();
  }
  setRunConfig(runConfig) {
    if (this._runConfig === runConfig) return;
    this._runConfig = runConfig;
    this._notifySubscribers();
  }
  _emptyTextAndAttachments() {
    this._attachments = [];
    this._text = "";
    this._notifySubscribers();
  }
  async _onClearAttachments() {
    const adapter = this.getAttachmentAdapter();
    if (adapter) {
      const pending = this._attachments.filter((a) => !isAttachmentComplete(a));
      await Promise.all(pending.map((a) => adapter.remove(a)));
    }
  }
  async reset() {
    if (this._attachments.length === 0 && this._text === "" && this._role === "user" && Object.keys(this._runConfig).length === 0 && this._quote === void 0) return;
    this._role = "user";
    this._runConfig = {};
    this._quote = void 0;
    const task = this._onClearAttachments();
    this._emptyTextAndAttachments();
    await task;
  }
  async clearAttachments() {
    const task = this._onClearAttachments();
    this.setAttachments([]);
    await task;
  }
  async send(options) {
    if (!this.canSend) return;
    if (this._dictationSession) {
      this._dictationSession.cancel();
      this._cleanupDictation();
    }
    const adapter = this.getAttachmentAdapter();
    const attachments = this.attachments.length > 0 ? Promise.all(this.attachments.map(async (a) => {
      if (isAttachmentComplete(a)) return a;
      if (!adapter) throw new Error("Attachments are not supported");
      return await adapter.send(a);
    })) : [];
    const text = this.text;
    const quote = this._quote;
    this._quote = void 0;
    this._emptyTextAndAttachments();
    const message = {
      createdAt: /* @__PURE__ */ new Date(),
      role: this.role,
      content: text ? [{
        type: "text",
        text
      }] : [],
      attachments: await attachments,
      runConfig: this.runConfig,
      metadata: { custom: { ...quote ? { quote } : {} } }
    };
    this.handleSend(message, options);
    this._notifyEventSubscribers("send", {});
  }
  cancel() {
    this.handleCancel();
  }
  async addAttachment(fileOrAttachment) {
    if (!(fileOrAttachment instanceof File)) {
      const adapter2 = this.getAttachmentAdapter();
      if (adapter2 && !fileMatchesAccept({
        name: fileOrAttachment.name,
        type: fileOrAttachment.contentType ?? ""
      }, adapter2.accept)) {
        const message = `File type ${fileOrAttachment.contentType || "unknown"} is not accepted. Accepted types: ${adapter2.accept}`;
        const err = new Error(message);
        this._safeEmitAttachmentAddError("not-accepted", message, void 0, err);
        throw err;
      }
      const a = {
        id: fileOrAttachment.id ?? generateId(),
        type: fileOrAttachment.type ?? "document",
        name: fileOrAttachment.name,
        contentType: fileOrAttachment.contentType,
        content: fileOrAttachment.content,
        status: { type: "complete" }
      };
      this._attachments = [...this._attachments, a];
      this._notifySubscribers();
      this._notifyEventSubscribers("attachmentAdd", {});
      return;
    }
    const upsertAttachment = (a) => {
      const idx = this._attachments.findIndex((attachment) => attachment.id === a.id);
      if (idx !== -1) this._attachments = [
        ...this._attachments.slice(0, idx),
        a,
        ...this._attachments.slice(idx + 1)
      ];
      else this._attachments = [...this._attachments, a];
      this._notifySubscribers();
    };
    const adapter = this.getAttachmentAdapter();
    if (!adapter) {
      const message = "Attachments are not supported";
      const err = new Error(message);
      this._safeEmitAttachmentAddError("no-adapter", message, void 0, err);
      throw err;
    }
    if (!fileMatchesAccept({
      name: fileOrAttachment.name,
      type: fileOrAttachment.type
    }, adapter.accept)) {
      const message = `File type ${fileOrAttachment.type || "unknown"} is not accepted. Accepted types: ${adapter.accept}`;
      const err = new Error(message);
      this._safeEmitAttachmentAddError("not-accepted", message, void 0, err);
      throw err;
    }
    let lastAttachment;
    try {
      const promiseOrGenerator = adapter.add({ file: fileOrAttachment });
      if (Symbol.asyncIterator in promiseOrGenerator) for await (const r of promiseOrGenerator) {
        lastAttachment = r;
        upsertAttachment(r);
      }
      else {
        lastAttachment = await promiseOrGenerator;
        upsertAttachment(lastAttachment);
      }
    } catch (e) {
      if (lastAttachment) upsertAttachment({
        ...lastAttachment,
        status: {
          type: "incomplete",
          reason: "error"
        }
      });
      this._safeEmitAttachmentAddError("adapter-error", e instanceof Error ? e.message : String(e), lastAttachment?.id, e instanceof Error ? e : void 0);
      throw e;
    }
    if (lastAttachment?.status.type === "incomplete" && lastAttachment.status.reason === "error") this._safeEmitAttachmentAddError("adapter-error", "Attachment upload did not complete successfully.", lastAttachment?.id);
    else this._notifyEventSubscribers("attachmentAdd", {});
  }
  _safeEmitAttachmentAddError(reason, message, attachmentId, error) {
    try {
      this._notifyEventSubscribers("attachmentAddError", {
        reason,
        message,
        ...attachmentId !== void 0 && { attachmentId },
        ...error !== void 0 && { error }
      });
    } catch (subscriberError) {
      console.error("[assistant-ui] attachmentAddError subscriber threw:", subscriberError);
    }
  }
  async removeAttachment(attachmentId) {
    const index = this._attachments.findIndex((a) => a.id === attachmentId);
    if (index === -1) throw new Error("Attachment not found");
    const attachment = this._attachments[index];
    if (!isAttachmentComplete(attachment)) {
      const adapter = this.getAttachmentAdapter();
      if (!adapter) throw new Error("Attachments are not supported");
      await adapter.remove(attachment);
    }
    this._attachments = this._attachments.filter((a) => a.id !== attachmentId);
    this._notifySubscribers();
  }
  _dictation;
  _dictationSession;
  _dictationUnsubscribes = [];
  _dictationBaseText = "";
  _currentInterimText = "";
  _dictationSessionIdCounter = 0;
  _activeDictationSessionId;
  _isCleaningDictation = false;
  get dictation() {
    return this._dictation;
  }
  _isActiveSession(sessionId, session) {
    return this._activeDictationSessionId === sessionId && this._dictationSession === session;
  }
  startDictation() {
    const adapter = this.getDictationAdapter();
    if (!adapter) throw new Error("Dictation adapter not configured");
    if (this._dictationSession) {
      for (const unsub of this._dictationUnsubscribes) unsub();
      this._dictationUnsubscribes = [];
      this._dictationSession.stop().catch(() => {
      });
      this._dictationSession = void 0;
    }
    const inputDisabled = adapter.disableInputDuringDictation ?? false;
    this._dictationBaseText = this._text;
    this._currentInterimText = "";
    const session = adapter.listen();
    this._dictationSession = session;
    const sessionId = ++this._dictationSessionIdCounter;
    this._activeDictationSessionId = sessionId;
    this._dictation = {
      status: session.status,
      inputDisabled
    };
    this._notifySubscribers();
    const unsubSpeech = session.onSpeech((result) => {
      if (!this._isActiveSession(sessionId, session)) return;
      const isFinal = result.isFinal !== false;
      const separator = this._dictationBaseText && !this._dictationBaseText.endsWith(" ") && result.transcript ? " " : "";
      if (isFinal) {
        this._dictationBaseText = this._dictationBaseText + separator + result.transcript;
        this._currentInterimText = "";
        this._text = this._dictationBaseText;
        if (this._dictation) {
          const { transcript: _, ...rest } = this._dictation;
          this._dictation = rest;
        }
        this._notifySubscribers();
      } else {
        this._currentInterimText = separator + result.transcript;
        this._text = this._dictationBaseText + this._currentInterimText;
        if (this._dictation) this._dictation = {
          ...this._dictation,
          transcript: result.transcript
        };
        this._notifySubscribers();
      }
    });
    this._dictationUnsubscribes.push(unsubSpeech);
    const unsubStart = session.onSpeechStart(() => {
      if (!this._isActiveSession(sessionId, session)) return;
      this._dictation = {
        status: { type: "running" },
        inputDisabled,
        ...this._dictation?.transcript && { transcript: this._dictation.transcript }
      };
      this._notifySubscribers();
    });
    this._dictationUnsubscribes.push(unsubStart);
    const unsubEnd = session.onSpeechEnd(() => {
      this._cleanupDictation({ sessionId });
    });
    this._dictationUnsubscribes.push(unsubEnd);
    const statusInterval = setInterval(() => {
      if (!this._isActiveSession(sessionId, session)) return;
      if (session.status.type === "ended") this._cleanupDictation({ sessionId });
    }, 100);
    this._dictationUnsubscribes.push(() => clearInterval(statusInterval));
  }
  stopDictation() {
    if (!this._dictationSession) return;
    const session = this._dictationSession;
    const sessionId = this._activeDictationSessionId;
    session.stop().finally(() => {
      this._cleanupDictation({ sessionId });
    });
  }
  _cleanupDictation(options) {
    if (options?.sessionId !== void 0 && options.sessionId !== this._activeDictationSessionId || this._isCleaningDictation) return;
    this._isCleaningDictation = true;
    try {
      for (const unsub of this._dictationUnsubscribes) unsub();
      this._dictationUnsubscribes = [];
      this._dictationSession = void 0;
      this._activeDictationSessionId = void 0;
      this._dictation = void 0;
      this._dictationBaseText = "";
      this._currentInterimText = "";
      this._notifySubscribers();
    } finally {
      this._isCleaningDictation = false;
    }
  }
  _eventSubscribers = /* @__PURE__ */ new Map();
  _notifyEventSubscribers(event, payload) {
    const subscribers = this._eventSubscribers.get(event);
    if (!subscribers) return;
    for (const callback of subscribers) callback(payload);
  }
  unstable_on(event, callback) {
    const wrapped = callback;
    let subscribers = this._eventSubscribers.get(event);
    if (!subscribers) {
      subscribers = /* @__PURE__ */ new Set();
      this._eventSubscribers.set(event, subscribers);
    }
    subscribers.add(wrapped);
    return () => {
      this._eventSubscribers.get(event)?.delete(wrapped);
    };
  }
};

// node_modules/@assistant-ui/core/dist/runtime/base/default-thread-composer-runtime-core.js
var DefaultThreadComposerRuntimeCore = class extends BaseComposerRuntimeCore {
  runtime;
  _canCancel = false;
  get canCancel() {
    return this._canCancel;
  }
  get canSend() {
    return !this.isEmpty && !this.runtime.isSendDisabled;
  }
  getAttachmentAdapter() {
    return this.runtime.adapters?.attachments;
  }
  getDictationAdapter() {
    return this.runtime.adapters?.dictation;
  }
  constructor(runtime) {
    super();
    this.runtime = runtime;
    this.connect();
  }
  connect() {
    let lastIsSendDisabled = this.runtime.isSendDisabled;
    return this.runtime.subscribe(() => {
      let changed = false;
      if (this.canCancel !== this.runtime.capabilities.cancel) {
        this._canCancel = this.runtime.capabilities.cancel;
        changed = true;
      }
      if (lastIsSendDisabled !== this.runtime.isSendDisabled) {
        lastIsSendDisabled = this.runtime.isSendDisabled;
        changed = true;
      }
      if (changed) this._notifySubscribers();
    });
  }
  async handleSend(message, options) {
    this.runtime.append({
      ...message,
      parentId: this.runtime.messages.at(-1)?.id ?? null,
      sourceId: null,
      startRun: options?.startRun
    });
  }
  async handleCancel() {
    this.runtime.cancelRun();
  }
};

// node_modules/@assistant-ui/core/dist/runtime/base/default-edit-composer-runtime-core.js
var DefaultEditComposerRuntimeCore = class extends BaseComposerRuntimeCore {
  runtime;
  endEditCallback;
  get canCancel() {
    return true;
  }
  get canSend() {
    return !this.isEmpty;
  }
  getAttachmentAdapter() {
    return this.runtime.adapters?.attachments;
  }
  getDictationAdapter() {
    return this.runtime.adapters?.dictation;
  }
  _previousText;
  _previousAttachments;
  _nonTextPassthrough;
  _parentId;
  _sourceId;
  constructor(runtime, endEditCallback, { parentId, message }) {
    super();
    this.runtime = runtime;
    this.endEditCallback = endEditCallback;
    this._parentId = parentId;
    this._sourceId = message.id;
    this._previousText = getThreadMessageText(message);
    this.setText(this._previousText);
    this.setRole(message.role);
    if (message.role === "user") {
      this._previousAttachments = [...message.attachments ?? [], ...liftNonTextParts(message.content)];
      this._nonTextPassthrough = [];
    } else {
      this._previousAttachments = message.attachments ?? [];
      this._nonTextPassthrough = message.content.filter((p) => p.type !== "text");
    }
    this.setAttachments(this._previousAttachments);
    this.setRunConfig({ ...runtime.composer.runConfig });
  }
  get parentId() {
    return this._parentId;
  }
  get sourceId() {
    return this._sourceId;
  }
  async handleSend(message, options) {
    const text = getThreadMessageText(message);
    const attachmentsChanged = !attachmentsEqual(message.attachments ?? [], this._previousAttachments);
    if (text !== this._previousText || attachmentsChanged || options?.startRun) {
      const content = this._nonTextPassthrough.length > 0 ? [...message.content, ...this._nonTextPassthrough] : message.content;
      this.runtime.append({
        ...message,
        content,
        parentId: this._parentId,
        sourceId: this._sourceId,
        startRun: options?.startRun
      });
    }
    this.handleCancel();
  }
  handleCancel() {
    this.endEditCallback();
    this._notifySubscribers();
  }
};

// node_modules/@assistant-ui/core/dist/runtime/base/base-thread-runtime-core.js
var BaseThreadRuntimeCore = class {
  _contextProvider;
  _subscriptions = /* @__PURE__ */ new Set();
  _isInitialized = false;
  repository = new MessageRepository();
  _voiceMessages = [];
  _voiceGeneration = 0;
  _cachedMergedMessages = null;
  _cachedVoiceGeneration = -1;
  _cachedMergedBase = null;
  _markVoiceMessagesDirty() {
    this._voiceGeneration++;
    this._cachedMergedMessages = null;
  }
  _getBaseMessages() {
    return this.repository.getMessages();
  }
  get messages() {
    if (this._voiceMessages.length === 0) return this._getBaseMessages();
    const base = this._getBaseMessages();
    if (this._cachedVoiceGeneration !== this._voiceGeneration || this._cachedMergedBase !== base) {
      this._cachedMergedMessages = [...base, ...this._voiceMessages];
      this._cachedVoiceGeneration = this._voiceGeneration;
      this._cachedMergedBase = base;
    }
    return this._cachedMergedMessages;
  }
  get state() {
    let mostRecentAssistantMessage;
    for (const message of this.messages) if (message.role === "assistant") mostRecentAssistantMessage = message;
    return mostRecentAssistantMessage?.metadata.unstable_state ?? null;
  }
  composer = new DefaultThreadComposerRuntimeCore(this);
  constructor(_contextProvider) {
    this._contextProvider = _contextProvider;
  }
  getModelContext() {
    return this._contextProvider.getModelContext();
  }
  _editComposers = /* @__PURE__ */ new Map();
  getEditComposer(messageId) {
    return this._editComposers.get(messageId);
  }
  beginEdit(messageId) {
    if (this._editComposers.has(messageId)) throw new Error("Edit already in progress");
    this._editComposers.set(messageId, new DefaultEditComposerRuntimeCore(this, () => this._editComposers.delete(messageId), this.repository.getMessage(messageId)));
    this._notifySubscribers();
  }
  getMessageById(messageId) {
    try {
      return this.repository.getMessage(messageId);
    } catch {
      const baseMessages = this.repository.getMessages();
      const voiceIdx = this._voiceMessages.findIndex((m) => m.id === messageId);
      if (voiceIdx !== -1) return {
        parentId: voiceIdx > 0 ? this._voiceMessages[voiceIdx - 1].id : baseMessages.at(-1)?.id ?? null,
        message: this._voiceMessages[voiceIdx],
        index: baseMessages.length + voiceIdx
      };
      return;
    }
  }
  getBranches(messageId) {
    if (this._voiceMessages.some((m) => m.id === messageId)) return [];
    return this.repository.getBranches(messageId);
  }
  switchToBranch(branchId) {
    this.repository.switchToBranch(branchId);
    this._notifySubscribers();
  }
  _notifySubscribers() {
    for (const callback of this._subscriptions) callback();
  }
  _notifyEventSubscribers(event, payload) {
    const subscribers = this._eventSubscribers.get(event);
    if (!subscribers) return;
    for (const callback of subscribers) callback(payload);
  }
  subscribe(callback) {
    this._subscriptions.add(callback);
    return () => this._subscriptions.delete(callback);
  }
  submitFeedback({ messageId, type }) {
    const adapter = this.adapters?.feedback;
    if (!adapter) throw new Error("Feedback adapter not configured");
    const { message, parentId } = this.repository.getMessage(messageId);
    adapter.submit({
      message,
      type
    });
    if (message.role === "assistant") {
      const updatedMessage = {
        ...message,
        metadata: {
          ...message.metadata,
          submittedFeedback: { type }
        }
      };
      this.repository.addOrUpdateMessage(parentId, updatedMessage);
    }
    this._notifySubscribers();
  }
  _stopSpeaking;
  speech;
  speak(messageId) {
    const adapter = this.adapters?.speech;
    if (!adapter) throw new Error("Speech adapter not configured");
    const { message } = this.repository.getMessage(messageId);
    this._stopSpeaking?.();
    const utterance = adapter.speak(getThreadMessageText(message));
    const unsub = utterance.subscribe(() => {
      if (utterance.status.type === "ended") {
        this._stopSpeaking = void 0;
        this.speech = void 0;
      } else this.speech = {
        messageId,
        status: utterance.status
      };
      this._notifySubscribers();
    });
    this.speech = {
      messageId,
      status: utterance.status
    };
    this._notifySubscribers();
    this._stopSpeaking = () => {
      utterance.cancel();
      unsub();
      this.speech = void 0;
      this._stopSpeaking = void 0;
    };
  }
  stopSpeaking() {
    if (!this._stopSpeaking) throw new Error("No message is being spoken");
    this._stopSpeaking();
    this._notifySubscribers();
  }
  _voiceSession;
  _voiceUnsubs = [];
  voice;
  _voiceVolume = 0;
  _voiceVolumeSubscribers = /* @__PURE__ */ new Set();
  getVoiceVolume = () => this._voiceVolume;
  subscribeVoiceVolume = (callback) => {
    this._voiceVolumeSubscribers.add(callback);
    return () => this._voiceVolumeSubscribers.delete(callback);
  };
  connectVoice() {
    const adapter = this.adapters?.voice;
    if (!adapter) throw new Error("Voice adapter not configured");
    this.disconnectVoice();
    const session = adapter.connect({});
    this._voiceSession = session;
    const unsubs = [];
    let currentMode = "listening";
    this.voice = {
      status: session.status,
      isMuted: session.isMuted,
      mode: currentMode
    };
    this._voiceVolume = 0;
    this._notifySubscribers();
    unsubs.push(session.onStatusChange((status) => {
      if (status.type === "ended") {
        this._finishVoiceAssistantMessage();
        this._voiceSession = void 0;
        this.voice = void 0;
      } else this.voice = {
        status,
        isMuted: session.isMuted,
        mode: currentMode
      };
      this._notifySubscribers();
    }));
    unsubs.push(session.onModeChange((mode) => {
      currentMode = mode;
      if (this.voice) {
        this.voice = {
          ...this.voice,
          mode
        };
        this._notifySubscribers();
      }
    }));
    unsubs.push(session.onVolumeChange((volume) => {
      this._voiceVolume = volume;
      for (const cb of this._voiceVolumeSubscribers) cb();
    }));
    unsubs.push(session.onTranscript((transcript) => {
      this._handleVoiceTranscript(transcript);
    }));
    this._voiceUnsubs = unsubs;
  }
  _currentAssistantMsg = null;
  _handleVoiceTranscript(transcript) {
    this.ensureInitialized();
    if (transcript.role === "user") {
      this._finishVoiceAssistantMessage();
      this._currentAssistantMsg = null;
      if (transcript.isFinal) {
        this._voiceMessages.push({
          id: generateId(),
          role: "user",
          content: [{
            type: "text",
            text: transcript.text
          }],
          metadata: { custom: {} },
          createdAt: /* @__PURE__ */ new Date(),
          status: {
            type: "complete",
            reason: "unknown"
          },
          attachments: []
        });
        this._markVoiceMessagesDirty();
        this._notifySubscribers();
      }
    } else {
      if (!this._currentAssistantMsg) {
        this._currentAssistantMsg = {
          id: generateId(),
          role: "assistant",
          content: [{
            type: "text",
            text: transcript.text
          }],
          metadata: {
            unstable_state: this.state,
            unstable_annotations: [],
            unstable_data: [],
            steps: [],
            custom: {}
          },
          status: { type: "running" },
          createdAt: /* @__PURE__ */ new Date()
        };
        this._voiceMessages.push(this._currentAssistantMsg);
      } else {
        const idx = this._voiceMessages.indexOf(this._currentAssistantMsg);
        if (idx === -1) return;
        const updated = {
          ...this._currentAssistantMsg,
          content: [{
            type: "text",
            text: transcript.text
          }],
          ...transcript.isFinal ? { status: {
            type: "complete",
            reason: "stop"
          } } : {}
        };
        this._voiceMessages[idx] = updated;
        this._currentAssistantMsg = updated;
      }
      if (transcript.isFinal) this._currentAssistantMsg = null;
      this._markVoiceMessagesDirty();
      this._notifySubscribers();
    }
  }
  _finishVoiceAssistantMessage() {
    const last = this._voiceMessages.at(-1);
    if (last?.role === "assistant" && last.status.type === "running") {
      const idx = this._voiceMessages.length - 1;
      this._voiceMessages[idx] = {
        ...last,
        status: {
          type: "complete",
          reason: "stop"
        }
      };
      this._markVoiceMessagesDirty();
      this._notifySubscribers();
    }
  }
  disconnectVoice() {
    this._finishVoiceAssistantMessage();
    this._currentAssistantMsg = null;
    for (const unsub of this._voiceUnsubs) unsub();
    this._voiceUnsubs = [];
    this._voiceSession?.disconnect();
    this._voiceSession = void 0;
    this.voice = void 0;
    this._voiceVolume = 0;
    for (const cb of this._voiceVolumeSubscribers) cb();
    this._voiceMessages = [];
    this._markVoiceMessagesDirty();
    this._notifySubscribers();
  }
  muteVoice() {
    if (!this._voiceSession) throw new Error("No active voice session");
    this._voiceSession.mute();
    this.voice = {
      ...this.voice,
      isMuted: true
    };
    this._notifySubscribers();
  }
  unmuteVoice() {
    if (!this._voiceSession) throw new Error("No active voice session");
    this._voiceSession.unmute();
    this.voice = {
      ...this.voice,
      isMuted: false
    };
    this._notifySubscribers();
  }
  ensureInitialized() {
    if (!this._isInitialized) {
      this._isInitialized = true;
      this._notifyEventSubscribers("initialize", {});
    }
  }
  export() {
    return this.repository.export();
  }
  import(data) {
    this.ensureInitialized();
    this.repository.clear();
    this.repository.import(data);
    this._notifySubscribers();
  }
  reset(initialMessages) {
    this.import(ExportedMessageRepository.fromArray(initialMessages ?? []));
  }
  _eventSubscribers = /* @__PURE__ */ new Map();
  unstable_on(event, callback) {
    const wrapped = callback;
    if (event === "modelContextUpdate") return this._contextProvider.subscribe?.(() => wrapped({})) ?? (() => {
    });
    let subscribers = this._eventSubscribers.get(event);
    if (!subscribers) {
      subscribers = /* @__PURE__ */ new Set();
      this._eventSubscribers.set(event, subscribers);
    }
    subscribers.add(wrapped);
    if (event === "initialize" && this._isInitialized) queueMicrotask(() => {
      if (subscribers.has(wrapped)) wrapped({});
    });
    return () => {
      this._eventSubscribers.get(event)?.delete(wrapped);
    };
  }
};

// node_modules/@assistant-ui/core/dist/runtimes/external-store/thread-message-converter.js
var ThreadMessageConverter = class {
  cache = /* @__PURE__ */ new WeakMap();
  convertMessages(messages, converter) {
    return messages.map((m, idx) => {
      const newMessage = converter(this.cache.get(m), m, idx);
      this.cache.set(m, newMessage);
      return newMessage;
    });
  }
};

// node_modules/@assistant-ui/core/dist/utils/json/is-json.js
function isRecord(value) {
  return value != null && typeof value === "object" && !Array.isArray(value);
}
function isJSONValue(value, currentDepth = 0) {
  if (currentDepth > 100) return false;
  if (value === null || typeof value === "string" || typeof value === "boolean") return true;
  if (typeof value === "number") return !Number.isNaN(value) && Number.isFinite(value);
  if (Array.isArray(value)) return value.every((item) => isJSONValue(item, currentDepth + 1));
  if (isRecord(value)) return Object.entries(value).every(([key, val]) => typeof key === "string" && isJSONValue(val, currentDepth + 1));
  return false;
}

// node_modules/@assistant-ui/core/dist/utils/json/is-json-equal.js
var MAX_JSON_DEPTH = 100;
var isJSONValueEqualAtDepth = (a, b, currentDepth) => {
  if (a === b) return true;
  if (currentDepth > MAX_JSON_DEPTH) return false;
  if (a == null || b == null) return false;
  if (Array.isArray(a)) {
    if (!Array.isArray(b) || a.length !== b.length) return false;
    return a.every((item, index) => isJSONValueEqualAtDepth(item, b[index], currentDepth + 1));
  }
  if (Array.isArray(b)) return false;
  if (!isRecord(a) || !isRecord(b)) return false;
  const aKeys = Object.keys(a);
  const bKeys = Object.keys(b);
  if (aKeys.length !== bKeys.length) return false;
  return aKeys.every((key) => Object.hasOwn(b, key) && isJSONValueEqualAtDepth(a[key], b[key], currentDepth + 1));
};
var isJSONValueEqual = (a, b) => {
  if (!isJSONValue(a) || !isJSONValue(b)) return false;
  return isJSONValueEqualAtDepth(a, b, 0);
};

// node_modules/@assistant-ui/core/dist/runtimes/tool-invocations/ToolInvocationTracker.js
var isArgsTextComplete = (argsText) => {
  try {
    JSON.parse(argsText);
    return true;
  } catch {
    return false;
  }
};
var parseArgsText = (argsText) => {
  try {
    return JSON.parse(argsText);
  } catch {
    return;
  }
};
var isEquivalentCompleteArgsText = (previous, next) => {
  const previousValue = parseArgsText(previous);
  const nextValue = parseArgsText(next);
  if (previousValue === void 0 || nextValue === void 0) return false;
  return isJSONValueEqual(previousValue, nextValue);
};
var ToolInvocationTracker = class {
  _getTools;
  _callbacks;
  _entries = /* @__PURE__ */ new Map();
  /**
  * Tool call ids whose `execute` should be short-circuited in the wrapper.
  * Populated when an entry is created with a result already attached
  * (history reload, mid-run resume, etc.) — `execute` is suppressed so
  * client-side side effects don't double-run. Membership outlives the
  * entry: `reset()` deliberately does *not* clear this so post-abort
  * cancellation `result` chunks for pre-resolved entries can still be
  * recognized and dropped. Growth is bounded by the number of pre-resolved
  * tool calls observed in the session.
  */
  _skipExecuteStreamIds = /* @__PURE__ */ new Set();
  _humanInput = /* @__PURE__ */ new Map();
  /** In-flight `execute` invocations keyed by tool call id. */
  _executing = /* @__PURE__ */ new Set();
  _settledResolvers = [];
  _statuses = /* @__PURE__ */ new Map();
  _ac = new AbortController();
  _pendingRestore = true;
  /** Cached last snapshot, used to skip processing on identical re-renders. */
  _lastSnapshot = null;
  _isRunning = false;
  _controller;
  /**
  * Set when the assistant-stream pipeline has died (errored out via
  * `.pipeTo(...).catch(...)`). The next `setState` re-initializes the
  * pipeline and demotes all active entries to restored so they survive
  * across the restart without re-firing `streamCall` (preserves the
  * "exactly once" contract). Capped at a single auto-restart per session
  * — repeated failures keep the tracker dead with a more visible error.
  */
  _pipelineDead = false;
  _pipelineRestartUsed = false;
  constructor(getTools, callbacks) {
    this._getTools = getTools;
    this._callbacks = callbacks;
    this._initPipeline();
  }
  /**
  * Build the assistant-stream pipeline. Called once from the constructor
  * and at most once again if `_pipelineDead` is set (see F.4 in
  * EDGE_CASES.md).
  */
  _initPipeline() {
    const [stream, controller] = createAssistantStreamController();
    this._controller = controller;
    const transform = toolResultStream(() => this._getWrappedTools(), () => this._ac.signal, (toolCallId, payload) => this._onHumanInput(toolCallId, payload), {
      onExecutionStart: (id) => this._onExecutionStart(id),
      onExecutionEnd: (id) => this._onExecutionEnd(id)
    });
    stream.pipeThrough(transform).pipeThrough(new AssistantMetaTransformStream()).pipeTo(new WritableStream({ write: (chunk) => {
      try {
        if (chunk.type !== "result") return;
        this._handleResultChunk(chunk);
      } catch (err) {
        console.error("[ToolInvocationTracker] result chunk handling failed", err);
      }
    } })).catch((err) => {
      console.error("[ToolInvocationTracker] stream pipeline failed; will attempt single restart on next setState", err);
      this._pipelineDead = true;
    });
  }
  /**
  * Feed the next observed snapshot into the tracker. Called from the host
  * runtime whenever its message list / running state changes.
  */
  setState(snapshot) {
    try {
      if (this._pipelineDead) {
        if (this._pipelineRestartUsed) return;
        this._pipelineRestartUsed = true;
        this._pipelineDead = false;
        this._demoteEntriesToRestored();
        this._executing.clear();
        this._ac = new AbortController();
        this._initPipeline();
      }
      if (this._lastSnapshot && this._lastSnapshot.messages === snapshot.messages && this._lastSnapshot.isRunning === snapshot.isRunning && this._lastSnapshot.isLoading === snapshot.isLoading) return;
      if (snapshot.isLoading === true) this._pendingRestore = true;
      const previousIsRunning = this._isRunning;
      this._isRunning = snapshot.isRunning;
      try {
        this._processMessages(snapshot.messages);
      } catch (err) {
        this._isRunning = previousIsRunning;
        throw err;
      }
      this._lastSnapshot = snapshot;
      this._pendingRestore = false;
    } catch (err) {
      console.error("[ToolInvocationTracker] setState failed; snapshot dropped", err);
    }
  }
  /**
  * Reset the tracker so the next observed snapshot is treated as historical.
  * Clears entries and aborts any in-flight executions. Used by callers like
  * `importExternalState` to mark a freshly loaded state as restored.
  */
  reset() {
    try {
      this._pendingRestore = true;
      this._entries.clear();
      this._lastSnapshot = null;
      this.abort().finally(() => {
        this._executing.clear();
      });
    } catch (err) {
      console.error("[ToolInvocationTracker] reset failed", err);
    }
  }
  /**
  * Abort any in-flight `execute()` invocations. Resolves once all of them
  * have settled (or immediately if none are running).
  */
  abort() {
    try {
      this._humanInput.forEach(({ reject }) => {
        try {
          reject(new Error("Tool execution aborted"));
        } catch {
        }
      });
      this._humanInput.clear();
      this._ac.abort();
      this._ac = new AbortController();
      if (this._executing.size === 0) return Promise.resolve();
      return new Promise((resolve) => {
        this._settledResolvers.push(resolve);
      });
    } catch (err) {
      console.error("[ToolInvocationTracker] abort failed", err);
      return Promise.resolve();
    }
  }
  /**
  * Resolve a pending human-input request for the given tool call. Returns
  * `true` if a pending request was resumed, `false` if the tracker has no
  * outstanding request for that id (the caller should fall back to its own
  * dispatch path).
  */
  resume(toolCallId, payload) {
    try {
      const handlers = this._humanInput.get(toolCallId);
      if (!handlers) return false;
      this._humanInput.delete(toolCallId);
      this._setStatus(toolCallId, { type: "executing" });
      handlers.resolve(payload);
      return true;
    } catch (err) {
      console.error("[ToolInvocationTracker] resume failed", err);
      return false;
    }
  }
  /**
  * Returns the current tool execution status map. The returned `Map` is
  * the tracker's internal store — do not mutate it. Treat the reference
  * as a snapshot that may be replaced wholesale on the next status
  * transition.
  */
  getStatuses() {
    return this._statuses;
  }
  _getWrappedTools() {
    const tools = this._getTools();
    if (!tools) return void 0;
    return Object.fromEntries(Object.entries(tools).map(([name, tool]) => {
      const execute = tool.execute;
      if (execute === void 0) return [name, tool];
      return [name, {
        ...tool,
        execute: (...[args, context2]) => {
          if (this._skipExecuteStreamIds.has(context2.toolCallId)) return new Promise(() => {
          });
          return execute(args, context2);
        }
      }];
    }));
  }
  _onHumanInput(toolCallId, payload) {
    return new Promise((resolve, reject) => {
      const previous = this._humanInput.get(toolCallId);
      if (previous) try {
        previous.reject(new Error("Human input request was superseded by a new request"));
      } catch {
      }
      this._humanInput.set(toolCallId, {
        resolve,
        reject
      });
      this._setStatus(toolCallId, {
        type: "interrupt",
        payload: {
          type: "human",
          payload
        }
      });
    });
  }
  _onExecutionStart(toolCallId) {
    if (this._skipExecuteStreamIds.has(toolCallId)) return;
    this._executing.add(toolCallId);
    this._setStatus(toolCallId, { type: "executing" });
  }
  _onExecutionEnd(toolCallId) {
    if (!this._executing.delete(toolCallId)) return;
    this._deleteStatus(toolCallId);
    if (this._executing.size === 0) this._settledResolvers.splice(0).forEach((resolve) => {
      try {
        resolve();
      } catch {
      }
    });
  }
  _handleResultChunk(chunk) {
    const toolCallId = chunk.meta.toolCallId;
    const entry = this._entries.get(toolCallId);
    if (!entry && this._skipExecuteStreamIds.has(toolCallId)) return;
    if (entry?.hasResult) return;
    this._invokeOnResult({
      type: "add-tool-result",
      toolCallId,
      toolName: chunk.meta.toolName,
      result: chunk.result,
      isError: chunk.isError,
      ...chunk.artifact !== void 0 && { artifact: chunk.artifact },
      ...chunk.modelContent !== void 0 && { modelContent: chunk.modelContent }
    });
  }
  _invokeOnResult(command) {
    try {
      this._callbacks.onResult(command);
    } catch (err) {
      console.error("[ToolInvocationTracker] onResult callback threw; result dropped", err);
    }
  }
  _invokeOnStatusesChange() {
    try {
      this._callbacks.onStatusesChange(this._statuses);
    } catch (err) {
      console.error("[ToolInvocationTracker] onStatusesChange callback threw; status change not propagated", err);
    }
  }
  _setStatus(toolCallId, status) {
    const next = new Map(this._statuses);
    next.set(toolCallId, status);
    this._statuses = next;
    this._invokeOnStatusesChange();
  }
  _deleteStatus(toolCallId) {
    if (!this._statuses.has(toolCallId)) return;
    const next = new Map(this._statuses);
    next.delete(toolCallId);
    this._statuses = next;
    this._invokeOnStatusesChange();
  }
  _hasExecutableTool(toolName) {
    const tool = this._getTools()?.[toolName];
    return tool?.execute !== void 0 || tool?.streamCall !== void 0;
  }
  _shouldCloseArgsStream({ toolName, argsText, hasResult }) {
    if (hasResult) return true;
    if (!this._hasExecutableTool(toolName)) return !this._isRunning && isArgsTextComplete(argsText);
    return isArgsTextComplete(argsText);
  }
  _startActiveEntry(toolCallId, toolName, skipExecute) {
    const toolCallController = this._controller.addToolCallPart({
      toolName,
      toolCallId
    });
    if (skipExecute) this._skipExecuteStreamIds.add(toolCallId);
    const entry = {
      toolName,
      controller: toolCallController,
      argsText: "",
      hasResult: false,
      argsComplete: false
    };
    this._entries.set(toolCallId, entry);
    return entry;
  }
  /**
  * Demote every active entry back to the restored phase. Used by the
  * pipeline-restart path so that, after a fresh pipeline is built, the
  * next observed snapshot does not re-fire `streamCall` for tool calls
  * that already fired pre-death. Args / hasResult tracking is preserved
  * so signature comparisons still work.
  */
  _demoteEntriesToRestored() {
    for (const [toolCallId, entry] of this._entries) {
      if (!entry.controller) continue;
      this._entries.set(toolCallId, {
        toolName: entry.toolName,
        argsText: entry.argsText,
        hasResult: entry.hasResult
      });
    }
  }
  _processArgsText(entry, content) {
    if (!entry.controller) return;
    const hasResult = content.result !== void 0;
    if (content.argsText !== entry.argsText) {
      let shouldWriteArgsText = true;
      if (entry.argsComplete) if (isEquivalentCompleteArgsText(entry.argsText, content.argsText)) {
        entry.argsText = content.argsText;
        shouldWriteArgsText = false;
      } else {
        if (true) console.warn("[ToolInvocationTracker] argsText changed after first completion; not re-firing streamCall (see EDGE_CASES.md A.4)", {
          previous: entry.argsText,
          next: content.argsText,
          toolCallId: content.toolCallId
        });
        shouldWriteArgsText = false;
      }
      else if (!content.argsText.startsWith(entry.argsText)) if (isArgsTextComplete(entry.argsText) && isArgsTextComplete(content.argsText) && isEquivalentCompleteArgsText(entry.argsText, content.argsText)) {
        const shouldClose = this._shouldCloseArgsStream({
          toolName: content.toolName,
          argsText: content.argsText,
          hasResult
        });
        if (shouldClose) entry.controller.argsText.close();
        entry.argsText = content.argsText;
        entry.argsComplete = shouldClose;
        shouldWriteArgsText = false;
      } else {
        if (true) console.warn("[ToolInvocationTracker] argsText regressed mid-stream; not restarting (see EDGE_CASES.md A.2)", {
          previous: entry.argsText,
          next: content.argsText,
          toolCallId: content.toolCallId
        });
        shouldWriteArgsText = false;
      }
      if (shouldWriteArgsText && entry.controller) {
        const delta = content.argsText.slice(entry.argsText.length);
        entry.controller.argsText.append(delta);
        const shouldClose = this._shouldCloseArgsStream({
          toolName: content.toolName,
          argsText: content.argsText,
          hasResult
        });
        if (shouldClose) entry.controller.argsText.close();
        entry.argsText = content.argsText;
        entry.argsComplete = shouldClose;
      }
    }
    if (!entry.argsComplete && entry.controller) {
      if (this._shouldCloseArgsStream({
        toolName: content.toolName,
        argsText: content.argsText,
        hasResult
      })) {
        entry.controller.argsText.close();
        entry.argsText = content.argsText;
        entry.argsComplete = true;
      }
    }
  }
  _processMessages(messages) {
    const isRestore = this._pendingRestore;
    for (const message of messages) {
      if (!message || !Array.isArray(message.content)) continue;
      for (const content of message.content) {
        if (!content || content.type !== "tool-call") continue;
        const existing = this._entries.get(content.toolCallId);
        if (isRestore) {
          if (!existing?.controller) this._entries.set(content.toolCallId, {
            toolName: content.toolName,
            argsText: content.argsText,
            hasResult: content.result !== void 0
          });
          if (content.messages) this._processMessages(content.messages);
          continue;
        }
        let entry = existing;
        if (entry && !entry.controller) {
          if (!(content.argsText !== entry.argsText || content.result !== void 0 !== entry.hasResult)) {
            if (content.messages) this._processMessages(content.messages);
            continue;
          }
          this._entries.delete(content.toolCallId);
          entry = void 0;
        }
        if (!entry) entry = this._startActiveEntry(content.toolCallId, content.toolName, content.result !== void 0);
        this._processArgsText(entry, content);
        if (content.result !== void 0 && !entry.hasResult) {
          const { controller: activeController } = entry;
          if (!activeController) continue;
          entry.hasResult = true;
          entry.argsComplete = true;
          activeController.setResponse(new ToolResponse({
            result: content.result,
            artifact: content.artifact,
            isError: content.isError,
            ...content.modelContent !== void 0 ? { modelContent: content.modelContent } : {}
          }));
          activeController.close();
        }
        if (content.messages) this._processMessages(content.messages);
      }
    }
  }
};

// node_modules/@assistant-ui/core/dist/runtimes/external-store/external-store-thread-runtime-core.js
var EMPTY_ARRAY3 = Object.freeze([]);
var shallowEqual2 = (a, b) => {
  const aKeys = Object.keys(a);
  if (aKeys.length !== Object.keys(b).length) return false;
  for (const key of aKeys) if (a[key] !== b[key]) return false;
  return true;
};
var hasUpcomingMessage = (isRunning, messages) => {
  return isRunning && messages[messages.length - 1]?.role !== "assistant";
};
var ExternalStoreThreadRuntimeCore = class extends BaseThreadRuntimeCore {
  _capabilities = {
    switchToBranch: false,
    switchBranchDuringRun: false,
    edit: false,
    reload: false,
    cancel: false,
    unstable_copy: false,
    speech: false,
    dictation: false,
    voice: false,
    attachments: false,
    feedback: false,
    queue: false
  };
  get capabilities() {
    return this._capabilities;
  }
  _messages;
  isDisabled;
  isSendDisabled;
  get isLoading() {
    return this._store.isLoading ?? false;
  }
  get isRunning() {
    return this._store.isRunning;
  }
  _getBaseMessages() {
    return this._messages;
  }
  get state() {
    return this._store.state ?? super.state;
  }
  get adapters() {
    return this._store.adapters;
  }
  suggestions = [];
  extras = void 0;
  _converter = new ThreadMessageConverter();
  _store;
  /**
  * Client-side tool-invocations pipeline. Constructed lazily on first
  * snapshot — only when `adapter.unstable_enableToolInvocations === true`.
  */
  _toolInvocations = null;
  beginEdit(messageId) {
    if (!this._store.onEdit) throw new Error("Runtime does not support editing.");
    super.beginEdit(messageId);
  }
  constructor(contextProvider, store) {
    super(contextProvider);
    this.__internal_setAdapter(store);
  }
  __internal_setAdapter(store) {
    if (this._store === store) return;
    const isRunning = store.isRunning ?? false;
    this.isDisabled = store.isDisabled ?? false;
    this.isSendDisabled = store.isSendDisabled ?? false;
    const oldStore = this._store;
    this._store = store;
    if (this.extras !== store.extras) this.extras = store.extras;
    const newSuggestions = store.suggestions ?? EMPTY_ARRAY3;
    if (!shallowEqual2(this.suggestions, newSuggestions)) this.suggestions = newSuggestions;
    const newCapabilities = {
      switchToBranch: this._store.setMessages !== void 0,
      switchBranchDuringRun: false,
      edit: this._store.onEdit !== void 0,
      reload: this._store.onReload !== void 0,
      cancel: this._store.onCancel !== void 0,
      speech: this._store.adapters?.speech !== void 0,
      dictation: this._store.adapters?.dictation !== void 0,
      voice: this._store.adapters?.voice !== void 0,
      unstable_copy: this._store.unstable_capabilities?.copy !== false,
      attachments: !!this._store.adapters?.attachments,
      feedback: !!this._store.adapters?.feedback,
      queue: false
    };
    if (!shallowEqual2(this._capabilities, newCapabilities)) this._capabilities = newCapabilities;
    let messages;
    if (store.messageRepository) {
      if (oldStore && oldStore.isRunning === store.isRunning && oldStore.messageRepository === store.messageRepository) {
        this._notifySubscribers();
        return;
      }
      this.repository.clear();
      this.repository.import(store.messageRepository);
      messages = this.repository.getMessages();
    } else if (store.messages) {
      if (oldStore) {
        if (oldStore.convertMessage !== store.convertMessage) this._converter = new ThreadMessageConverter();
        else if (oldStore.isRunning === store.isRunning && oldStore.messages === store.messages) {
          this._notifySubscribers();
          return;
        }
      }
      messages = !store.convertMessage ? store.messages : this._converter.convertMessages(store.messages, (cache, m, idx) => {
        if (!store.convertMessage) return m;
        const autoStatus = getAutoStatus(idx === (store.messages?.length ?? 0) - 1, isRunning, false, false, void 0);
        if (cache && (cache.role !== "assistant" || !isAutoStatus(cache.status) || cache.status === autoStatus)) return cache;
        const newMessage = fromThreadMessageLike(store.convertMessage(m, idx), idx.toString(), autoStatus);
        bindExternalStoreMessage(newMessage, m);
        return newMessage;
      });
      for (let i = 0; i < messages.length; i++) {
        const message = messages[i];
        const parent = messages[i - 1];
        this.repository.addOrUpdateMessage(parent?.id ?? null, message);
      }
    } else throw new Error("ExternalStoreAdapter must provide either 'messages' or 'messageRepository'");
    if (messages.length > 0) this.ensureInitialized();
    if ((oldStore?.isRunning ?? false) !== (store.isRunning ?? false)) if (store.isRunning) this._notifyEventSubscribers("runStart", {});
    else this._notifyEventSubscribers("runEnd", {});
    let optimisticId = null;
    if (hasUpcomingMessage(isRunning, messages)) {
      optimisticId = generateId();
      this.repository.addOrUpdateMessage(messages.at(-1)?.id ?? null, fromThreadMessageLike({
        role: "assistant",
        content: [],
        metadata: { isOptimistic: true }
      }, optimisticId, { type: "running" }));
    }
    this.repository.resetHead(optimisticId ?? messages.at(-1)?.id ?? null);
    this._messages = this.repository.getMessages();
    this._driveToolInvocations();
    this._notifySubscribers();
  }
  /**
  * Feed the current message snapshot into the tool-invocations tracker.
  * Opt-in via `adapter.unstable_enableToolInvocations: true`. The tracker
  * itself is fail-silent — see ToolInvocationTracker for the
  * state-transition contract.
  */
  _driveToolInvocations() {
    if (!this._store.unstable_enableToolInvocations) {
      if (this._toolInvocations) {
        this._toolInvocations.reset();
        this._toolInvocations = null;
        this._store.setToolStatuses?.({});
      }
      return;
    }
    if (!this._toolInvocations) this._toolInvocations = new ToolInvocationTracker(() => this.getModelContext().tools, {
      onResult: (command) => {
        try {
          const messageId = this._findMessageIdForToolCall(command.toolCallId);
          if (messageId === void 0) return;
          this._store.onAddToolResult?.({
            messageId,
            toolCallId: command.toolCallId,
            toolName: command.toolName,
            result: command.result,
            isError: command.isError,
            ...command.artifact !== void 0 && { artifact: command.artifact },
            ...command.modelContent !== void 0 && { modelContent: command.modelContent }
          });
        } catch (err) {
          console.error("[ExternalStoreThreadRuntimeCore] onAddToolResult dispatch failed", err);
        }
      },
      onStatusesChange: (statuses) => {
        this._store.setToolStatuses?.(Object.fromEntries(statuses));
      }
    });
    this._toolInvocations.setState({
      messages: this._messages,
      isRunning: this._store.isRunning ?? false,
      ...this._store.isLoading !== void 0 && { isLoading: this._store.isLoading }
    });
  }
  /**
  * Lookup table from `toolCallId` to the owning assistant message's `id`,
  * rebuilt lazily when `_messages` changes (see `_messagesForToolCallIndex`).
  */
  _toolCallToMessageId = /* @__PURE__ */ new Map();
  _messagesForToolCallIndex = null;
  /**
  * Look up the assistant message that owns a tool-call part. Lazily builds
  * (and caches) a `toolCallId → messageId` map keyed off the current
  * `_messages` reference, so onResult dispatches stay O(1) instead of
  * walking the full thread on every result.
  */
  _findMessageIdForToolCall(toolCallId) {
    if (this._messagesForToolCallIndex !== this._messages) {
      this._toolCallToMessageId.clear();
      const visit = (messages) => {
        for (const message of messages) {
          if (!Array.isArray(message.content)) continue;
          for (const part of message.content) {
            if (!part || part.type !== "tool-call") continue;
            this._toolCallToMessageId.set(part.toolCallId, message.id);
            if (part.messages) visit(part.messages);
          }
        }
      };
      visit(this._messages);
      this._messagesForToolCallIndex = this._messages;
    }
    return this._toolCallToMessageId.get(toolCallId);
  }
  switchToBranch(branchId) {
    if (!this._store.setMessages) throw new Error("Runtime does not support switching branches.");
    if (this._store.isRunning) return;
    this.repository.switchToBranch(branchId);
    this.updateMessages(this.repository.getMessages());
  }
  async append(message) {
    if (message.startRun ?? message.role === "user") await this._toolInvocations?.abort();
    if (message.parentId !== (this.messages.at(-1)?.id ?? null)) {
      if (!this._store.onEdit) throw new Error("Runtime does not support editing messages.");
      await this._store.onEdit(message);
    } else await this._store.onNew(message);
  }
  async startRun(config) {
    if (!this._store.onReload) throw new Error("Runtime does not support reloading messages.");
    await this._toolInvocations?.abort();
    await this._store.onReload(config.parentId, config);
  }
  async resumeRun(config) {
    if (!this._store.onResume) throw new Error("Runtime does not support resuming runs.");
    await this._store.onResume(config);
  }
  exportExternalState() {
    if (!this._store.onExportExternalState) throw new Error("Runtime does not support exporting external states.");
    return this._store.onExportExternalState();
  }
  importExternalState(state) {
    if (!this._store.onLoadExternalState) throw new Error("Runtime does not support importing external states.");
    if (this._toolInvocations) {
      this._toolInvocations.reset();
      this._store.setToolStatuses?.({});
    }
    this._store.onLoadExternalState(state);
  }
  cancelRun() {
    if (!this._store.onCancel) throw new Error("Runtime does not support cancelling runs.");
    this._toolInvocations?.abort();
    this._store.onCancel();
    const head = this.repository.getMessages().at(-1);
    if (head && head.metadata.isOptimistic && head.content.length === 0) this.repository.deleteMessage(head.id);
    let messages = this.repository.getMessages();
    const previousMessage = messages[messages.length - 1];
    if (previousMessage?.role === "user" && previousMessage.id === messages.at(-1)?.id) {
      this.repository.deleteMessage(previousMessage.id);
      if (!this.composer.text.trim()) this.composer.setText(getThreadMessageText(previousMessage));
      messages = this.repository.getMessages();
    } else this._notifySubscribers();
    setTimeout(() => {
      this.updateMessages(messages);
    }, 0);
  }
  addToolResult(options) {
    if (!this._store.onAddToolResult) throw new Error("Runtime does not support tool results.");
    this._store.onAddToolResult?.(options);
  }
  resumeToolCall(options) {
    if (this._toolInvocations?.resume(options.toolCallId, options.payload) ?? false) return;
    if (this._store.onResumeToolCall) {
      this._store.onResumeToolCall(options);
      return;
    }
    throw new Error(`Tool call ${options.toolCallId} is not waiting for resume.`);
  }
  respondToToolApproval(options) {
    if (!this._store.onRespondToToolApproval) throw new Error("Runtime does not support tool approvals.");
    this._store.onRespondToToolApproval(options);
  }
  reset(initialMessages) {
    const repo = new MessageRepository();
    repo.import(ExportedMessageRepository.fromArray(initialMessages ?? []));
    this.updateMessages(repo.getMessages());
  }
  import(data) {
    super.import(data);
    if (this._store.onImport) this._store.onImport(this.repository.getMessages());
  }
  updateMessages = (messages) => {
    if (this._store.convertMessage !== void 0) this._store.setMessages?.(messages.flatMap(getExternalStoreMessages));
    else this._store.setMessages?.(messages);
  };
};

// node_modules/@assistant-ui/core/dist/runtimes/external-store/external-store-runtime-core.js
var getThreadListAdapter = (store) => {
  return store.adapters?.threadList ?? {};
};
var ExternalStoreRuntimeCore = class extends BaseAssistantRuntimeCore {
  threads;
  constructor(adapter) {
    super();
    this.threads = new ExternalStoreThreadListRuntimeCore(getThreadListAdapter(adapter), () => new ExternalStoreThreadRuntimeCore(this._contextProvider, adapter));
  }
  setAdapter(adapter) {
    this.threads.__internal_setAdapter(getThreadListAdapter(adapter));
    this.threads.getMainThreadRuntimeCore().__internal_setAdapter(adapter);
  }
};

// node_modules/@assistant-ui/core/dist/react/runtimes/useExternalStoreRuntime.js
var import_react21 = __toESM(require_react(), 1);
var useExternalStoreRuntime = (store) => {
  const [runtime] = (0, import_react21.useState)(() => new ExternalStoreRuntimeCore(store));
  (0, import_react21.useEffect)(() => {
    runtime.setAdapter(store);
  });
  const { modelContext } = useRuntimeAdapters() ?? {};
  (0, import_react21.useEffect)(() => {
    if (!modelContext) return void 0;
    return runtime.registerModelContextProvider(modelContext);
  }, [modelContext, runtime]);
  return (0, import_react21.useMemo)(() => new AssistantRuntimeImpl(runtime), [runtime]);
};

// node_modules/@assistant-ui/core/dist/react/runtimes/useExternalStoreSharedOptions.js
var import_react22 = __toESM(require_react(), 1);
var useExternalStoreSharedOptions = (options) => {
  const { isDisabled, isSendDisabled, unstable_capabilities, suggestions } = options;
  return (0, import_react22.useMemo)(() => ({
    isDisabled,
    isSendDisabled,
    unstable_capabilities,
    suggestions
  }), [
    isDisabled,
    isSendDisabled,
    unstable_capabilities,
    suggestions
  ]);
};

// node_modules/@assistant-ui/core/dist/react/runtimes/external-message-converter.js
var import_react23 = __toESM(require_react(), 1);
var isPendingToolCall = (c) => c.type === "tool-call" && c.result === void 0;
var isInterruptedToolCall = (c) => {
  if (c.type !== "tool-call" || c.result !== void 0) return false;
  return c.interrupt != null || c.approval != null && c.approval.approved === void 0;
};
var mergeInnerMessages = (existing, incoming) => ({ [symbolInnerMessage]: [...existing[symbolInnerMessage] ?? [], ...incoming[symbolInnerMessage] ?? []] });
var joinExternalMessages = (messages) => {
  const assistantMessage = {
    role: "assistant",
    content: []
  };
  for (const output of messages) if (output.role === "tool") {
    const toolCallIdx = assistantMessage.content.findIndex((c) => c.type === "tool-call" && c.toolCallId === output.toolCallId);
    if (toolCallIdx !== -1) {
      const toolCall = assistantMessage.content[toolCallIdx];
      if (output.toolName !== void 0) {
        if (toolCall.toolName !== output.toolName) throw new Error(`Tool call name ${output.toolCallId} ${output.toolName} does not match existing tool call ${toolCall.toolName}`);
      }
      assistantMessage.content[toolCallIdx] = {
        ...toolCall,
        [symbolInnerMessage]: [...toolCall[symbolInnerMessage] ?? [], output],
        result: output.result,
        artifact: output.artifact,
        isError: output.isError,
        messages: output.messages
      };
    }
  } else {
    const role = output.role;
    const content = (typeof output.content === "string" ? [{
      type: "text",
      text: output.content
    }] : output.content).map((c) => ({
      ...c,
      [symbolInnerMessage]: [output]
    }));
    switch (role) {
      case "system":
      case "user":
        return {
          ...output,
          content
        };
      case "assistant":
        if (assistantMessage.content.length === 0) {
          assistantMessage.id = output.id;
          assistantMessage.createdAt ??= output.createdAt;
          assistantMessage.status ??= output.status;
          if (output.attachments) assistantMessage.attachments = [...assistantMessage.attachments ?? [], ...output.attachments];
          if (output.metadata) {
            assistantMessage.metadata ??= {};
            if (output.metadata.unstable_state) assistantMessage.metadata.unstable_state = output.metadata.unstable_state;
            if (output.metadata.unstable_annotations) assistantMessage.metadata.unstable_annotations = [...assistantMessage.metadata.unstable_annotations ?? [], ...output.metadata.unstable_annotations];
            if (output.metadata.unstable_data) assistantMessage.metadata.unstable_data = [...assistantMessage.metadata.unstable_data ?? [], ...output.metadata.unstable_data];
            if (output.metadata.steps) assistantMessage.metadata.steps = [...assistantMessage.metadata.steps ?? [], ...output.metadata.steps];
            if (output.metadata.custom) assistantMessage.metadata.custom = {
              ...assistantMessage.metadata.custom ?? {},
              ...output.metadata.custom
            };
            if (output.metadata.timing) assistantMessage.metadata.timing = output.metadata.timing;
            if (output.metadata.submittedFeedback) assistantMessage.metadata.submittedFeedback = output.metadata.submittedFeedback;
            if (output.metadata.isOptimistic) assistantMessage.metadata.isOptimistic = true;
          }
        }
        for (const part of content) {
          if (part.type === "tool-call") {
            const existingIdx = assistantMessage.content.findIndex((c) => c.type === "tool-call" && c.toolCallId === part.toolCallId);
            if (existingIdx !== -1) {
              const existing = assistantMessage.content[existingIdx];
              assistantMessage.content[existingIdx] = {
                ...existing,
                ...part,
                ...mergeInnerMessages(existing, part)
              };
              continue;
            }
          }
          if (part.type === "reasoning" && "parentId" in part && part.parentId) {
            const existingIdx = assistantMessage.content.findIndex((c) => c.type === "reasoning" && "parentId" in c && c.parentId === part.parentId);
            if (existingIdx !== -1) {
              const existing = assistantMessage.content[existingIdx];
              assistantMessage.content[existingIdx] = {
                ...existing,
                text: `${existing.text}

${part.text}`,
                ...mergeInnerMessages(existing, part)
              };
              continue;
            }
          }
          assistantMessage.content.push(part);
        }
        break;
      default:
        throw new Error(`Unknown message role: ${role}`);
    }
  }
  return assistantMessage;
};
var chunkExternalMessages = (callbackResults, joinStrategy) => {
  const results = [];
  let isAssistant = false;
  let pendingNone = false;
  let inputs = [];
  let outputs = [];
  const flush = () => {
    if (outputs.length) results.push({
      inputs,
      outputs
    });
    inputs = [];
    outputs = [];
    isAssistant = false;
    pendingNone = false;
  };
  for (const callbackResult of callbackResults) for (const output of callbackResult.outputs) {
    if (pendingNone && output.role !== "tool" || !isAssistant || output.role === "user" || output.role === "system") flush();
    isAssistant = output.role === "assistant" || output.role === "tool";
    if (inputs.at(-1) !== callbackResult.input) inputs.push(callbackResult.input);
    outputs.push(output);
    if (output.role === "assistant" && (output.convertConfig?.joinStrategy === "none" || joinStrategy === "none")) pendingNone = true;
  }
  flush();
  return results;
};
function createErrorAssistantMessage(error) {
  const msg = {
    id: generateErrorMessageId(),
    role: "assistant",
    content: [],
    status: {
      type: "incomplete",
      reason: "error",
      error
    },
    createdAt: /* @__PURE__ */ new Date(),
    metadata: {
      unstable_state: null,
      unstable_annotations: [],
      unstable_data: [],
      custom: {},
      steps: []
    }
  };
  bindExternalStoreMessage(msg, []);
  return msg;
}
var convertExternalMessages = (messages, callback, isRunning, metadata) => {
  const callbackResults = [];
  for (const message of messages) {
    const output = callback(message, metadata);
    const result2 = {
      input: message,
      outputs: Array.isArray(output) ? output : [output]
    };
    callbackResults.push(result2);
  }
  const chunks = chunkExternalMessages(callbackResults);
  const result = chunks.map((message, idx) => {
    const isLast = idx === chunks.length - 1;
    const joined = joinExternalMessages(message.outputs);
    const autoStatus = getAutoStatus(isLast, isRunning, typeof joined.content === "object" && joined.content.some(isInterruptedToolCall), typeof joined.content === "object" && joined.content.some(isPendingToolCall), isLast ? metadata.error : void 0);
    const newMessage = fromThreadMessageLike(joined, idx.toString(), autoStatus);
    bindExternalStoreMessage(newMessage, message.inputs);
    return newMessage;
  });
  if (metadata.error) {
    const lastMessage = result.at(-1);
    if (!lastMessage || lastMessage.role !== "assistant") result.push(createErrorAssistantMessage(metadata.error));
  }
  return result;
};
var useExternalMessageConverter = ({ callback, messages, isRunning, joinStrategy, metadata }) => {
  const state = (0, import_react23.useMemo)(() => ({
    metadata: metadata ?? {},
    callback,
    callbackCache: /* @__PURE__ */ new WeakMap(),
    chunkCache: /* @__PURE__ */ new WeakMap(),
    converterCache: new ThreadMessageConverter()
  }), [callback, metadata]);
  return (0, import_react23.useMemo)(() => {
    const callbackResults = [];
    for (const message of messages) {
      let result = state.callbackCache.get(message);
      if (!result) {
        const output = state.callback(message, state.metadata);
        result = {
          input: message,
          outputs: Array.isArray(output) ? output : [output]
        };
        state.callbackCache.set(message, result);
      }
      callbackResults.push(result);
    }
    const chunks = chunkExternalMessages(callbackResults, joinStrategy).map((m) => {
      const key = m.outputs[0];
      if (!key) return m;
      const cached = state.chunkCache.get(key);
      if (cached && shallowArrayEqual(cached.outputs, m.outputs)) return cached;
      state.chunkCache.set(key, m);
      return m;
    });
    const threadMessages = state.converterCache.convertMessages(chunks, (cache, message, idx) => {
      const isLast = idx === chunks.length - 1;
      const joined = joinExternalMessages(message.outputs);
      const autoStatus = getAutoStatus(isLast, isRunning, typeof joined.content === "object" && joined.content.some(isInterruptedToolCall), typeof joined.content === "object" && joined.content.some(isPendingToolCall), isLast ? state.metadata.error : void 0);
      if (cache && (cache.role !== "assistant" || !isAutoStatus(cache.status) || cache.status === autoStatus)) {
        if (shallowArrayEqual(getExternalStoreMessages(cache), message.inputs)) return cache;
      }
      const newMessage = fromThreadMessageLike(joined, idx.toString(), autoStatus);
      bindExternalStoreMessage(newMessage, message.inputs);
      return newMessage;
    });
    bindExternalStoreMessage(threadMessages, messages);
    if (state.metadata.error) {
      const lastMessage = threadMessages.at(-1);
      if (!lastMessage || lastMessage.role !== "assistant") threadMessages.push(createErrorAssistantMessage(state.metadata.error));
    }
    return threadMessages;
  }, [
    state,
    messages,
    isRunning,
    joinStrategy
  ]);
};
var shallowArrayEqual = (a, b) => {
  if (a.length !== b.length) return false;
  for (let i = 0; i < a.length; i++) if (a[i] !== b[i]) return false;
  return true;
};

// node_modules/@assistant-ui/core/dist/react/runtimes/createMessageConverter.js
var createMessageConverter = (callback) => {
  const result = {
    useThreadMessages: ({ messages, isRunning, joinStrategy, metadata }) => {
      return useExternalMessageConverter({
        callback,
        messages,
        isRunning,
        joinStrategy,
        metadata
      });
    },
    toThreadMessages: (messages, isRunning = false, metadata = {}) => {
      return convertExternalMessages(messages, callback, isRunning, metadata);
    },
    toOriginalMessages: (input) => {
      const messages = getExternalStoreMessages(input);
      if (messages.length === 0) throw new Error("No original messages found");
      return messages;
    },
    toOriginalMessage: (input) => {
      return result.toOriginalMessages(input)[0];
    },
    useOriginalMessage: () => {
      return result.useOriginalMessages()[0];
    },
    useOriginalMessages: () => {
      const aui = useAui();
      const partMessages = useAuiState((s) => {
        if (aui.part.source) return getExternalStoreMessages(s.part);
      });
      const messageMessages = useAuiState((s) => getExternalStoreMessages(s.message));
      const messages = partMessages ?? messageMessages;
      if (messages.length === 0) throw new Error("No original messages found");
      return messages;
    }
  };
  return result;
};

// node_modules/@assistant-ui/core/dist/runtimes/remote-thread-list/optimistic-state.js
var pipeTransforms = (initialState, extraParam, transforms) => {
  return transforms.reduce((state, transform) => {
    return transform?.(state, extraParam) ?? state;
  }, initialState);
};
var OptimisticState = class extends BaseSubscribable {
  _pendingTransforms = [];
  /**
  * `optimistic` callbacks from transforms that have already resolved.
  * Re-applied after every `then` callback so that a wholesale state
  * replacement (e.g. list()) cannot erase earlier completed effects
  * (e.g. delete). Cleared when no pending transforms remain.
  *
  * Correctness requirement: `optimistic` callbacks must be idempotent.
  */
  _completedOptimistics = [];
  _baseValue;
  _cachedValue;
  constructor(initialState) {
    super();
    this._baseValue = initialState;
    this._cachedValue = initialState;
  }
  _updateState() {
    this._cachedValue = this._pendingTransforms.reduce((state, transform) => {
      return pipeTransforms(state, transform.task, [transform.loading, transform.optimistic]);
    }, this._baseValue);
    this._notifySubscribers();
  }
  get baseValue() {
    return this._baseValue;
  }
  get value() {
    return this._cachedValue;
  }
  update(state) {
    this._baseValue = state;
    this._updateState();
  }
  async optimisticUpdate(transform) {
    const task = transform.execute();
    const pendingTransform = {
      ...transform,
      task
    };
    try {
      this._pendingTransforms.push(pendingTransform);
      this._updateState();
      const result = await task;
      this._baseValue = pipeTransforms(this._baseValue, result, [transform.optimistic, transform.then]);
      for (const fn of this._completedOptimistics) this._baseValue = fn(this._baseValue);
      if (transform.optimistic) this._completedOptimistics.push(transform.optimistic);
      return result;
    } finally {
      const index = this._pendingTransforms.indexOf(pendingTransform);
      if (index > -1) this._pendingTransforms.splice(index, 1);
      if (this._pendingTransforms.length === 0) this._completedOptimistics.length = 0;
      this._updateState();
    }
  }
};

// node_modules/@assistant-ui/core/dist/runtimes/remote-thread-list/empty-thread-core.js
var EMPTY_THREAD_ERROR = new Error("This is the empty thread, a placeholder for the main thread. You cannot perform any actions on this thread instance. This error is probably because you tried to call a thread method in your render function. Call the method inside a `useEffect` hook instead.");
var EMPTY_THREAD_CORE = {
  getMessageById() {
  },
  getBranches() {
    return [];
  },
  switchToBranch() {
    throw EMPTY_THREAD_ERROR;
  },
  append() {
    throw EMPTY_THREAD_ERROR;
  },
  startRun() {
    throw EMPTY_THREAD_ERROR;
  },
  resumeRun() {
    throw EMPTY_THREAD_ERROR;
  },
  cancelRun() {
    throw EMPTY_THREAD_ERROR;
  },
  addToolResult() {
    throw EMPTY_THREAD_ERROR;
  },
  resumeToolCall() {
    throw EMPTY_THREAD_ERROR;
  },
  respondToToolApproval() {
    throw EMPTY_THREAD_ERROR;
  },
  speak() {
    throw EMPTY_THREAD_ERROR;
  },
  stopSpeaking() {
    throw EMPTY_THREAD_ERROR;
  },
  connectVoice() {
    throw EMPTY_THREAD_ERROR;
  },
  disconnectVoice() {
    throw EMPTY_THREAD_ERROR;
  },
  getVoiceVolume: () => 0,
  subscribeVoiceVolume: () => () => {
  },
  muteVoice() {
    throw EMPTY_THREAD_ERROR;
  },
  unmuteVoice() {
    throw EMPTY_THREAD_ERROR;
  },
  submitFeedback() {
    throw EMPTY_THREAD_ERROR;
  },
  getModelContext() {
    return {};
  },
  exportExternalState() {
    throw EMPTY_THREAD_ERROR;
  },
  importExternalState() {
    throw EMPTY_THREAD_ERROR;
  },
  composer: {
    attachments: [],
    attachmentAccept: "*",
    async addAttachment() {
      throw EMPTY_THREAD_ERROR;
    },
    async removeAttachment() {
      throw EMPTY_THREAD_ERROR;
    },
    isEditing: true,
    canCancel: false,
    canSend: false,
    isEmpty: true,
    text: "",
    setText() {
      throw EMPTY_THREAD_ERROR;
    },
    role: "user",
    setRole() {
      throw EMPTY_THREAD_ERROR;
    },
    runConfig: {},
    setRunConfig() {
      throw EMPTY_THREAD_ERROR;
    },
    async reset() {
    },
    async clearAttachments() {
    },
    send() {
      throw EMPTY_THREAD_ERROR;
    },
    cancel() {
    },
    dictation: void 0,
    startDictation() {
      throw EMPTY_THREAD_ERROR;
    },
    stopDictation() {
    },
    quote: void 0,
    setQuote() {
      throw EMPTY_THREAD_ERROR;
    },
    subscribe() {
      return () => {
      };
    },
    unstable_on() {
      return () => {
      };
    }
  },
  getEditComposer() {
  },
  beginEdit() {
    throw EMPTY_THREAD_ERROR;
  },
  speech: void 0,
  voice: void 0,
  capabilities: {
    switchToBranch: false,
    switchBranchDuringRun: false,
    edit: false,
    reload: false,
    cancel: false,
    unstable_copy: false,
    speech: false,
    dictation: false,
    voice: false,
    attachments: false,
    feedback: false,
    queue: false
  },
  isDisabled: false,
  isSendDisabled: false,
  isLoading: true,
  messages: [],
  state: null,
  suggestions: [],
  extras: void 0,
  subscribe() {
    return () => {
    };
  },
  import() {
    throw EMPTY_THREAD_ERROR;
  },
  export() {
    return { messages: [] };
  },
  reset() {
    throw EMPTY_THREAD_ERROR;
  },
  unstable_on() {
    return () => {
    };
  }
};

// node_modules/@assistant-ui/core/dist/runtimes/remote-thread-list/remote-thread-state.js
function createThreadMappingId(id) {
  return id;
}
var normalizeCursor = (c) => c || void 0;
var classifyThreads = (threads, acc) => {
  for (const thread of threads) {
    if (acc.threadIdMap[thread.remoteId] !== void 0) continue;
    switch (thread.status) {
      case "regular":
        acc.threadIds.push(thread.remoteId);
        break;
      case "archived":
        acc.archivedThreadIds.push(thread.remoteId);
        break;
      default: {
        const _exhaustiveCheck = thread.status;
        throw new Error(`Unsupported state: ${_exhaustiveCheck}`);
      }
    }
    const mappingId = createThreadMappingId(thread.remoteId);
    acc.threadIdMap[thread.remoteId] = mappingId;
    acc.threadData[mappingId] = {
      id: thread.remoteId,
      remoteId: thread.remoteId,
      externalId: thread.externalId,
      status: thread.status,
      title: thread.title,
      custom: thread.custom,
      initializeTask: Promise.resolve({
        remoteId: thread.remoteId,
        externalId: thread.externalId
      })
    };
  }
  return acc;
};
var getThreadData = (state, threadIdOrRemoteId) => {
  const idx = state.threadIdMap[threadIdOrRemoteId];
  if (idx === void 0) return void 0;
  return state.threadData[idx];
};
var updateStatusReducer = (state, threadIdOrRemoteId, newStatus) => {
  const data = getThreadData(state, threadIdOrRemoteId);
  if (!data) return state;
  const { id, remoteId, status: lastStatus } = data;
  if (lastStatus === newStatus) return state;
  const newState = { ...state };
  switch (lastStatus) {
    case "new":
      newState.newThreadId = void 0;
      break;
    case "regular":
      newState.threadIds = newState.threadIds.filter((t) => t !== id);
      break;
    case "archived":
      newState.archivedThreadIds = newState.archivedThreadIds.filter((t) => t !== id);
      break;
    default:
      throw new Error(`Unsupported state: ${lastStatus}`);
  }
  switch (newStatus) {
    case "regular":
      newState.threadIds = [id, ...newState.threadIds];
      break;
    case "archived":
      newState.archivedThreadIds = [id, ...newState.archivedThreadIds];
      break;
    case "deleted":
      newState.threadData = Object.fromEntries(Object.entries(newState.threadData).filter(([key]) => key !== id));
      newState.threadIdMap = Object.fromEntries(Object.entries(newState.threadIdMap).filter(([key]) => key !== id && key !== remoteId));
      break;
    default:
      throw new Error(`Unsupported state: ${newStatus}`);
  }
  if (newStatus !== "deleted") newState.threadData = {
    ...newState.threadData,
    [id]: {
      ...data,
      status: newStatus
    }
  };
  return newState;
};

// node_modules/@assistant-ui/core/dist/react/runtimes/RemoteThreadListHookInstanceManager.js
var import_react24 = __toESM(require_react(), 1);
var import_jsx_runtime12 = __toESM(require_jsx_runtime(), 1);
var ProviderRenderDetector = ({ detectorRef }) => {
  (0, import_react24.useLayoutEffect)(() => {
    detectorRef.current = true;
  }, [detectorRef]);
  return null;
};
var RemoteThreadListHookInstanceManager = class extends BaseSubscribable {
  useRuntimeHook;
  instances = /* @__PURE__ */ new Map();
  useAliveThreadsKeysChanged = create(() => ({}));
  parent;
  constructor(runtimeHook, parent) {
    super();
    this.parent = parent;
    this.useRuntimeHook = create(() => ({ useRuntime: runtimeHook }));
  }
  startThreadRuntime(threadId) {
    if (!this.instances.has(threadId)) {
      this.instances.set(threadId, {});
      this.useAliveThreadsKeysChanged.setState({}, true);
    }
    return new Promise((resolve, reject) => {
      const callback = () => {
        const instance = this.instances.get(threadId);
        if (!instance) {
          dispose();
          reject(new Error("Thread was deleted before runtime was started"));
        } else if (!instance.runtime) return;
        else {
          dispose();
          resolve(instance.runtime);
        }
      };
      const dispose = this.subscribe(callback);
      callback();
    });
  }
  getThreadRuntimeCore(threadId) {
    const instance = this.instances.get(threadId);
    if (!instance) return void 0;
    return instance.runtime;
  }
  stopThreadRuntime(threadId) {
    this.instances.delete(threadId);
    this.useAliveThreadsKeysChanged.setState({}, true);
  }
  setRuntimeHook(newRuntimeHook) {
    if (this.useRuntimeHook.getState().useRuntime !== newRuntimeHook) this.useRuntimeHook.setState({ useRuntime: newRuntimeHook }, true);
  }
  _RuntimeBinder = ({ threadId, children }) => {
    const { useRuntime } = this.useRuntimeHook();
    const runtime = useRuntime();
    const threadBinding = runtime.thread.__internal_threadBinding;
    const updateRuntime = (0, import_react24.useCallback)(() => {
      const aliveThread = this.instances.get(threadId);
      if (!aliveThread) throw new Error("Thread not found. This is a bug in assistant-ui.");
      aliveThread.runtime = threadBinding.getState();
      this._notifySubscribers();
    }, [threadId, threadBinding]);
    const isMounted = (0, import_react24.useRef)(false);
    if (!isMounted.current) updateRuntime();
    (0, import_react24.useEffect)(() => {
      isMounted.current = true;
      updateRuntime();
      return threadBinding.outerSubscribe(updateRuntime);
    }, [threadBinding, updateRuntime]);
    const aui = useAui();
    const initPromiseRef = (0, import_react24.useRef)(void 0);
    const hasInitializedRef = (0, import_react24.useRef)(false);
    (0, import_react24.useEffect)(() => {
      const runtimeCore = threadBinding.getState();
      const setGetInitializePromise = runtimeCore.__internal_setGetInitializePromise;
      if (typeof setGetInitializePromise === "function") setGetInitializePromise.call(runtimeCore, () => initPromiseRef.current);
    }, [threadBinding]);
    (0, import_react24.useEffect)(() => {
      hasInitializedRef.current = false;
      return runtime.threads.main.unstable_on("initialize", () => {
        if (hasInitializedRef.current) return;
        if (aui.threadListItem().getState().status !== "new") return;
        hasInitializedRef.current = true;
        initPromiseRef.current = aui.threadListItem().initialize();
        const dispose = runtime.thread.unstable_on("runEnd", () => {
          dispose();
          aui.threadListItem().generateTitle();
        });
      });
    }, [runtime, aui]);
    return (0, import_jsx_runtime12.jsx)(import_jsx_runtime12.Fragment, { children });
  };
  _OuterActiveThreadProvider = (0, import_react24.memo)(({ threadId, provider: Provider }) => {
    const runtime = (0, import_react24.useMemo)(() => new ThreadListRuntimeImpl(this.parent).getItemById(threadId), [threadId]);
    const detectorRef = (0, import_react24.useRef)(false);
    (0, import_react24.useEffect)(() => {
      if (Provider !== import_react24.Fragment) {
        const id = setTimeout(() => {
          if (!detectorRef.current) console.warn("RemoteThreadListAdapter.unstable_Provider did not render its `children` synchronously. Render `children` on first commit; deferring them behind a loading state, Suspense boundary, or `useEffect` gate strands the runtime binder and leaves the thread without context.");
        }, 100);
        return () => clearTimeout(id);
      }
    }, [Provider]);
    return (0, import_jsx_runtime12.jsx)(ThreadListItemRuntimeProvider, {
      runtime,
      children: (0, import_jsx_runtime12.jsx)(Provider, { children: (0, import_jsx_runtime12.jsx)(this._RuntimeBinder, {
        threadId,
        children: (0, import_jsx_runtime12.jsx)(ProviderRenderDetector, { detectorRef })
      }) })
    });
  });
  __internal_RenderThreadRuntimes = ({ provider }) => {
    this.useAliveThreadsKeysChanged();
    return Array.from(this.instances.keys()).map((threadId) => (0, import_jsx_runtime12.jsx)(this._OuterActiveThreadProvider, {
      threadId,
      provider
    }, threadId));
  };
};

// node_modules/@assistant-ui/core/dist/react/runtimes/RemoteThreadListThreadListRuntimeCore.js
var import_react25 = __toESM(require_react(), 1);
var import_jsx_runtime13 = __toESM(require_jsx_runtime(), 1);
var RemoteThreadListThreadListRuntimeCore = class extends BaseSubscribable {
  _options;
  _hookManager;
  _loadThreadsPromise;
  _loadMorePromise;
  _loadGeneration = 0;
  _mainThreadId;
  _state = new OptimisticState({
    isLoading: true,
    isLoadingMore: false,
    cursor: void 0,
    newThreadId: void 0,
    threadIds: [],
    archivedThreadIds: [],
    threadIdMap: {},
    threadData: {}
  });
  get threadItems() {
    return this._state.value.threadData;
  }
  getLoadThreadsPromise() {
    if (!this._loadThreadsPromise) {
      const generation = this._loadGeneration;
      this._loadThreadsPromise = this._state.optimisticUpdate({
        execute: () => this._options.adapter.list(),
        loading: (state) => {
          return {
            ...state,
            isLoading: true
          };
        },
        then: (state, l) => {
          if (generation !== this._loadGeneration) return state;
          const fresh = classifyThreads(l.threads, {
            threadIds: [],
            archivedThreadIds: [],
            threadIdMap: {},
            threadData: {}
          });
          return {
            ...state,
            isLoading: false,
            cursor: normalizeCursor(l.nextCursor),
            threadIds: fresh.threadIds,
            archivedThreadIds: fresh.archivedThreadIds,
            threadIdMap: {
              ...state.threadIdMap,
              ...fresh.threadIdMap
            },
            threadData: {
              ...state.threadData,
              ...fresh.threadData
            }
          };
        }
      }).catch((error) => {
        if (generation !== this._loadGeneration) return;
        console.error("[assistant-ui] thread list load failed:", error);
        this._loadThreadsPromise = void 0;
        this._state.update({
          ...this._state.baseValue,
          isLoading: false
        });
      }).then(() => {
      });
    }
    return this._loadThreadsPromise;
  }
  loadMore() {
    if (this._loadMorePromise) return this._loadMorePromise;
    const initialState = this._state.value;
    if (initialState.cursor === void 0 || initialState.isLoading) return Promise.resolve();
    const generation = this._loadGeneration;
    const adapter = this._options.adapter;
    const cursor = initialState.cursor;
    const dedup = this._state.optimisticUpdate({
      execute: () => adapter.list({ after: cursor }),
      loading: (state) => ({
        ...state,
        isLoadingMore: true
      }),
      then: (state, l) => {
        if (generation !== this._loadGeneration) return state;
        if (adapter !== this._options.adapter) return state;
        const appended = classifyThreads(l.threads, {
          threadIds: [...state.threadIds],
          archivedThreadIds: [...state.archivedThreadIds],
          threadIdMap: { ...state.threadIdMap },
          threadData: { ...state.threadData }
        });
        return {
          ...state,
          isLoadingMore: false,
          cursor: normalizeCursor(l.nextCursor),
          threadIds: appended.threadIds,
          archivedThreadIds: appended.archivedThreadIds,
          threadIdMap: appended.threadIdMap,
          threadData: appended.threadData
        };
      }
    }).catch((error) => {
      console.error("[assistant-ui] thread list loadMore failed:", error);
    }).then(() => {
      if (this._loadMorePromise === dedup) this._loadMorePromise = void 0;
    });
    this._loadMorePromise = dedup;
    return dedup;
  }
  contextProvider;
  constructor(options, contextProvider) {
    super();
    this.contextProvider = contextProvider;
    this._state.subscribe(() => this._notifySubscribers());
    this._hookManager = new RemoteThreadListHookInstanceManager(options.runtimeHook, this);
    this.useProvider = create(() => ({ Provider: options.adapter.unstable_Provider ?? import_react25.Fragment }));
    this.__internal_setOptions(options);
    this.switchToNewThread();
  }
  _initialThreadLoaded = false;
  useProvider;
  __internal_setOptions(options) {
    if (this._options === options) return;
    const adapterChanged = this._options !== void 0 && this._options.adapter !== options.adapter;
    this._options = options;
    const Provider = options.adapter.unstable_Provider ?? import_react25.Fragment;
    if (Provider !== this.useProvider.getState().Provider) this.useProvider.setState({ Provider }, true);
    this._hookManager.setRuntimeHook(options.runtimeHook);
    if (adapterChanged) {
      this._loadGeneration++;
      this._loadThreadsPromise = void 0;
      this._loadMorePromise = void 0;
      this._state.update({
        ...this._state.baseValue,
        cursor: void 0
      });
    }
  }
  __internal_load() {
    this.getLoadThreadsPromise();
    const startThreadId = this._options.threadId ?? this._options.initialThreadId;
    if (!this._initialThreadLoaded && startThreadId) {
      this._initialThreadLoaded = true;
      this.switchToThread(startThreadId).catch(() => {
      });
    }
  }
  reload() {
    this._loadGeneration++;
    this._loadThreadsPromise = void 0;
    this._loadMorePromise = void 0;
    this._state.update({
      ...this._state.baseValue,
      cursor: void 0
    });
    return this.getLoadThreadsPromise();
  }
  get isLoading() {
    return this._state.value.isLoading;
  }
  get isLoadingMore() {
    return this._state.value.isLoadingMore;
  }
  get hasMore() {
    return this._state.value.cursor !== void 0;
  }
  get threadIds() {
    return this._state.value.threadIds;
  }
  get archivedThreadIds() {
    return this._state.value.archivedThreadIds;
  }
  get newThreadId() {
    return this._state.value.newThreadId;
  }
  get mainThreadId() {
    return this._mainThreadId;
  }
  getMainThreadRuntimeCore() {
    const result = this._hookManager.getThreadRuntimeCore(this._mainThreadId);
    if (!result) return EMPTY_THREAD_CORE;
    return result;
  }
  getThreadRuntimeCore(threadIdOrRemoteId) {
    const data = this.getItemById(threadIdOrRemoteId);
    if (!data) throw new Error("Thread not found");
    const result = this._hookManager.getThreadRuntimeCore(data.id);
    if (!result) throw new Error("Thread not found");
    return result;
  }
  getItemById(threadIdOrRemoteId) {
    return getThreadData(this._state.value, threadIdOrRemoteId);
  }
  async switchToThread(threadIdOrRemoteId, options) {
    let data = this.getItemById(threadIdOrRemoteId);
    if (!data) {
      const remoteMetadata = await this._options.adapter.fetch(threadIdOrRemoteId);
      const state = this._state.value;
      const mappingId = createThreadMappingId(remoteMetadata.remoteId);
      const newThreadData = {
        ...state.threadData,
        [mappingId]: {
          id: mappingId,
          initializeTask: Promise.resolve({
            remoteId: remoteMetadata.remoteId,
            externalId: remoteMetadata.externalId
          }),
          remoteId: remoteMetadata.remoteId,
          externalId: remoteMetadata.externalId,
          status: remoteMetadata.status,
          title: remoteMetadata.title,
          custom: remoteMetadata.custom
        }
      };
      const newThreadIdMap = {
        ...state.threadIdMap,
        [remoteMetadata.remoteId]: mappingId
      };
      const threadIdsWithoutRemote = state.threadIds.filter((id) => id !== remoteMetadata.remoteId);
      const archivedThreadIdsWithoutRemote = state.archivedThreadIds.filter((id) => id !== remoteMetadata.remoteId);
      const newThreadIds = remoteMetadata.status === "regular" ? [...threadIdsWithoutRemote, remoteMetadata.remoteId] : threadIdsWithoutRemote;
      const newArchivedThreadIds = remoteMetadata.status === "archived" ? [...archivedThreadIdsWithoutRemote, remoteMetadata.remoteId] : archivedThreadIdsWithoutRemote;
      this._state.update({
        ...state,
        threadIds: newThreadIds,
        archivedThreadIds: newArchivedThreadIds,
        threadIdMap: newThreadIdMap,
        threadData: newThreadData
      });
      data = this.getItemById(threadIdOrRemoteId);
    }
    if (!data) throw new Error("Thread not found");
    if (this._mainThreadId === data.id) return;
    const task = this._hookManager.startThreadRuntime(data.id);
    if (this.mainThreadId !== void 0) await task;
    else task.then(() => this._notifySubscribers());
    if (data.status === "archived" && options?.unarchive !== false) await this.unarchive(data.id);
    this._mainThreadId = data.id;
    this._notifySubscribers();
  }
  async switchToNewThread() {
    while (this._state.baseValue.newThreadId !== void 0 && this._state.value.newThreadId === void 0) await this._state.waitForUpdate();
    const state = this._state.value;
    let id = this._state.value.newThreadId;
    if (id === void 0) {
      do
        id = `__LOCALID_${generateId()}`;
      while (state.threadIdMap[id]);
      const mappingId = createThreadMappingId(id);
      this._state.update({
        ...state,
        newThreadId: id,
        threadIdMap: {
          ...state.threadIdMap,
          [id]: mappingId
        },
        threadData: {
          ...state.threadData,
          [mappingId]: {
            status: "new",
            id,
            remoteId: void 0,
            externalId: void 0,
            title: void 0,
            custom: void 0
          }
        }
      });
    }
    return this.switchToThread(id);
  }
  initialize = async (threadId) => {
    if (this._state.value.newThreadId !== threadId) {
      const data = this.getItemById(threadId);
      if (!data) throw new Error("Thread not found");
      if (data.status === "new") throw new Error("Unexpected new state");
      return data.initializeTask;
    }
    return this._state.optimisticUpdate({
      execute: () => {
        return this._options.adapter.initialize(threadId);
      },
      optimistic: (state) => {
        return updateStatusReducer(state, threadId, "regular");
      },
      loading: (state, task) => {
        const mappingId = createThreadMappingId(threadId);
        return {
          ...state,
          threadData: {
            ...state.threadData,
            [mappingId]: {
              ...state.threadData[mappingId],
              initializeTask: task
            }
          }
        };
      },
      then: (state, { remoteId, externalId }) => {
        const data = getThreadData(state, threadId);
        if (!data) return state;
        const mappingId = createThreadMappingId(threadId);
        return {
          ...state,
          threadIdMap: {
            ...state.threadIdMap,
            [remoteId]: mappingId
          },
          threadData: {
            ...state.threadData,
            [mappingId]: {
              ...data,
              initializeTask: Promise.resolve({
                remoteId,
                externalId
              }),
              remoteId,
              externalId
            }
          }
        };
      }
    });
  };
  generateTitle = async (threadId) => {
    const data = this.getItemById(threadId);
    if (!data) throw new Error("Thread not found");
    if (data.status === "new") throw new Error("Thread is not yet initialized");
    const { remoteId } = await data.initializeTask;
    const runtimeCore = this._hookManager.getThreadRuntimeCore(data.id);
    if (!runtimeCore) return;
    const messages = runtimeCore.messages;
    const stream = await this._options.adapter.generateTitle(remoteId, messages);
    const messageStream = AssistantMessageStream.fromAssistantStream(stream);
    for await (const result of messageStream) {
      const newTitle = result.parts.filter((c) => c.type === "text")[0]?.text;
      const state = this._state.baseValue;
      const currentData = getThreadData(state, data.id);
      if (!currentData) continue;
      this._state.update({
        ...state,
        threadData: {
          ...state.threadData,
          [currentData.id]: {
            ...currentData,
            title: newTitle
          }
        }
      });
    }
  };
  rename(threadIdOrRemoteId, newTitle) {
    const data = this.getItemById(threadIdOrRemoteId);
    if (!data) throw new Error("Thread not found");
    if (data.status === "new") throw new Error("Thread is not yet initialized");
    return this._state.optimisticUpdate({
      execute: async () => {
        const { remoteId } = await data.initializeTask;
        return this._options.adapter.rename(remoteId, newTitle);
      },
      optimistic: (state) => {
        const data2 = getThreadData(state, threadIdOrRemoteId);
        if (!data2) return state;
        return {
          ...state,
          threadData: {
            ...state.threadData,
            [data2.id]: {
              ...data2,
              title: newTitle
            }
          }
        };
      }
    });
  }
  updateCustom(threadIdOrRemoteId, custom) {
    const data = this.getItemById(threadIdOrRemoteId);
    if (!data) throw new Error("Thread not found");
    if (data.status === "new") throw new Error("Thread is not yet initialized");
    if (!this._options.adapter.updateCustom) throw new Error("Remote thread list adapter does not support updating custom metadata");
    return this._state.optimisticUpdate({
      execute: async () => {
        const { remoteId } = await data.initializeTask;
        const adapter = this._options.adapter;
        if (!adapter.updateCustom) throw new Error("Remote thread list adapter does not support updating custom metadata");
        return adapter.updateCustom(remoteId, custom);
      },
      optimistic: (state) => {
        const data2 = getThreadData(state, threadIdOrRemoteId);
        if (!data2) return state;
        return {
          ...state,
          threadData: {
            ...state.threadData,
            [data2.id]: {
              ...data2,
              custom
            }
          }
        };
      }
    });
  }
  async _ensureThreadIsNotMain(threadId) {
    if (threadId === this.newThreadId) throw new Error("Cannot ensure new thread is not main");
    if (threadId === this._mainThreadId) await this.switchToNewThread();
  }
  async archive(threadIdOrRemoteId) {
    const data = this.getItemById(threadIdOrRemoteId);
    if (!data) throw new Error("Thread not found");
    if (data.status !== "regular") throw new Error("Thread is not yet initialized or already archived");
    await this._ensureThreadIsNotMain(data.id);
    return this._state.optimisticUpdate({
      execute: async () => {
        const { remoteId } = await data.initializeTask;
        return this._options.adapter.archive(remoteId);
      },
      optimistic: (state) => {
        return updateStatusReducer(state, data.id, "archived");
      }
    });
  }
  unarchive(threadIdOrRemoteId) {
    const data = this.getItemById(threadIdOrRemoteId);
    if (!data) throw new Error("Thread not found");
    if (data.status !== "archived") throw new Error("Thread is not archived");
    return this._state.optimisticUpdate({
      execute: async () => {
        try {
          const { remoteId } = await data.initializeTask;
          return await this._options.adapter.unarchive(remoteId);
        } catch (error) {
          await this._ensureThreadIsNotMain(data.id);
          throw error;
        }
      },
      optimistic: (state) => {
        return updateStatusReducer(state, data.id, "regular");
      }
    });
  }
  async delete(threadIdOrRemoteId) {
    const data = this.getItemById(threadIdOrRemoteId);
    if (!data) throw new Error("Thread not found");
    if (data.status !== "regular" && data.status !== "archived") throw new Error("Thread is not yet initialized");
    await this._ensureThreadIsNotMain(data.id);
    this._hookManager.stopThreadRuntime(data.id);
    return this._state.optimisticUpdate({
      execute: async () => {
        const { remoteId } = await data.initializeTask;
        return await this._options.adapter.delete(remoteId);
      },
      optimistic: (state) => {
        return updateStatusReducer(state, data.id, "deleted");
      }
    });
  }
  async detach(threadIdOrRemoteId) {
    const data = this.getItemById(threadIdOrRemoteId);
    if (!data) throw new Error("Thread not found");
    if (data.status !== "regular" && data.status !== "archived") throw new Error("Thread is not yet initialized");
    await this._ensureThreadIsNotMain(data.id);
    this._hookManager.stopThreadRuntime(data.id);
  }
  useBoundIds = create(() => []);
  __internal_RenderComponent = () => {
    const id = (0, import_react25.useId)();
    (0, import_react25.useEffect)(() => {
      this.useBoundIds.setState((s) => [...s, id], true);
      return () => {
        this.useBoundIds.setState((s) => s.filter((i) => i !== id), true);
      };
    }, [id]);
    const boundIds = this.useBoundIds();
    const { Provider } = this.useProvider();
    const adapters = { modelContext: this.contextProvider };
    return (boundIds.length === 0 || boundIds[0] === id) && (0, import_jsx_runtime13.jsx)(RuntimeAdapterProvider, {
      adapters,
      children: (0, import_jsx_runtime13.jsx)(this._hookManager.__internal_RenderThreadRuntimes, { provider: Provider })
    });
  };
};

// node_modules/@assistant-ui/core/dist/react/runtimes/useRemoteThreadListRuntime.js
var import_react26 = __toESM(require_react(), 1);
var RemoteThreadListRuntimeCore = class extends BaseAssistantRuntimeCore {
  threads;
  constructor(options) {
    super();
    this.threads = new RemoteThreadListThreadListRuntimeCore(options, this._contextProvider);
  }
  get RenderComponent() {
    return this.threads.__internal_RenderComponent;
  }
};
var useRemoteThreadListRuntimeImpl = (options) => {
  const [runtime] = (0, import_react26.useState)(() => new RemoteThreadListRuntimeCore(options));
  (0, import_react26.useEffect)(() => {
    runtime.threads.__internal_setOptions(options);
    runtime.threads.__internal_load();
  }, [runtime, options]);
  return (0, import_react26.useMemo)(() => new AssistantRuntimeImpl(runtime), [runtime]);
};
var useRemoteThreadListRuntime = (options) => {
  const runtimeHookRef = (0, import_react26.useRef)(options.runtimeHook);
  runtimeHookRef.current = options.runtimeHook;
  const startThreadIdRef = (0, import_react26.useRef)(options.threadId ?? options.initialThreadId);
  const stableRuntimeHook = (0, import_react26.useCallback)(() => {
    return runtimeHookRef.current();
  }, []);
  const stableOptions = (0, import_react26.useMemo)(() => ({
    adapter: options.adapter,
    allowNesting: options.allowNesting,
    initialThreadId: startThreadIdRef.current,
    runtimeHook: stableRuntimeHook
  }), [
    options.adapter,
    options.allowNesting,
    stableRuntimeHook
  ]);
  if (useAui().threadListItem.source !== null) {
    if (!stableOptions.allowNesting) throw new Error("useRemoteThreadListRuntime cannot be nested inside another RemoteThreadListRuntime. Set allowNesting: true to allow nesting (the inner runtime will become a no-op).");
    return stableRuntimeHook();
  }
  const runtime = useRemoteThreadListRuntimeImpl(stableOptions);
  const prevThreadIdRef = (0, import_react26.useRef)(options.threadId);
  (0, import_react26.useEffect)(() => {
    if (options.threadId === prevThreadIdRef.current) return;
    prevThreadIdRef.current = options.threadId;
    if (options.threadId) runtime.threads.switchToThread(options.threadId).catch(() => {
    });
    else runtime.threads.switchToNewThread().catch(() => {
    });
  }, [runtime, options.threadId]);
  return runtime;
};

// node_modules/assistant-cloud/dist/AssistantCloudAuthStrategy.js
var getJwtExpiry = (jwt) => {
  try {
    const bodyPart = jwt.split(".")[1];
    if (!bodyPart) throw new Error("Invalid JWT format");
    let base64 = bodyPart.replace(/-/g, "+").replace(/_/g, "/");
    while (base64.length % 4 !== 0) base64 += "=";
    const payload = atob(base64);
    const exp = JSON.parse(payload).exp;
    if (!exp || typeof exp !== "number") throw new Error('JWT does not contain a valid "exp" field');
    return exp * 1e3;
  } catch (error) {
    throw new Error(`Unable to determine the token expiry: ${error}`);
  }
};
var AssistantCloudJWTAuthStrategy = class {
  strategy = "jwt";
  cachedToken = null;
  tokenExpiry = null;
  #authTokenCallback;
  constructor(authTokenCallback) {
    this.#authTokenCallback = authTokenCallback;
  }
  async getAuthHeaders() {
    const currentTime = Date.now();
    if (this.cachedToken && this.tokenExpiry && this.tokenExpiry - currentTime > 30 * 1e3) return { Authorization: `Bearer ${this.cachedToken}` };
    const newToken = await this.#authTokenCallback();
    if (!newToken) return false;
    this.cachedToken = newToken;
    this.tokenExpiry = getJwtExpiry(newToken);
    return { Authorization: `Bearer ${newToken}` };
  }
  readAuthHeaders(headers) {
    const authHeader = headers.get("Authorization");
    if (!authHeader) return;
    const [scheme, token] = authHeader.split(" ");
    if (scheme !== "Bearer" || !token) throw new Error("Invalid auth header received");
    this.cachedToken = token;
    this.tokenExpiry = getJwtExpiry(token);
  }
};
var AssistantCloudAPIKeyAuthStrategy = class {
  strategy = "api-key";
  #apiKey;
  #userId;
  #workspaceId;
  constructor(apiKey, userId, workspaceId) {
    this.#apiKey = apiKey;
    this.#userId = userId;
    this.#workspaceId = workspaceId;
  }
  async getAuthHeaders() {
    return {
      Authorization: `Bearer ${this.#apiKey}`,
      "Aui-User-Id": this.#userId,
      "Aui-Workspace-Id": this.#workspaceId
    };
  }
  readAuthHeaders() {
  }
};
var AUI_REFRESH_TOKEN_NAME = "aui:refresh_token";
var AssistantCloudAnonymousAuthStrategy = class {
  strategy = "anon";
  baseUrl;
  jwtStrategy;
  constructor(baseUrl2) {
    this.baseUrl = baseUrl2;
    this.jwtStrategy = new AssistantCloudJWTAuthStrategy(async () => {
      const currentTime = Date.now();
      const storedRefreshTokenJson = localStorage.getItem(AUI_REFRESH_TOKEN_NAME);
      const storedRefreshToken = storedRefreshTokenJson ? JSON.parse(storedRefreshTokenJson) : void 0;
      if (storedRefreshToken) if (new Date(storedRefreshToken.expires_at).getTime() - currentTime > 30 * 1e3) {
        const response2 = await fetch(`${this.baseUrl}/v1/auth/tokens/refresh`, {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ refresh_token: storedRefreshToken.token })
        });
        if (response2.ok) {
          const { access_token: access_token2, refresh_token: refresh_token2 } = await response2.json();
          if (refresh_token2) localStorage.setItem(AUI_REFRESH_TOKEN_NAME, JSON.stringify(refresh_token2));
          return access_token2;
        }
      } else localStorage.removeItem(AUI_REFRESH_TOKEN_NAME);
      const response = await fetch(`${this.baseUrl}/v1/auth/tokens/anonymous`, { method: "POST" });
      if (!response.ok) return null;
      const { access_token, refresh_token } = await response.json();
      if (!access_token || !refresh_token) return null;
      localStorage.setItem(AUI_REFRESH_TOKEN_NAME, JSON.stringify(refresh_token));
      return access_token;
    });
  }
  async getAuthHeaders() {
    return this.jwtStrategy.getAuthHeaders();
  }
  readAuthHeaders(headers) {
    this.jwtStrategy.readAuthHeaders(headers);
  }
};

// node_modules/assistant-cloud/dist/AssistantCloudAPI.js
var CloudAPIError = class extends Error {
  constructor(message) {
    super(message);
    this.name = "APIError";
  }
};
var AssistantCloudAPI = class {
  _auth;
  _baseUrl;
  constructor(config) {
    if ("authToken" in config) {
      this._baseUrl = config.baseUrl;
      this._auth = new AssistantCloudJWTAuthStrategy(config.authToken);
    } else if ("apiKey" in config) {
      this._baseUrl = (config.baseUrl ?? "https://backend.assistant-api.com").replace(/\/$/, "");
      this._auth = new AssistantCloudAPIKeyAuthStrategy(config.apiKey, config.userId, config.workspaceId);
    } else if ("anonymous" in config) {
      this._baseUrl = config.baseUrl;
      this._auth = new AssistantCloudAnonymousAuthStrategy(config.baseUrl);
    } else throw new Error("Invalid configuration: Must provide authToken, apiKey, or anonymous configuration");
  }
  async initializeAuth() {
    return !!this._auth.getAuthHeaders();
  }
  async makeRawRequest(endpoint, options = {}) {
    const authHeaders = await this._auth.getAuthHeaders();
    if (!authHeaders) throw new Error("Authorization failed");
    const headers = {
      ...authHeaders,
      ...options.headers,
      "Content-Type": "application/json"
    };
    const queryParams = new URLSearchParams();
    if (options.query) for (const [key, value] of Object.entries(options.query)) {
      if (value === false) continue;
      if (value === true) queryParams.set(key, "true");
      else queryParams.set(key, value.toString());
    }
    const url = new URL(`${this._baseUrl}/v1${endpoint}`);
    url.search = queryParams.toString();
    const response = await fetch(url, {
      method: options.method ?? "GET",
      headers,
      body: options.body ? JSON.stringify(options.body) : null
    });
    this._auth.readAuthHeaders(response.headers);
    if (!response.ok) {
      const text = await response.text();
      try {
        throw new CloudAPIError(JSON.parse(text).message);
      } catch (error) {
        if (error instanceof CloudAPIError) throw error;
        throw new Error(`Request failed with status ${response.status}, ${text}`);
      }
    }
    return response;
  }
  async makeRequest(endpoint, options = {}) {
    return (await this.makeRawRequest(endpoint, options)).json();
  }
};

// node_modules/assistant-cloud/dist/AssistantCloudAuthTokens.js
var AssistantCloudAuthTokens = class {
  cloud;
  constructor(cloud) {
    this.cloud = cloud;
  }
  async create() {
    return this.cloud.makeRequest("/auth/tokens", { method: "POST" });
  }
};

// node_modules/assistant-cloud/dist/AssistantCloudRuns.js
var AssistantCloudRuns = class {
  cloud;
  constructor(cloud) {
    this.cloud = cloud;
  }
  __internal_getAssistantOptions(assistantId) {
    return {
      api: `${this.cloud._baseUrl}/v1/runs/stream`,
      headers: async () => {
        const headers = await this.cloud._auth.getAuthHeaders();
        if (!headers) throw new Error("Authorization failed");
        return {
          ...headers,
          Accept: "text/plain"
        };
      },
      body: {
        assistant_id: assistantId,
        response_format: "vercel-ai-data-stream/v1",
        thread_id: "unstable_todo"
      }
    };
  }
  async stream(body) {
    const response = await this.cloud.makeRawRequest("/runs/stream", {
      method: "POST",
      headers: { Accept: "text/plain" },
      body
    });
    return AssistantStream.fromResponse(response, new PlainTextDecoder());
  }
  async report(body) {
    return this.cloud.makeRequest("/runs", {
      method: "POST",
      body
    });
  }
};

// node_modules/assistant-cloud/dist/AssistantCloudThreadMessages.js
var AssistantCloudThreadMessages = class {
  cloud;
  constructor(cloud) {
    this.cloud = cloud;
  }
  async list(threadId, query) {
    return this.cloud.makeRequest(`/threads/${encodeURIComponent(threadId)}/messages`, { query });
  }
  async create(threadId, body) {
    return this.cloud.makeRequest(`/threads/${encodeURIComponent(threadId)}/messages`, {
      method: "POST",
      body
    });
  }
  async update(threadId, messageId, body) {
    return this.cloud.makeRequest(`/threads/${encodeURIComponent(threadId)}/messages/${encodeURIComponent(messageId)}`, {
      method: "PUT",
      body
    });
  }
};

// node_modules/assistant-cloud/dist/AssistantCloudThreads.js
var AssistantCloudThreads = class {
  cloud;
  messages;
  constructor(cloud) {
    this.cloud = cloud;
    this.messages = new AssistantCloudThreadMessages(cloud);
  }
  async list(query) {
    return this.cloud.makeRequest("/threads", { query });
  }
  async get(threadId) {
    return this.cloud.makeRequest(`/threads/${encodeURIComponent(threadId)}`);
  }
  async create(body) {
    return this.cloud.makeRequest("/threads", {
      method: "POST",
      body
    });
  }
  async update(threadId, body) {
    return this.cloud.makeRequest(`/threads/${encodeURIComponent(threadId)}`, {
      method: "PUT",
      body
    });
  }
  async delete(threadId) {
    return this.cloud.makeRequest(`/threads/${encodeURIComponent(threadId)}`, { method: "DELETE" });
  }
};

// node_modules/assistant-cloud/dist/AssistantCloudFiles.js
var AssistantCloudFiles = class {
  cloud;
  constructor(cloud) {
    this.cloud = cloud;
  }
  async pdfToImages(body) {
    return this.cloud.makeRequest("/files/pdf-to-images", {
      method: "POST",
      body
    });
  }
  async generatePresignedUploadUrl(body) {
    return this.cloud.makeRequest("/files/attachments/generate-presigned-upload-url", {
      method: "POST",
      body
    });
  }
};

// node_modules/assistant-cloud/dist/AssistantCloud.js
var AssistantCloud = class {
  threads;
  auth;
  runs;
  files;
  telemetry;
  constructor(config) {
    const api = new AssistantCloudAPI(config);
    this.threads = new AssistantCloudThreads(api);
    this.auth = { tokens: new AssistantCloudAuthTokens(api) };
    this.runs = new AssistantCloudRuns(api);
    this.files = new AssistantCloudFiles(api);
    const t = config.telemetry;
    this.telemetry = t === false ? { enabled: false } : t === true || t === void 0 ? { enabled: true } : {
      enabled: t.enabled !== false,
      ...t
    };
  }
};

// node_modules/assistant-cloud/dist/CloudMessagePersistence.js
var CloudMessagePersistence = class {
  cloud;
  idMapping = {};
  constructor(cloud) {
    this.cloud = cloud;
  }
  /**
  * Persist a message to the cloud.
  *
  * @param threadId - Remote thread ID
  * @param messageId - Local message ID (used for tracking)
  * @param parentId - Local parent message ID (or null for first message)
  * @param format - Message format (e.g., "aui/v0", "ai-sdk/v6")
  * @param content - Message content (format-specific)
  */
  async append(threadId, messageId, parentId, format, content) {
    const resolvedParentId = parentId ? await this.idMapping[parentId] ?? parentId : null;
    const task = this.cloud.threads.messages.create(threadId, {
      parent_id: resolvedParentId,
      format,
      content
    }).then(({ message_id }) => {
      this.idMapping[messageId] = message_id;
      return message_id;
    }).catch((err) => {
      if (this.idMapping[messageId] === task) delete this.idMapping[messageId];
      throw err;
    });
    this.idMapping[messageId] = task;
    return task.then(() => {
    });
  }
  /**
  * Update an already-persisted message in the cloud.
  */
  async update(threadId, messageId, _format, content) {
    const remoteId = await this.getRemoteId(messageId);
    if (!remoteId) return;
    await this.cloud.threads.messages.update(threadId, remoteId, { content });
  }
  /**
  * Check if a message has been persisted (or is currently being persisted).
  */
  isPersisted(messageId) {
    return messageId in this.idMapping;
  }
  /**
  * Get the remote ID for a local message ID (resolved).
  * Returns undefined if not persisted.
  */
  async getRemoteId(messageId) {
    const entry = this.idMapping[messageId];
    if (!entry) return void 0;
    return entry;
  }
  /**
  * Load messages from the cloud and populate the ID mapping.
  *
  * The ID mapping is populated so that `isPersisted()` returns true for
  * loaded messages, preventing re-persistence of already-stored messages.
  *
  * @param threadId - Remote thread ID
  * @param format - Optional format filter
  * @returns Array of cloud messages
  */
  async load(threadId, format) {
    const { messages } = await this.cloud.threads.messages.list(threadId, format ? { format } : void 0);
    for (const m of messages) this.idMapping[m.id] = m.id;
    return messages;
  }
  /**
  * Reset the ID mapping (call when switching threads).
  */
  reset() {
    this.idMapping = {};
  }
};

// node_modules/assistant-cloud/dist/FormattedCloudPersistence.js
var createFormattedPersistence = (persistence, adapter) => ({
  append: async (threadId, item) => {
    const messageId = adapter.getId(item.message);
    const encoded = adapter.encode(item);
    return persistence.append(threadId, messageId, item.parentId, adapter.format, encoded);
  },
  update: persistence.update ? async (threadId, item, messageId) => {
    const encoded = adapter.encode(item);
    return persistence.update(threadId, messageId, adapter.format, encoded);
  } : void 0,
  load: async (threadId) => {
    return { messages: (await persistence.load(threadId, adapter.format)).filter((m) => m.format === adapter.format).map((m) => adapter.decode({
      id: m.id,
      parent_id: m.parent_id,
      format: m.format,
      content: m.content
    })).reverse() };
  },
  isPersisted: (messageId) => persistence.isPersisted(messageId)
});

// node_modules/@assistant-ui/core/dist/react/runtimes/cloud/CloudFileAttachmentAdapter.js
var guessAttachmentType = (contentType) => {
  if (contentType.startsWith("image/")) return "image";
  if (contentType.startsWith("text/")) return "document";
  return "file";
};
var CloudFileAttachmentAdapter = class {
  cloud;
  accept = "*";
  constructor(cloud) {
    this.cloud = cloud;
  }
  uploadedUrls = /* @__PURE__ */ new Map();
  async *add({ file }) {
    const id = crypto.randomUUID();
    let attachment = {
      id,
      type: guessAttachmentType(file.type),
      name: file.name,
      contentType: file.type,
      file,
      status: {
        type: "running",
        reason: "uploading",
        progress: 0
      }
    };
    yield attachment;
    try {
      const { signedUrl, publicUrl } = await this.cloud.files.generatePresignedUploadUrl({ filename: file.name });
      await fetch(signedUrl, {
        method: "PUT",
        body: file,
        headers: { "Content-Type": file.type },
        mode: "cors"
      });
      this.uploadedUrls.set(id, publicUrl);
      attachment = {
        ...attachment,
        status: {
          type: "requires-action",
          reason: "composer-send"
        }
      };
      yield attachment;
    } catch {
      attachment = {
        ...attachment,
        status: {
          type: "incomplete",
          reason: "error"
        }
      };
      yield attachment;
    }
  }
  async remove(attachment) {
    this.uploadedUrls.delete(attachment.id);
  }
  async send(attachment) {
    const url = this.uploadedUrls.get(attachment.id);
    if (!url) throw new Error("Attachment not uploaded");
    this.uploadedUrls.delete(attachment.id);
    let content;
    if (attachment.type === "image") content = [{
      type: "image",
      image: url,
      filename: attachment.name
    }];
    else content = [{
      type: "file",
      data: url,
      mimeType: attachment.contentType ?? "",
      filename: attachment.name
    }];
    return {
      ...attachment,
      status: { type: "complete" },
      content
    };
  }
};

// node_modules/@assistant-ui/core/dist/react/runtimes/cloud/auiV0.js
function auiV0Encode(message) {
  const status = message.status?.type === "running" ? {
    type: "incomplete",
    reason: "cancelled"
  } : message.status;
  return {
    role: message.role,
    content: message.content.map((part) => {
      const type = part.type;
      switch (type) {
        case "text":
          return {
            type: "text",
            text: part.text
          };
        case "reasoning":
          return {
            type: "reasoning",
            text: part.text
          };
        case "source":
          if (part.sourceType === "url") return {
            type: "source",
            sourceType: "url",
            id: part.id,
            url: part.url,
            ...part.title != null ? { title: part.title } : void 0,
            ...part.providerMetadata != null ? { providerMetadata: part.providerMetadata } : void 0
          };
          return {
            type: "source",
            sourceType: "document",
            id: part.id,
            title: part.title,
            mediaType: part.mediaType,
            ...part.filename != null ? { filename: part.filename } : void 0,
            ...part.providerMetadata != null ? { providerMetadata: part.providerMetadata } : void 0
          };
        case "tool-call":
          if (!isJSONValue(part.result)) console.warn(`tool-call result is not JSON! ${JSON.stringify(part)}`);
          return {
            type: "tool-call",
            toolCallId: part.toolCallId,
            toolName: part.toolName,
            ...JSON.stringify(part.args) === part.argsText ? { args: part.args } : { argsText: part.argsText },
            ...part.result ? { result: part.result } : void 0,
            ...part.isError ? { isError: true } : void 0
          };
        case "image":
          return {
            type: "image",
            image: part.image
          };
        case "file":
          return {
            type: "file",
            data: part.data,
            mimeType: part.mimeType,
            ...part.filename ? { filename: part.filename } : void 0
          };
        default:
          throw new Error(`Message part type not supported by aui/v0: ${type}`);
      }
    }),
    metadata: message.metadata,
    ...status ? { status } : void 0
  };
}
function auiV0Decode(cloudMessage) {
  const payload = cloudMessage.content;
  const message = fromThreadMessageLike({
    id: cloudMessage.id,
    createdAt: cloudMessage.created_at,
    ...payload
  }, cloudMessage.id, {
    type: "complete",
    reason: "unknown"
  });
  return {
    parentId: cloudMessage.parent_id,
    message
  };
}

// node_modules/@assistant-ui/core/dist/react/runtimes/cloud/AssistantCloudThreadHistoryAdapter.js
var import_react27 = __toESM(require_react(), 1);
var globalPersistence = /* @__PURE__ */ new WeakMap();
var AssistantCloudThreadHistoryAdapter = class {
  cloudRef;
  aui;
  constructor(cloudRef, aui) {
    this.cloudRef = cloudRef;
    this.aui = aui;
  }
  get _persistence() {
    const key = this.aui.threadListItem();
    if (!globalPersistence.has(key)) globalPersistence.set(key, new CloudMessagePersistence(this.cloudRef.current));
    return globalPersistence.get(key);
  }
  withFormat(formatAdapter) {
    const adapter = this;
    const formatted = createFormattedPersistence(this._persistence, formatAdapter);
    return {
      async append(item) {
        const { remoteId } = await adapter.aui.threadListItem().initialize();
        await formatted.append(remoteId, item);
      },
      async update(item, localMessageId) {
        const remoteId = adapter.aui.threadListItem().getState().remoteId;
        if (!remoteId) return;
        await formatted.update?.(remoteId, item, localMessageId);
      },
      reportTelemetry(items, options) {
        const encodedRunMessages = items.map((item) => formatAdapter.encode(item));
        adapter._reportRunTelemetry(formatAdapter.format, encodedRunMessages, options);
      },
      async load() {
        const remoteId = adapter.aui.threadListItem().getState().remoteId;
        if (!remoteId) return { messages: [] };
        return formatted.load(remoteId);
      }
    };
  }
  async append({ parentId, message }) {
    const { remoteId } = await this.aui.threadListItem().initialize();
    const encoded = auiV0Encode(message);
    await this._persistence.append(remoteId, message.id, parentId, "aui/v0", encoded);
    if (this.cloudRef.current.telemetry.enabled) this._maybeReportRun(remoteId, "aui/v0", encoded);
  }
  async load() {
    const remoteId = this.aui.threadListItem().getState().remoteId;
    if (!remoteId) return { messages: [] };
    return { messages: (await this._persistence.load(remoteId, "aui/v0")).filter((m) => m.format === "aui/v0").map(auiV0Decode).reverse() };
  }
  _reportRunTelemetry(format, runMessages, options) {
    if (!this.cloudRef.current.telemetry.enabled) return;
    const remoteId = this.aui.threadListItem().getState().remoteId;
    if (!remoteId) return;
    const extracted = extractRunTelemetry(format, runMessages);
    if (!extracted) return;
    this._sendReport(remoteId, extracted, options?.durationMs, options?.stepTimestamps);
  }
  _maybeReportRun(remoteId, format, content) {
    const extracted = extractTelemetry(format, content);
    if (!extracted) return;
    this._sendReport(remoteId, extracted);
  }
  _sendReport(remoteId, data, durationMs, stepTimestamps) {
    const mergedSteps = mergeStepTimestamps(data.steps, stepTimestamps);
    const initial = {
      thread_id: remoteId,
      status: data.status,
      ...data.totalSteps != null ? { total_steps: data.totalSteps } : void 0,
      ...data.toolCalls ? { tool_calls: data.toolCalls } : void 0,
      ...mergedSteps ? { steps: mergedSteps } : void 0,
      ...data.inputTokens != null ? { input_tokens: data.inputTokens } : void 0,
      ...data.outputTokens != null ? { output_tokens: data.outputTokens } : void 0,
      ...data.reasoningTokens != null ? { reasoning_tokens: data.reasoningTokens } : void 0,
      ...data.cachedInputTokens != null ? { cached_input_tokens: data.cachedInputTokens } : void 0,
      ...durationMs != null ? { duration_ms: durationMs } : void 0,
      ...data.outputText != null ? { output_text: data.outputText } : void 0,
      ...data.metadata ? { metadata: data.metadata } : void 0,
      ...data.modelId ? { model_id: data.modelId } : void 0
    };
    const { beforeReport } = this.cloudRef.current.telemetry;
    const report = beforeReport ? beforeReport(initial) : initial;
    if (!report) return;
    this.cloudRef.current.runs.report(report).catch(() => {
    });
  }
};
var MAX_SPAN_CONTENT = 5e4;
function truncateStr(value) {
  if (value.length <= MAX_SPAN_CONTENT) return value;
  return value.slice(0, MAX_SPAN_CONTENT);
}
function safeStringify(value) {
  if (value == null) return void 0;
  try {
    return truncateStr(JSON.stringify(value));
  } catch {
    return;
  }
}
var BASE64_PATTERN = /^[A-Za-z0-9+/]{100,}={0,2}$/;
function summarizeMcpResult(value) {
  if (value == null) return void 0;
  try {
    const parsed = typeof value === "string" ? JSON.parse(value) : value;
    if (Array.isArray(parsed)) {
      const summarized = parsed.map((item) => {
        if (item && typeof item === "object" && item.type) {
          if ((item.type === "image" || item.type === "audio") && typeof item.data === "string" && BASE64_PATTERN.test(item.data.slice(0, 200))) {
            const sizeKB = (item.data.length * 3 / 4 / 1024).toFixed(1);
            return {
              ...item,
              data: `[${item.type}: ${sizeKB}KB]`
            };
          }
        }
        return item;
      });
      return truncateStr(JSON.stringify(summarized));
    }
  } catch {
  }
  return safeStringify(value);
}
function buildToolCall(toolName, toolCallId, args, result, argsText, toolSource) {
  const call = {
    tool_name: toolName,
    tool_call_id: toolCallId
  };
  const toolArgs = argsText ?? safeStringify(args);
  if (toolArgs !== void 0) call.tool_args = toolArgs;
  const toolResult = toolSource === "mcp" ? summarizeMcpResult(result) : safeStringify(result);
  if (toolResult !== void 0) call.tool_result = toolResult;
  if (toolSource) call.tool_source = toolSource;
  return call;
}
function mergeStepTimestamps(steps, timestamps) {
  if (!timestamps) return steps;
  if (!steps) return timestamps.map((t) => ({ ...t }));
  const len = Math.min(steps.length, timestamps.length);
  return steps.map((s, i) => ({
    ...s,
    ...i < len ? timestamps[i] : void 0
  }));
}
function extractTelemetry(format, content) {
  switch (format) {
    case "aui/v0":
      return extractAuiV0(content);
    case "ai-sdk/v6":
      return extractAiSdkV6(content);
    default:
      return null;
  }
}
function extractRunTelemetry(format, runMessages) {
  if (format === "ai-sdk/v6") return aggregateAiSdkV6RunSteps(runMessages);
  for (let i = runMessages.length - 1; i >= 0; i--) {
    const result = extractTelemetry(format, runMessages[i]);
    if (result) return result;
  }
  return null;
}
var AUI_STATUS_MAP = {
  error: "error",
  incomplete: "incomplete"
};
function extractAuiV0(content) {
  const msg = content;
  if (msg.role !== "assistant") return null;
  const toolCalls = msg.content?.filter((p) => p.type === "tool-call" && p.toolName && p.toolCallId).map((p) => buildToolCall(p.toolName, p.toolCallId, p.args, p.result, p.argsText));
  const textParts = msg.content?.filter((p) => p.type === "text" && p.text);
  const outputText = textParts && textParts.length > 0 ? truncateStr(textParts.map((p) => p.text).join("")) : void 0;
  const steps = msg.metadata?.steps;
  let inputTokens;
  let outputTokens;
  let reasoningTokens;
  let cachedInputTokens;
  if (steps && steps.length > 0) {
    let totalInput = 0;
    let totalOutput = 0;
    let totalReasoning = 0;
    let totalCachedInput = 0;
    let hasInput = false;
    let hasOutput = false;
    let hasReasoning = false;
    let hasCachedInput = false;
    for (const step of steps) {
      if (step.usage?.inputTokens != null) {
        totalInput += step.usage.inputTokens;
        hasInput = true;
      }
      if (step.usage?.outputTokens != null) {
        totalOutput += step.usage.outputTokens;
        hasOutput = true;
      }
      if (step.usage?.reasoningTokens != null) {
        totalReasoning += step.usage.reasoningTokens;
        hasReasoning = true;
      }
      if (step.usage?.cachedInputTokens != null) {
        totalCachedInput += step.usage.cachedInputTokens;
        hasCachedInput = true;
      }
    }
    inputTokens = hasInput ? totalInput : void 0;
    outputTokens = hasOutput ? totalOutput : void 0;
    reasoningTokens = hasReasoning ? totalReasoning : void 0;
    cachedInputTokens = hasCachedInput ? totalCachedInput : void 0;
  }
  const statusType = msg.status?.type;
  const status = statusType && AUI_STATUS_MAP[statusType] || "completed";
  const metadata = msg.metadata?.custom;
  const modelId = msg.metadata?.modelId ?? (typeof msg.metadata?.custom?.modelId === "string" ? msg.metadata.custom.modelId : void 0);
  const telemetrySteps = steps && steps.length > 1 ? steps.map((s) => ({
    ...s.usage?.inputTokens != null ? { input_tokens: s.usage.inputTokens } : void 0,
    ...s.usage?.outputTokens != null ? { output_tokens: s.usage.outputTokens } : void 0,
    ...s.usage?.reasoningTokens != null ? { reasoning_tokens: s.usage.reasoningTokens } : void 0,
    ...s.usage?.cachedInputTokens != null ? { cached_input_tokens: s.usage.cachedInputTokens } : void 0
  })) : void 0;
  return {
    status,
    ...toolCalls && toolCalls.length > 0 ? { toolCalls } : void 0,
    ...steps?.length ? { totalSteps: steps.length } : void 0,
    ...inputTokens != null ? { inputTokens } : void 0,
    ...outputTokens != null ? { outputTokens } : void 0,
    ...reasoningTokens != null ? { reasoningTokens } : void 0,
    ...cachedInputTokens != null ? { cachedInputTokens } : void 0,
    ...outputText != null ? { outputText } : void 0,
    ...metadata ? { metadata } : void 0,
    ...telemetrySteps ? { steps: telemetrySteps } : void 0,
    ...modelId ? { modelId } : void 0
  };
}
function isToolCallPart(p) {
  if (!p.toolCallId) return false;
  if (p.type === "tool-call" || p.type === "dynamic-tool") return !!p.toolName;
  return p.type.startsWith("tool-") || p.type.startsWith("dynamic-tool-");
}
function isDynamicToolPart(p) {
  return p.type === "dynamic-tool" || p.type.startsWith("dynamic-tool-");
}
function partToToolCall(p) {
  const toolSource = isDynamicToolPart(p) ? "mcp" : void 0;
  return buildToolCall(p.toolName ?? p.type.slice(5), p.toolCallId, p.args ?? p.input, p.result ?? p.output, void 0, toolSource);
}
function collectAiSdkV6Parts(parts) {
  const textParts = [];
  const toolCalls = [];
  const stepsData = [];
  let currentStepToolCalls = null;
  for (const p of parts) if (p.type === "step-start") {
    if (currentStepToolCalls !== null) stepsData.push({ tool_calls: currentStepToolCalls });
    currentStepToolCalls = [];
  } else if (p.type === "text" && p.text) textParts.push(p.text);
  else if (isToolCallPart(p)) {
    const tc = partToToolCall(p);
    toolCalls.push(tc);
    if (currentStepToolCalls !== null) currentStepToolCalls.push(tc);
  }
  if (currentStepToolCalls !== null) stepsData.push({ tool_calls: currentStepToolCalls });
  return {
    textParts,
    toolCalls,
    stepsData
  };
}
function extractModelId(metadata) {
  if (!metadata) return void 0;
  if (typeof metadata.modelId === "string") return metadata.modelId;
  const custom = metadata.custom;
  if (typeof custom?.modelId === "string") return custom.modelId;
}
function buildAiSdkV6Result(textParts, toolCalls, totalSteps, metadata, stepsData, usage) {
  const hasText = textParts.length > 0;
  const outputText = hasText ? truncateStr(textParts.join("")) : void 0;
  const modelId = extractModelId(metadata);
  const steps = stepsData && stepsData.length > 1 ? stepsData.map((s) => ({ ...s.tool_calls.length > 0 ? { tool_calls: s.tool_calls } : void 0 })) : void 0;
  return {
    status: hasText ? "completed" : "incomplete",
    ...toolCalls.length > 0 ? { toolCalls } : void 0,
    ...totalSteps > 0 ? { totalSteps } : void 0,
    ...usage?.inputTokens != null ? { inputTokens: usage.inputTokens } : void 0,
    ...usage?.outputTokens != null ? { outputTokens: usage.outputTokens } : void 0,
    ...usage?.reasoningTokens != null ? { reasoningTokens: usage.reasoningTokens } : void 0,
    ...usage?.cachedInputTokens != null ? { cachedInputTokens: usage.cachedInputTokens } : void 0,
    ...outputText != null ? { outputText } : void 0,
    ...metadata ? { metadata } : void 0,
    ...steps ? { steps } : void 0,
    ...modelId ? { modelId } : void 0
  };
}
function normalizeUsage(u) {
  const input = u.inputTokens ?? u.promptTokens;
  const output = u.outputTokens ?? u.completionTokens;
  if (input == null && output == null && u.reasoningTokens == null && u.cachedInputTokens == null) return;
  return {
    ...input != null ? { inputTokens: input } : void 0,
    ...output != null ? { outputTokens: output } : void 0,
    ...u.reasoningTokens != null ? { reasoningTokens: u.reasoningTokens } : void 0,
    ...u.cachedInputTokens != null ? { cachedInputTokens: u.cachedInputTokens } : void 0
  };
}
function extractAiSdkV6Usage(metadata) {
  const usage = metadata?.usage;
  if (usage) {
    const normalized = normalizeUsage(usage);
    if (normalized) return normalized;
  }
  const steps = metadata?.steps;
  if (steps && steps.length > 0) {
    let inputTokens = 0;
    let outputTokens = 0;
    let reasoningTokens = 0;
    let cachedInputTokens = 0;
    let hasInput = false;
    let hasOutput = false;
    let hasReasoning = false;
    let hasCachedInput = false;
    let hasAny = false;
    for (const s of steps) {
      if (!s.usage) continue;
      const n = normalizeUsage(s.usage);
      if (n) {
        if (n.inputTokens != null) {
          inputTokens += n.inputTokens;
          hasInput = true;
        }
        if (n.outputTokens != null) {
          outputTokens += n.outputTokens;
          hasOutput = true;
        }
        if (n.reasoningTokens != null) {
          reasoningTokens += n.reasoningTokens;
          hasReasoning = true;
        }
        if (n.cachedInputTokens != null) {
          cachedInputTokens += n.cachedInputTokens;
          hasCachedInput = true;
        }
        hasAny = true;
      }
    }
    if (hasAny) return {
      ...hasInput ? { inputTokens } : void 0,
      ...hasOutput ? { outputTokens } : void 0,
      ...hasReasoning ? { reasoningTokens } : void 0,
      ...hasCachedInput ? { cachedInputTokens } : void 0
    };
  }
}
function extractAiSdkV6(content) {
  const msg = content;
  if (msg.role !== "assistant") return null;
  const { textParts, toolCalls, stepsData } = collectAiSdkV6Parts(msg.parts ?? []);
  return buildAiSdkV6Result(textParts, toolCalls, stepsData.length, msg.metadata, stepsData, extractAiSdkV6Usage(msg.metadata));
}
function aggregateAiSdkV6RunSteps(stepMessages) {
  const allTextParts = [];
  const allToolCalls = [];
  const allStepsData = [];
  let hasAssistant = false;
  let metadata;
  let inputTokens = 0;
  let outputTokens = 0;
  let reasoningTokens = 0;
  let cachedInputTokens = 0;
  let hasInput = false;
  let hasOutput = false;
  let hasReasoning = false;
  let hasCachedInput = false;
  for (const content of stepMessages) {
    const msg = content;
    if (msg.role !== "assistant") continue;
    hasAssistant = true;
    const { textParts, toolCalls, stepsData } = collectAiSdkV6Parts(msg.parts ?? []);
    allTextParts.push(...textParts);
    allToolCalls.push(...toolCalls);
    allStepsData.push(...stepsData);
    if (msg.metadata) metadata = msg.metadata;
    const usage = extractAiSdkV6Usage(msg.metadata);
    if (usage) {
      if (usage.inputTokens != null) {
        inputTokens += usage.inputTokens;
        hasInput = true;
      }
      if (usage.outputTokens != null) {
        outputTokens += usage.outputTokens;
        hasOutput = true;
      }
      if (usage.reasoningTokens != null) {
        reasoningTokens += usage.reasoningTokens;
        hasReasoning = true;
      }
      if (usage.cachedInputTokens != null) {
        cachedInputTokens += usage.cachedInputTokens;
        hasCachedInput = true;
      }
    }
  }
  if (!hasAssistant) return null;
  return buildAiSdkV6Result(allTextParts, allToolCalls, allStepsData.length, metadata, allStepsData, {
    ...hasInput ? { inputTokens } : void 0,
    ...hasOutput ? { outputTokens } : void 0,
    ...hasReasoning ? { reasoningTokens } : void 0,
    ...hasCachedInput ? { cachedInputTokens } : void 0
  });
}
function useAssistantCloudThreadHistoryAdapter(cloudRef) {
  const aui = useAui();
  const [adapter] = (0, import_react27.useState)(() => new AssistantCloudThreadHistoryAdapter(cloudRef, aui));
  return adapter;
}

// node_modules/@assistant-ui/core/dist/react/runtimes/cloud/useCloudThreadListAdapter.js
var import_react28 = __toESM(require_react(), 1);
var import_jsx_runtime14 = __toESM(require_jsx_runtime(), 1);
var toCustom = (value) => isRecord(value) ? value : void 0;
var baseUrl = typeof process !== "undefined" && process?.env?.NEXT_PUBLIC_ASSISTANT_BASE_URL;
var autoCloud = baseUrl ? new AssistantCloud({
  baseUrl,
  anonymous: true
}) : void 0;
var useCloudThreadListAdapter = (adapter) => {
  const adapterRef = (0, import_react28.useRef)(adapter);
  (0, import_react28.useEffect)(() => {
    adapterRef.current = adapter;
  }, [adapter]);
  const unstable_Provider = (0, import_react28.useCallback)(function Provider({ children }) {
    const history = useAssistantCloudThreadHistoryAdapter({ get current() {
      return adapterRef.current.cloud ?? autoCloud;
    } });
    const cloudInstance = adapterRef.current.cloud ?? autoCloud;
    const attachments = (0, import_react28.useMemo)(() => new CloudFileAttachmentAdapter(cloudInstance), [cloudInstance]);
    return (0, import_jsx_runtime14.jsx)(RuntimeAdapterProvider, {
      adapters: (0, import_react28.useMemo)(() => ({
        history,
        attachments
      }), [history, attachments]),
      children
    });
  }, []);
  const cloud = adapter.cloud ?? autoCloud;
  if (!cloud) {
    const ref = adapterRef;
    const inMemory = new InMemoryThreadListAdapter();
    inMemory.initialize = async (threadId) => {
      return {
        remoteId: threadId,
        externalId: (await ref.current.create?.())?.externalId
      };
    };
    return inMemory;
  }
  return {
    list: async () => {
      const { threads } = await cloud.threads.list();
      return { threads: threads.map((t) => ({
        status: t.is_archived ? "archived" : "regular",
        remoteId: t.id,
        title: t.title,
        externalId: t.external_id ?? void 0,
        custom: toCustom(t.metadata)
      })) };
    },
    initialize: async () => {
      const t = await (adapter.create?.() ?? Promise.resolve());
      const external_id = t ? t.externalId : void 0;
      const { thread_id: remoteId } = await cloud.threads.create({
        last_message_at: /* @__PURE__ */ new Date(),
        external_id
      });
      return {
        externalId: external_id,
        remoteId
      };
    },
    rename: async (threadId, newTitle) => {
      return cloud.threads.update(threadId, { title: newTitle });
    },
    updateCustom: async (threadId, custom) => {
      return cloud.threads.update(threadId, { metadata: custom ?? null });
    },
    archive: async (threadId) => {
      return cloud.threads.update(threadId, { is_archived: true });
    },
    unarchive: async (threadId) => {
      return cloud.threads.update(threadId, { is_archived: false });
    },
    delete: async (threadId) => {
      await adapter.delete?.(threadId);
      return cloud.threads.delete(threadId);
    },
    generateTitle: async (threadId, messages) => {
      const filteredMessages = messages.map((msg) => ({
        ...msg,
        content: msg.content.filter((part) => part.type === "text" || part.type === "tool-call")
      }));
      return cloud.runs.stream({
        thread_id: threadId,
        assistant_id: "system/thread_title",
        messages: filteredMessages
      });
    },
    fetch: async (threadId) => {
      const thread = await cloud.threads.get(threadId);
      return {
        status: thread.is_archived ? "archived" : "regular",
        remoteId: thread.id,
        title: thread.title,
        externalId: thread.external_id ?? void 0,
        custom: toCustom(thread.metadata)
      };
    },
    unstable_Provider
  };
};

// node_modules/@assistant-ui/core/dist/react/primitives/generativeUI/GenerativeUI.js
var import_react29 = __toESM(require_react(), 1);
var import_jsx_runtime15 = __toESM(require_jsx_runtime(), 1);
var GenerativeUIRenderError = class extends Error {
  componentName;
  constructor(componentName, message = `Component "${componentName}" is not in the generative-ui allowlist.`) {
    super(message);
    this.name = "GenerativeUIRenderError";
    this.componentName = componentName;
  }
};
var isObjectNode = (node) => typeof node === "object" && node !== null;
var renderNode = (node, components, Fallback, path) => {
  if (node === void 0 || node === null) return null;
  if (typeof node === "string") return node;
  if (!isObjectNode(node) || !("component" in node)) {
    if (typeof process !== "undefined" && true) console.warn(`[generative-ui] Skipping malformed node at ${path}:`, node);
    return null;
  }
  const { component, props, children, key } = node;
  const Resolved = components[component];
  if (!Resolved) {
    if (Fallback) return (0, import_jsx_runtime15.jsx)(Fallback, {
      component,
      props
    }, key ?? path);
    throw new GenerativeUIRenderError(component);
  }
  const renderedChildren = children?.length ? children.map((child, i) => renderNode(child, components, Fallback, `${path}/${i}`)) : void 0;
  return (0, import_react29.createElement)(Resolved, {
    ...props ?? {},
    key: key ?? path
  }, ...renderedChildren ?? []);
};
var normalizeRoot = (spec) => {
  if (!spec || spec.root === void 0 || spec.root === null) return [];
  const root = spec.root;
  return Array.isArray(root) ? root : [root];
};
var GenerativeUIRender = ({ spec, components, Fallback }) => {
  return (0, import_jsx_runtime15.jsx)(import_jsx_runtime15.Fragment, { children: (0, import_react29.useMemo)(() => normalizeRoot(spec), [spec]).map((node, i) => renderNode(node, components, Fallback, `${i}`)) });
};
GenerativeUIRender.displayName = "GenerativeUIRender";
var MessagePrimitiveGenerativeUI = ({ components, spec, Fallback }) => {
  const storeSpec = useAuiState((s) => {
    const part = s.part;
    return part?.type === "generative-ui" ? part.spec : void 0;
  });
  const partSpec = spec ?? storeSpec;
  if (!partSpec) return null;
  return (0, import_jsx_runtime15.jsx)(GenerativeUIRender, {
    spec: partSpec,
    components,
    Fallback
  });
};
MessagePrimitiveGenerativeUI.displayName = "MessagePrimitive.GenerativeUI";

// node_modules/@assistant-ui/core/dist/react/utils/groupParts.js
var GROUPBY_MEMO_KEY = /* @__PURE__ */ Symbol.for("@assistant-ui/groupBy.memoKey");
var groupPartByType = (map) => {
  const lookup = map;
  const fn = ((part, context2) => {
    if (part.type === "tool-call") {
      const isMcpApp = isMcpAppUri(part.mcp?.app?.resourceUri);
      if ((isMcpApp || (context2?.toolUIs?.[part.toolName]?.[0]?.standalone ?? false)) && lookup["standalone-tool-call"] !== void 0) return lookup["standalone-tool-call"];
      if (isMcpApp && lookup["mcp-app"] !== void 0) return lookup["mcp-app"];
    }
    return lookup[part.type] ?? [];
  });
  const sortedEntries = Object.keys(map).sort().map((k) => [k, map[k]]);
  fn[GROUPBY_MEMO_KEY] = `groupPartByType:${JSON.stringify(sortedEntries)}`;
  return fn;
};
var makeChildNodeKey = (parent) => {
  const idx = parent.nextChildIdx++;
  return parent.nodeKey === "" ? String(idx) : `${parent.nodeKey}.${idx}`;
};
var buildGroupTree = (paths) => {
  const root = {
    key: "",
    nodeKey: "",
    indices: [],
    children: [],
    nextChildIdx: 0
  };
  const stack = [root];
  const closeTop = () => {
    const closing = stack.pop();
    stack[stack.length - 1].children.push({
      type: "group",
      key: closing.key,
      nodeKey: closing.nodeKey,
      indices: closing.indices,
      children: closing.children
    });
  };
  for (let i = 0; i < paths.length; i++) {
    const path = paths[i];
    let common = 0;
    while (common < stack.length - 1 && common < path.length && stack[common + 1].key === path[common]) common++;
    while (stack.length - 1 > common) closeTop();
    while (stack.length - 1 < path.length) {
      const parent = stack[stack.length - 1];
      stack.push({
        key: path[stack.length - 1],
        nodeKey: makeChildNodeKey(parent),
        indices: [],
        children: [],
        nextChildIdx: 0
      });
    }
    const top = stack[stack.length - 1];
    top.children.push({
      type: "part",
      index: i,
      nodeKey: makeChildNodeKey(top)
    });
    for (let s = 1; s < stack.length; s++) stack[s].indices.push(i);
  }
  while (stack.length > 1) closeTop();
  return root.children;
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useVoice.js
var import_react30 = __toESM(require_react(), 1);
var useVoiceState = () => {
  return useAuiState((s) => s.thread.voice);
};
var getServerVolume = () => 0;
var useVoiceVolume = () => {
  const thread = useAui().thread();
  return (0, import_react30.useSyncExternalStore)(thread.subscribeVoiceVolume, thread.getVoiceVolume, getServerVolume);
};
var useVoiceControls = () => {
  const aui = useAui();
  return {
    connect: (0, import_react30.useCallback)(() => {
      aui.thread().connectVoice();
    }, [aui]),
    disconnect: (0, import_react30.useCallback)(() => {
      aui.thread().disconnectVoice();
    }, [aui]),
    mute: (0, import_react30.useCallback)(() => {
      aui.thread().muteVoice();
    }, [aui]),
    unmute: (0, import_react30.useCallback)(() => {
      aui.thread().unmuteVoice();
    }, [aui])
  };
};

// node_modules/@assistant-ui/core/dist/runtimes/local/should-continue.js
var shouldContinue = (result, humanToolNames) => {
  if (humanToolNames === void 0) return result.status?.type === "requires-action" && result.status.reason === "tool-calls" && result.content.every((c) => c.type !== "tool-call" || !!c.result);
  return result.status?.type === "requires-action" && result.status.reason === "tool-calls" && result.content.every((c) => c.type !== "tool-call" || !!c.result || !humanToolNames.includes(c.toolName));
};

// node_modules/@assistant-ui/core/dist/runtimes/local/local-thread-runtime-core.js
var AbortError = class extends Error {
  name = "AbortError";
  detach;
  constructor(detach, message) {
    super(message);
    this.detach = detach;
  }
};
var LocalThreadRuntimeCore = class extends BaseThreadRuntimeCore {
  capabilities = {
    switchToBranch: true,
    switchBranchDuringRun: true,
    edit: true,
    reload: true,
    cancel: true,
    unstable_copy: true,
    speech: false,
    dictation: false,
    voice: false,
    attachments: false,
    feedback: false,
    queue: false
  };
  abortController = null;
  isDisabled = false;
  isSendDisabled = false;
  _isLoading = false;
  get isLoading() {
    return this._isLoading;
  }
  _suggestions = [];
  _suggestionsController = null;
  get suggestions() {
    return this._suggestions;
  }
  get adapters() {
    return this._options.adapters;
  }
  constructor(contextProvider, options) {
    super(contextProvider);
    this.__internal_setOptions(options);
  }
  _options;
  _lastRunConfig = {};
  _getThreadId;
  __internal_setGetThreadId(getThreadId) {
    this._getThreadId = getThreadId;
  }
  _getInitializePromise;
  __internal_setGetInitializePromise(getPromise) {
    this._getInitializePromise = getPromise;
  }
  get extras() {
  }
  __internal_setOptions(options) {
    if (this._options === options) return;
    this._options = options;
    let hasUpdates = false;
    const canSpeak = options.adapters?.speech !== void 0;
    if (this.capabilities.speech !== canSpeak) {
      this.capabilities.speech = canSpeak;
      hasUpdates = true;
    }
    const canDictate = options.adapters?.dictation !== void 0;
    if (this.capabilities.dictation !== canDictate) {
      this.capabilities.dictation = canDictate;
      hasUpdates = true;
    }
    const canVoice = options.adapters?.voice !== void 0;
    if (this.capabilities.voice !== canVoice) {
      this.capabilities.voice = canVoice;
      hasUpdates = true;
    }
    const canAttach = options.adapters?.attachments !== void 0;
    if (this.capabilities.attachments !== canAttach) {
      this.capabilities.attachments = canAttach;
      hasUpdates = true;
    }
    const canFeedback = options.adapters?.feedback !== void 0;
    if (this.capabilities.feedback !== canFeedback) {
      this.capabilities.feedback = canFeedback;
      hasUpdates = true;
    }
    if (hasUpdates) this._notifySubscribers();
  }
  _loadPromise;
  __internal_load() {
    if (this._loadPromise) return this._loadPromise;
    const promise = this.adapters.history?.load() ?? Promise.resolve(null);
    this._isLoading = true;
    this._notifySubscribers();
    this._loadPromise = promise.then((repo) => {
      if (!repo) return;
      this.repository.import(repo);
      if (repo.messages.length > 0) this.ensureInitialized();
      this._notifySubscribers();
      const resume = this.adapters.history?.resume?.bind(this.adapters.history);
      if (repo.unstable_resume && resume) this.startRun({
        parentId: this.repository.headId,
        sourceId: this.repository.headId,
        runConfig: this._lastRunConfig
      }, resume).catch(() => {
      });
    }).finally(() => {
      this._isLoading = false;
      this._notifySubscribers();
    });
    return this._loadPromise;
  }
  async append(message) {
    this.ensureInitialized();
    const initPromise = this._getInitializePromise?.();
    if (initPromise) await initPromise;
    const newMessage = fromThreadMessageLike(message, generateId(), {
      type: "complete",
      reason: "unknown"
    });
    this.repository.addOrUpdateMessage(message.parentId, newMessage);
    this._options.adapters.history?.append({
      parentId: message.parentId,
      message: newMessage,
      ...message.runConfig !== void 0 && { runConfig: message.runConfig }
    });
    if (message.startRun ?? message.role === "user") await this.startRun({
      parentId: newMessage.id,
      sourceId: message.sourceId,
      runConfig: message.runConfig ?? {}
    });
    else {
      this.repository.resetHead(newMessage.id);
      this._notifySubscribers();
    }
  }
  resumeRun({ stream, ...startConfig }) {
    if (!stream) throw new Error("You must pass a stream parameter to resume runs.");
    return this.startRun(startConfig, stream);
  }
  exportExternalState() {
    throw new Error("Runtime does not support exporting external states.");
  }
  importExternalState() {
    throw new Error("Runtime does not support importing external states.");
  }
  async startRun({ parentId, runConfig }, runCallback) {
    this.ensureInitialized();
    let message = {
      id: generateId(),
      role: "assistant",
      status: { type: "running" },
      content: [],
      metadata: {
        unstable_state: this.state,
        unstable_annotations: [],
        unstable_data: [],
        steps: [],
        custom: {}
      },
      createdAt: /* @__PURE__ */ new Date()
    };
    this._notifyEventSubscribers("runStart", {});
    try {
      this._suggestions = [];
      this._suggestionsController?.abort();
      this._suggestionsController = null;
      this._notifySubscribers();
      do {
        message = await this.performRoundtrip(parentId, message, runConfig, runCallback);
        runCallback = void 0;
      } while (shouldContinue(message, this._options.unstable_humanToolNames));
    } finally {
      this._notifyEventSubscribers("runEnd", {});
    }
    this._suggestionsController = new AbortController();
    const signal = this._suggestionsController.signal;
    if (this.adapters.suggestion && message.status?.type !== "requires-action") {
      const promiseOrGenerator = this.adapters.suggestion?.generate({ messages: this.messages });
      if (Symbol.asyncIterator in promiseOrGenerator) for await (const r of promiseOrGenerator) {
        if (signal.aborted) break;
        this._suggestions = r;
        this._notifySubscribers();
      }
      else {
        const result = await promiseOrGenerator;
        if (signal.aborted) return;
        this._suggestions = result;
        this._notifySubscribers();
      }
    }
  }
  async performRoundtrip(parentId, message, runConfig, runCallback) {
    const messages = parentId ? this.repository.getMessages(parentId) : [];
    this.abortController?.abort();
    this.abortController = new AbortController();
    const initialContent = message.content;
    const initialAnnotations = message.metadata?.unstable_annotations;
    const initialData = message.metadata?.unstable_data;
    const initialSteps = message.metadata?.steps;
    const initialCustom = message.metadata?.custom;
    const updateMessage = (m) => {
      const newSteps = m.metadata?.steps;
      const steps = newSteps ? [...initialSteps ?? [], ...newSteps] : void 0;
      const newAnnotations = m.metadata?.unstable_annotations;
      const newData = m.metadata?.unstable_data;
      const annotations = newAnnotations ? [...initialAnnotations ?? [], ...newAnnotations] : void 0;
      const data = newData ? [...initialData ?? [], ...newData] : void 0;
      message = {
        ...message,
        ...m.content ? { content: [...initialContent, ...m.content ?? []] } : void 0,
        status: m.status ?? message.status,
        ...m.metadata ? { metadata: {
          ...message.metadata,
          ...m.metadata.unstable_state ? { unstable_state: m.metadata.unstable_state } : void 0,
          ...annotations ? { unstable_annotations: annotations } : void 0,
          ...data ? { unstable_data: data } : void 0,
          ...steps ? { steps } : void 0,
          ...m.metadata?.timing ? { timing: m.metadata.timing } : void 0,
          ...m.metadata?.custom ? { custom: {
            ...initialCustom ?? {},
            ...m.metadata.custom
          } } : void 0
        } } : void 0
      };
      this.repository.addOrUpdateMessage(parentId, message);
      this._notifySubscribers();
    };
    const maxSteps = this._options.maxSteps ?? 2;
    if ((message.metadata?.steps?.length ?? 0) >= maxSteps) {
      updateMessage({ status: {
        type: "incomplete",
        reason: "tool-calls"
      } });
      return message;
    } else {
      updateMessage({ status: { type: "running" } });
      this.repository.resetHead(message.id);
      this._notifySubscribers();
    }
    try {
      this._lastRunConfig = runConfig ?? {};
      const context2 = this.getModelContext();
      runCallback = runCallback ?? this.adapters.chatModel.run.bind(this.adapters.chatModel);
      const abortSignal = this.abortController.signal;
      const threadId = this._getThreadId?.();
      const promiseOrGenerator = runCallback({
        messages,
        runConfig: this._lastRunConfig,
        abortSignal,
        context: context2,
        unstable_assistantMessageId: message.id,
        unstable_threadId: threadId,
        unstable_parentId: parentId,
        unstable_getMessage() {
          return message;
        }
      });
      if (Symbol.asyncIterator in promiseOrGenerator) for await (const r of promiseOrGenerator) {
        if (abortSignal.aborted) {
          updateMessage({ status: {
            type: "incomplete",
            reason: "cancelled"
          } });
          break;
        }
        updateMessage(r);
      }
      else updateMessage(await promiseOrGenerator);
      if (message.status.type === "running") updateMessage({ status: {
        type: "complete",
        reason: "unknown"
      } });
    } catch (e) {
      if (e instanceof AbortError) updateMessage({ status: {
        type: "incomplete",
        reason: "cancelled"
      } });
      else if (e instanceof Error && e.name === "AbortError") updateMessage({ status: {
        type: "incomplete",
        reason: "cancelled"
      } });
      else {
        updateMessage({ status: {
          type: "incomplete",
          reason: "error",
          error: e instanceof Error ? e.message : `[${typeof e}] ${new String(e).toString()}`
        } });
        throw e;
      }
    } finally {
      this.abortController = null;
      if (message.status.type === "complete" || message.status.type === "incomplete") await this._options.adapters.history?.append({
        parentId,
        message,
        runConfig: this._lastRunConfig
      });
    }
    return message;
  }
  detach() {
    const error = new AbortError(true);
    this.abortController?.abort(error);
    this.abortController = null;
  }
  cancelRun() {
    const error = new AbortError(false);
    this.abortController?.abort(error);
    this.abortController = null;
  }
  addToolResult({ messageId, toolCallId, result, isError, artifact }) {
    const messageData = this.repository.getMessage(messageId);
    const { parentId } = messageData;
    let { message } = messageData;
    if (message.role !== "assistant") throw new Error("Tried to add tool result to non-assistant message");
    let added = false;
    let found = false;
    const newContent = message.content.map((c) => {
      if (c.type !== "tool-call") return c;
      if (c.toolCallId !== toolCallId) return c;
      found = true;
      if (!c.result) added = true;
      return {
        ...c,
        result,
        artifact,
        isError
      };
    });
    if (!found) throw new Error("Tried to add tool result to non-existing tool call");
    message = {
      ...message,
      content: newContent
    };
    this.repository.addOrUpdateMessage(parentId, message);
    if (added && shouldContinue(message, this._options.unstable_humanToolNames)) this.performRoundtrip(parentId, message, this._lastRunConfig).catch(() => {
    });
  }
  resumeToolCall(_options) {
    throw new Error("Local runtime does not support resuming tool calls.");
  }
  respondToToolApproval(_options) {
    throw new Error("Local runtime does not support tool approvals.");
  }
};

// node_modules/@assistant-ui/core/dist/runtimes/local/local-thread-list-runtime-core.js
var EMPTY_ARRAY4 = Object.freeze([]);
var DEFAULT_THREAD_ID2 = "__DEFAULT_ID__";
var DEFAULT_THREAD_DATA2 = Object.freeze({ [DEFAULT_THREAD_ID2]: {
  id: DEFAULT_THREAD_ID2,
  remoteId: void 0,
  externalId: void 0,
  status: "regular",
  title: void 0
} });
var LocalThreadListRuntimeCore = class extends BaseSubscribable {
  _mainThread;
  constructor(_threadFactory) {
    super();
    this._mainThread = _threadFactory();
  }
  get isLoading() {
    return false;
  }
  getMainThreadRuntimeCore() {
    return this._mainThread;
  }
  get newThreadId() {
  }
  get threadIds() {
    return EMPTY_ARRAY4;
  }
  get archivedThreadIds() {
    return EMPTY_ARRAY4;
  }
  get mainThreadId() {
    return DEFAULT_THREAD_ID2;
  }
  get threadItems() {
    return DEFAULT_THREAD_DATA2;
  }
  getThreadRuntimeCore() {
    throw new Error("Method not implemented.");
  }
  getLoadThreadsPromise() {
    return Promise.resolve();
  }
  getItemById(threadId) {
    if (threadId === this.mainThreadId) return {
      status: "regular",
      id: this.mainThreadId,
      remoteId: this.mainThreadId,
      externalId: void 0,
      title: void 0,
      isMain: true
    };
    throw new Error("Method not implemented");
  }
  async switchToThread() {
    throw new Error("Method not implemented.");
  }
  switchToNewThread() {
    throw new Error("Method not implemented.");
  }
  rename() {
    throw new Error("Method not implemented.");
  }
  archive() {
    throw new Error("Method not implemented.");
  }
  detach() {
    throw new Error("Method not implemented.");
  }
  unarchive() {
    throw new Error("Method not implemented.");
  }
  delete() {
    throw new Error("Method not implemented.");
  }
  initialize(threadId) {
    return Promise.resolve({
      remoteId: threadId,
      externalId: void 0
    });
  }
  generateTitle() {
    throw new Error("Method not implemented.");
  }
};

// node_modules/@assistant-ui/core/dist/runtimes/local/local-runtime-core.js
var LocalRuntimeCore = class extends BaseAssistantRuntimeCore {
  threads;
  Provider = void 0;
  _options;
  constructor(options, initialMessages) {
    super();
    this._options = options;
    this.threads = new LocalThreadListRuntimeCore(() => {
      return new LocalThreadRuntimeCore(this._contextProvider, this._options);
    });
    if (initialMessages) this.threads.getMainThreadRuntimeCore().import(ExportedMessageRepository.fromArray(initialMessages));
  }
};

// node_modules/@assistant-ui/core/dist/react/runtimes/useLocalRuntime.js
var import_react31 = __toESM(require_react(), 1);
var useLocalThreadRuntime = (chatModel, { initialMessages, ...options }) => {
  const { modelContext, ...threadListAdapters } = useRuntimeAdapters() ?? {};
  const opt = {
    ...options,
    adapters: {
      ...threadListAdapters,
      ...options.adapters,
      chatModel
    }
  };
  const [runtime] = (0, import_react31.useState)(() => new LocalRuntimeCore(opt, initialMessages));
  const threadIdRef = (0, import_react31.useRef)(void 0);
  threadIdRef.current = useAuiState((s) => s.threadListItem.remoteId);
  (0, import_react31.useEffect)(() => {
    runtime.threads.getMainThreadRuntimeCore().__internal_setGetThreadId(() => threadIdRef.current);
  }, [runtime]);
  (0, import_react31.useEffect)(() => {
    return () => {
      runtime.threads.getMainThreadRuntimeCore().detach();
    };
  }, [runtime]);
  (0, import_react31.useEffect)(() => {
    runtime.threads.getMainThreadRuntimeCore().__internal_setOptions(opt);
    runtime.threads.getMainThreadRuntimeCore().__internal_load();
  });
  (0, import_react31.useEffect)(() => {
    if (!modelContext) return void 0;
    return runtime.registerModelContextProvider(modelContext);
  }, [modelContext, runtime]);
  return (0, import_react31.useMemo)(() => new AssistantRuntimeImpl(runtime), [runtime]);
};
var splitLocalRuntimeOptions = (options) => {
  const { cloud, initialMessages, maxSteps, adapters, unstable_humanToolNames, ...rest } = options;
  return {
    localRuntimeOptions: {
      cloud,
      initialMessages,
      maxSteps,
      adapters,
      unstable_humanToolNames
    },
    otherOptions: rest
  };
};
var useLocalRuntime = (chatModel, { cloud, ...options } = {}) => {
  return useRemoteThreadListRuntime({
    runtimeHook: function RuntimeHook() {
      return useLocalThreadRuntime(chatModel, options);
    },
    adapter: useCloudThreadListAdapter({ cloud }),
    allowNesting: true
  });
};

// node_modules/@assistant-ui/core/dist/store/runtime-clients/thread-list-runtime-client.js
var ThreadListItemClientById = resource(({ runtime, id }) => {
  return tapResource(ThreadListItemClient({ runtime: tapMemo(() => runtime.getItemById(id), [runtime, id]) }));
});
var ThreadListClient = resource(({ runtime, __internal_assistantRuntime }) => {
  const runtimeState = tapSubscribable(runtime);
  const main = tapClientResource(ThreadClient({ runtime: runtime.main }));
  const threadItems = tapClientLookup(() => Object.keys(runtimeState.threadItems).map((id) => withKey(id, ThreadListItemClientById({
    runtime,
    id
  }))), [runtimeState.threadItems, runtime]);
  const state = tapMemo(() => {
    return {
      mainThreadId: runtimeState.mainThreadId,
      newThreadId: runtimeState.newThreadId ?? null,
      isLoading: runtimeState.isLoading,
      isLoadingMore: runtimeState.isLoadingMore,
      hasMore: runtimeState.hasMore,
      threadIds: runtimeState.threadIds,
      archivedThreadIds: runtimeState.archivedThreadIds,
      threadItems: threadItems.state,
      main: main.state
    };
  }, [
    runtimeState,
    threadItems.state,
    main.state
  ]);
  return {
    getState: () => state,
    thread: () => main.methods,
    item: (threadIdOrOptions) => {
      if (threadIdOrOptions === "main") return threadItems.get({ key: state.mainThreadId });
      if ("id" in threadIdOrOptions) return threadItems.get({ key: threadIdOrOptions.id });
      const { index, archived = false } = threadIdOrOptions;
      const id = archived ? state.archivedThreadIds[index] : state.threadIds[index];
      return threadItems.get({ key: id });
    },
    switchToThread: async (threadId, options) => {
      await runtime.switchToThread(threadId, options);
    },
    switchToNewThread: async () => {
      await runtime.switchToNewThread();
    },
    getLoadThreadsPromise: () => runtime.getLoadThreadsPromise(),
    reload: () => runtime.reload(),
    loadMore: () => runtime.loadMore(),
    __internal_getAssistantRuntime: () => __internal_assistantRuntime
  };
});

// node_modules/@assistant-ui/core/dist/store/clients/runtime-adapter.js
var RuntimeAdapterResource = resource((runtime) => {
  const clientRef = tapAssistantClientRef();
  tapEffect(() => {
    return runtime.registerModelContextProvider(clientRef.current.modelContext());
  }, [runtime, clientRef]);
  return tapResource(ThreadListClient({
    runtime: runtime.threads,
    __internal_assistantRuntime: runtime
  }));
});
var baseRuntimeAdapterTransformScopes = (scopes, parent) => {
  scopes.thread ??= Derived({
    source: "threads",
    query: { type: "main" },
    get: (aui) => aui.threads().thread("main")
  });
  scopes.threadListItem ??= Derived({
    source: "threads",
    query: { type: "main" },
    get: (aui) => aui.threads().item("main")
  });
  scopes.composer ??= Derived({
    source: "thread",
    query: {},
    get: (aui) => aui.threads().thread("main").composer()
  });
  if (!scopes.modelContext && parent.modelContext.source === null) scopes.modelContext = ModelContext();
  if (!scopes.suggestions && parent.suggestions.source === null) scopes.suggestions = Suggestions();
};

// node_modules/@assistant-ui/core/dist/react/RuntimeAdapter.js
var RuntimeAdapter = resource((runtime) => tapResource(RuntimeAdapterResource(runtime)));
attachTransformScopes(RuntimeAdapter, (scopes, parent) => {
  baseRuntimeAdapterTransformScopes(scopes, parent);
  if (!scopes.tools && parent.tools.source === null) scopes.tools = Tools({});
  if (!scopes.dataRenderers && parent.dataRenderers.source === null) scopes.dataRenderers = DataRenderers();
});

// node_modules/@assistant-ui/core/dist/react/AssistantProvider.js
var import_react32 = __toESM(require_react(), 1);
var import_jsx_runtime16 = __toESM(require_jsx_runtime(), 1);
var getRenderComponent = (runtime) => {
  return runtime._core?.RenderComponent;
};
var AssistantProviderBase = (0, import_react32.memo)(({ runtime, aui: parent = null, children }) => {
  const aui = useAui({ threads: RuntimeAdapter(runtime) }, { parent });
  const RenderComponent = getRenderComponent(runtime);
  return (0, import_jsx_runtime16.jsxs)(AuiProvider, {
    value: aui,
    children: [RenderComponent && (0, import_jsx_runtime16.jsx)(RenderComponent, {}), children]
  });
});

// node_modules/@assistant-ui/core/dist/react/AssistantRuntimeProvider.js
var import_react33 = __toESM(require_react(), 1);
var import_jsx_runtime17 = __toESM(require_jsx_runtime(), 1);
var AssistantRuntimeProvider = (0, import_react33.memo)(({ runtime, aui, children }) => {
  return (0, import_jsx_runtime17.jsx)(AssistantProviderBase, {
    runtime,
    aui: aui ?? null,
    children
  });
});

// node_modules/@assistant-ui/core/dist/react/providers/ChainOfThoughtPartByIndexProvider.js
var import_jsx_runtime18 = __toESM(require_jsx_runtime(), 1);
var ChainOfThoughtPartByIndexProvider = ({ index, children }) => {
  return (0, import_jsx_runtime18.jsx)(AuiProvider, {
    value: useAui({ part: Derived({
      source: "chainOfThought",
      query: {
        type: "index",
        index
      },
      get: (aui) => aui.chainOfThought().part({ index })
    }) }),
    children
  });
};

// node_modules/@assistant-ui/core/dist/react/providers/QueueItemByIndexProvider.js
var import_jsx_runtime19 = __toESM(require_jsx_runtime(), 1);
var QueueItemByIndexProvider = ({ index, children }) => {
  return (0, import_jsx_runtime19.jsx)(AuiProvider, {
    value: useAui({ queueItem: Derived({
      source: "composer",
      query: { index },
      get: (aui) => aui.composer().queueItem({ index })
    }) }),
    children
  });
};

// node_modules/@assistant-ui/core/dist/react/adapters/LocalStorageThreadListAdapter.js
var import_react34 = __toESM(require_react(), 1);
var import_jsx_runtime20 = __toESM(require_jsx_runtime(), 1);

// node_modules/@assistant-ui/core/dist/react/primitives/thread/ThreadMessages.js
var import_react35 = __toESM(require_react(), 1);
var import_jsx_runtime21 = __toESM(require_jsx_runtime(), 1);
var isComponentsSame = (prev, next) => {
  return prev.Message === next.Message && prev.EditComposer === next.EditComposer && prev.UserEditComposer === next.UserEditComposer && prev.AssistantEditComposer === next.AssistantEditComposer && prev.SystemEditComposer === next.SystemEditComposer && prev.UserMessage === next.UserMessage && prev.AssistantMessage === next.AssistantMessage && prev.SystemMessage === next.SystemMessage;
};
var DEFAULT_SYSTEM_MESSAGE = () => null;
var getComponent = (components, role, isEditing) => {
  switch (role) {
    case "user":
      if (isEditing) return components.UserEditComposer ?? components.EditComposer ?? components.UserMessage ?? components.Message;
      else return components.UserMessage ?? components.Message;
    case "assistant":
      if (isEditing) return components.AssistantEditComposer ?? components.EditComposer ?? components.AssistantMessage ?? components.Message;
      else return components.AssistantMessage ?? components.Message;
    case "system":
      if (isEditing) return components.SystemEditComposer ?? components.EditComposer ?? components.SystemMessage ?? components.Message;
      else return components.SystemMessage ?? components.Message ?? DEFAULT_SYSTEM_MESSAGE;
    default:
      throw new Error(`Unknown message role: ${role}`);
  }
};
var ThreadMessageComponent = ({ components }) => {
  return (0, import_jsx_runtime21.jsx)(getComponent(components, useAuiState((s) => s.message.role), useAuiState((s) => s.message.composer.isEditing)), {});
};
var ThreadPrimitiveMessageByIndex = (0, import_react35.memo)(({ index, components }) => {
  return (0, import_jsx_runtime21.jsx)(MessageByIndexProvider, {
    index,
    children: (0, import_jsx_runtime21.jsx)(ThreadMessageComponent, { components })
  });
}, (prev, next) => prev.index === next.index && isComponentsSame(prev.components, next.components));
ThreadPrimitiveMessageByIndex.displayName = "ThreadPrimitive.MessageByIndex";
var ThreadPrimitiveMessagesInner = ({ children }) => {
  const messagesLength = useAuiState((s) => s.thread.messages.length);
  return (0, import_react35.useMemo)(() => {
    if (messagesLength === 0) return null;
    return Array.from({ length: messagesLength }, (_, index) => (0, import_jsx_runtime21.jsx)(MessageByIndexProvider, {
      index,
      children: (0, import_jsx_runtime21.jsx)(RenderChildrenWithAccessor, {
        getItemState: (aui) => aui.thread().message({ index }).getState(),
        children: (getItem) => children({ get message() {
          return getItem();
        } })
      })
    }, index));
  }, [messagesLength, children]);
};
var ThreadPrimitiveMessagesImpl = ({ components, children }) => {
  if (components) return (0, import_jsx_runtime21.jsx)(ThreadPrimitiveMessagesInner, { children: () => (0, import_jsx_runtime21.jsx)(ThreadMessageComponent, { components }) });
  return (0, import_jsx_runtime21.jsx)(ThreadPrimitiveMessagesInner, { children });
};
ThreadPrimitiveMessagesImpl.displayName = "ThreadPrimitive.Messages";
var ThreadPrimitiveMessages = (0, import_react35.memo)(ThreadPrimitiveMessagesImpl, (prev, next) => {
  if (prev.children || next.children) return prev.children === next.children;
  return isComponentsSame(prev.components, next.components);
});

// node_modules/@assistant-ui/core/dist/react/utils/getMessageQuote.js
var getMessageQuote = (state) => {
  const metadata = state.message.metadata;
  if (!metadata || typeof metadata !== "object") return void 0;
  return metadata.custom?.quote;
};

// node_modules/@assistant-ui/core/dist/react/primitives/message/MessageParts.js
var import_react36 = __toESM(require_react(), 1);
var import_jsx_runtime22 = __toESM(require_jsx_runtime(), 1);
var createGroupState = (groupType) => {
  let start = -1;
  return {
    startGroup: (index) => {
      if (start === -1) start = index;
    },
    endGroup: (endIndex, ranges) => {
      if (start !== -1) {
        ranges.push({
          type: groupType,
          startIndex: start,
          endIndex
        });
        start = -1;
      }
    },
    finalize: (endIndex, ranges) => {
      if (start !== -1) ranges.push({
        type: groupType,
        startIndex: start,
        endIndex
      });
    }
  };
};
var groupMessageParts = (messageTypes, useChainOfThought) => {
  const ranges = [];
  if (useChainOfThought) {
    const chainOfThoughtGroup = createGroupState("chainOfThoughtGroup");
    for (let i = 0; i < messageTypes.length; i++) {
      const type = messageTypes[i];
      if (type === "tool-call" || type === "reasoning") chainOfThoughtGroup.startGroup(i);
      else {
        chainOfThoughtGroup.endGroup(i - 1, ranges);
        ranges.push({
          type: "single",
          index: i
        });
      }
    }
    chainOfThoughtGroup.finalize(messageTypes.length - 1, ranges);
  } else {
    const toolGroup = createGroupState("toolGroup");
    const reasoningGroup = createGroupState("reasoningGroup");
    for (let i = 0; i < messageTypes.length; i++) {
      const type = messageTypes[i];
      if (type === "tool-call") {
        reasoningGroup.endGroup(i - 1, ranges);
        toolGroup.startGroup(i);
      } else if (type === "reasoning") {
        toolGroup.endGroup(i - 1, ranges);
        reasoningGroup.startGroup(i);
      } else {
        toolGroup.endGroup(i - 1, ranges);
        reasoningGroup.endGroup(i - 1, ranges);
        ranges.push({
          type: "single",
          index: i
        });
      }
    }
    toolGroup.finalize(messageTypes.length - 1, ranges);
    reasoningGroup.finalize(messageTypes.length - 1, ranges);
  }
  return ranges;
};
var useMessagePartsGroups = (useChainOfThought) => {
  const messageTypes = useAuiState(useShallow((s) => s.message.parts.map((c) => c.type)));
  return (0, import_react36.useMemo)(() => {
    if (messageTypes.length === 0) return [];
    return groupMessageParts(messageTypes, useChainOfThought);
  }, [messageTypes, useChainOfThought]);
};
var ToolUIDisplay = ({ Fallback, ...props }) => {
  const Render = useAuiState((s) => s.tools.toolUIs[props.toolName]?.[0]?.render ?? Fallback);
  if (!Render) return null;
  return (0, import_jsx_runtime22.jsx)(Render, { ...props });
};
var getDataRenderer = (dataRenderers, name, inlineFallback) => {
  const named = dataRenderers.renderers[name]?.[0];
  if (named) return named;
  return dataRenderers.fallbacks[0] ?? inlineFallback;
};
var DataUIDisplay = ({ Fallback, ...props }) => {
  const Render = useAuiState((s) => getDataRenderer(s.dataRenderers, props.name, Fallback));
  if (!Render) return null;
  return (0, import_jsx_runtime22.jsx)(Render, { ...props });
};
var defaultComponents = {
  Text: () => null,
  Reasoning: () => null,
  Source: () => null,
  Image: () => null,
  File: () => null,
  Unstable_Audio: () => null,
  ToolGroup: ({ children }) => children,
  ReasoningGroup: ({ children }) => children
};
var MessagePartComponent = ({ components: { Text = defaultComponents.Text, Reasoning = defaultComponents.Reasoning, Image = defaultComponents.Image, Source = defaultComponents.Source, File: File2 = defaultComponents.File, Unstable_Audio: Audio = defaultComponents.Unstable_Audio, tools = {}, data, generativeUI } = {} }) => {
  const aui = useAui();
  const part = useAuiState((s) => s.part);
  const type = part.type;
  if (type === "tool-call") {
    const addResult = aui.part().addToolResult;
    const resume = aui.part().resumeToolCall;
    const respondToApproval = aui.part().respondToToolApproval;
    if ("Override" in tools) return (0, import_jsx_runtime22.jsx)(tools.Override, {
      ...part,
      addResult,
      resume,
      respondToApproval
    });
    const Tool = tools.by_name?.[part.toolName] ?? tools.Fallback;
    return (0, import_jsx_runtime22.jsx)(ToolUIDisplay, {
      ...part,
      Fallback: Tool,
      addResult,
      resume,
      respondToApproval
    });
  }
  if (part.status?.type === "requires-action") throw new Error("Encountered unexpected requires-action status");
  switch (type) {
    case "text":
      return (0, import_jsx_runtime22.jsx)(Text, { ...part });
    case "reasoning":
      return (0, import_jsx_runtime22.jsx)(Reasoning, { ...part });
    case "source":
      return (0, import_jsx_runtime22.jsx)(Source, { ...part });
    case "image":
      return (0, import_jsx_runtime22.jsx)(Image, { ...part });
    case "file":
      return (0, import_jsx_runtime22.jsx)(File2, { ...part });
    case "audio":
      return (0, import_jsx_runtime22.jsx)(Audio, { ...part });
    case "data": {
      const Data = data?.by_name?.[part.name] ?? data?.Fallback;
      return (0, import_jsx_runtime22.jsx)(DataUIDisplay, {
        ...part,
        Fallback: Data
      });
    }
    case "generative-ui":
      if (!generativeUI?.components) {
        if (typeof process !== "undefined" && true) console.warn("MessagePrimitive.Parts received a generative-ui part but no `components.generativeUI.components` allowlist was provided. Pass an allowlist or render with <MessagePrimitive.GenerativeUI />.");
        return null;
      }
      return (0, import_jsx_runtime22.jsx)(GenerativeUIRender, {
        spec: part.spec,
        components: generativeUI.components,
        Fallback: generativeUI.Fallback
      });
    default:
      console.warn(`Unknown message part type: ${type}`);
      return null;
  }
};
var MessagePrimitivePartByIndex = (0, import_react36.memo)(({ index, components }) => {
  return (0, import_jsx_runtime22.jsx)(PartByIndexProvider, {
    index,
    children: (0, import_jsx_runtime22.jsx)(MessagePartComponent, { components })
  });
}, (prev, next) => prev.index === next.index && prev.components?.Text === next.components?.Text && prev.components?.Reasoning === next.components?.Reasoning && prev.components?.Source === next.components?.Source && prev.components?.Image === next.components?.Image && prev.components?.File === next.components?.File && prev.components?.Unstable_Audio === next.components?.Unstable_Audio && prev.components?.tools === next.components?.tools && prev.components?.data === next.components?.data && prev.components?.generativeUI === next.components?.generativeUI && prev.components?.ToolGroup === next.components?.ToolGroup && prev.components?.ReasoningGroup === next.components?.ReasoningGroup);
MessagePrimitivePartByIndex.displayName = "MessagePrimitive.PartByIndex";
var EmptyPartFallback = ({ status, component: Component }) => {
  return (0, import_jsx_runtime22.jsx)(TextMessagePartProvider, {
    text: "",
    isRunning: status.type === "running",
    children: (0, import_jsx_runtime22.jsx)(Component, {
      type: "text",
      text: "",
      status
    })
  });
};
var COMPLETE_STATUS3 = Object.freeze({ type: "complete" });
var RUNNING_STATUS = Object.freeze({ type: "running" });
var EmptyPartsImpl = ({ components }) => {
  const status = useAuiState((s) => s.message.status ?? COMPLETE_STATUS3);
  if (components?.Empty) return (0, import_jsx_runtime22.jsx)(components.Empty, { status });
  if (status.type !== "running") return null;
  return (0, import_jsx_runtime22.jsx)(EmptyPartFallback, {
    status,
    component: components?.Text ?? defaultComponents.Text
  });
};
var EmptyParts = (0, import_react36.memo)(EmptyPartsImpl, (prev, next) => prev.components?.Empty === next.components?.Empty && prev.components?.Text === next.components?.Text);
var ConditionalEmptyImpl = ({ components, enabled }) => {
  if (!useAuiState((s) => {
    if (!enabled) return false;
    if (s.message.parts.length === 0) return false;
    const lastPart = s.message.parts[s.message.parts.length - 1];
    return lastPart?.type !== "text" && lastPart?.type !== "reasoning";
  })) return null;
  return (0, import_jsx_runtime22.jsx)(EmptyParts, { components });
};
var ConditionalEmpty = (0, import_react36.memo)(ConditionalEmptyImpl, (prev, next) => prev.enabled === next.enabled && prev.components?.Empty === next.components?.Empty && prev.components?.Text === next.components?.Text);
var QuoteRendererImpl = ({ Quote }) => {
  const quoteInfo = useAuiState(getMessageQuote);
  if (!quoteInfo) return null;
  return (0, import_jsx_runtime22.jsx)(Quote, {
    text: quoteInfo.text,
    messageId: quoteInfo.messageId
  });
};
var QuoteRenderer = (0, import_react36.memo)(QuoteRendererImpl);
function resolveToolRender(toolsState, part) {
  const named = toolsState.toolUIs[part.toolName]?.[0]?.render ?? null;
  if (named) return named;
  if (isMcpAppUri(part.mcp?.app?.resourceUri) && toolsState.mcpApp) return toolsState.mcpApp.render;
  return null;
}
var RegisteredToolUI = () => {
  const aui = useAui();
  const part = useAuiState((s) => s.part);
  const Render = useAuiState((s) => s.part.type === "tool-call" ? resolveToolRender(s.tools, s.part) : null);
  if (!Render || part.type !== "tool-call") return null;
  return (0, import_jsx_runtime22.jsx)(Render, {
    ...part,
    addResult: aui.part().addToolResult,
    resume: aui.part().resumeToolCall,
    respondToApproval: aui.part().respondToToolApproval
  });
};
var RegisteredDataRendererUI = () => {
  const part = useAuiState((s) => s.part);
  const Render = useAuiState((s) => s.part.type === "data" ? getDataRenderer(s.dataRenderers, s.part.name, void 0) ?? null : null);
  if (!Render || part.type !== "data") return null;
  return (0, import_jsx_runtime22.jsx)(Render, { ...part });
};
var DefaultPartFallback = () => {
  const partType = useAuiState((s) => s.part.type);
  if (partType === "tool-call") return (0, import_jsx_runtime22.jsx)(RegisteredToolUI, {});
  if (partType === "data") return (0, import_jsx_runtime22.jsx)(RegisteredDataRendererUI, {});
  return null;
};
var EMPTY_RUNNING_TEXT_PART = Object.freeze({
  type: "text",
  text: "",
  status: RUNNING_STATUS
});
var MessagePartChildren = ({ index, children }) => {
  const aui = useAui();
  const dataRenderers = useAuiState((s) => s.dataRenderers);
  return (0, import_jsx_runtime22.jsx)(PartByIndexProvider, {
    index,
    children: (0, import_jsx_runtime22.jsx)(RenderChildrenWithAccessor, {
      getItemState: (aui2) => aui2.message().part({ index }).getState(),
      children: (getItem) => children({ get part() {
        const state = getItem();
        if (state.type === "tool-call") {
          const hasUI = resolveToolRender(aui.tools().getState(), state) !== null;
          const partMethods = aui.message().part({ index });
          return {
            ...state,
            toolUI: hasUI ? (0, import_jsx_runtime22.jsx)(RegisteredToolUI, {}) : null,
            addResult: partMethods.addToolResult,
            resume: partMethods.resumeToolCall,
            respondToApproval: partMethods.respondToToolApproval
          };
        }
        if (state.type === "data") {
          const hasUI = getDataRenderer(dataRenderers, state.name, void 0) !== void 0;
          return {
            ...state,
            dataRendererUI: hasUI ? (0, import_jsx_runtime22.jsx)(RegisteredDataRendererUI, {}) : null
          };
        }
        return state;
      } })
    })
  });
};
var MessagePrimitivePartsInner = ({ children }) => {
  const contentLength = useAuiState((s) => s.message.parts.length);
  const isRunning = useAuiState((s) => (s.message.status?.type ?? "complete") === "running");
  const isEmptyRunning = contentLength === 0 && isRunning;
  if (contentLength === 0) {
    if (!isEmptyRunning) return null;
    return (0, import_jsx_runtime22.jsx)(TextMessagePartProvider, {
      text: "",
      isRunning: true,
      children: children({ part: EMPTY_RUNNING_TEXT_PART })
    });
  }
  return (0, import_jsx_runtime22.jsx)(import_jsx_runtime22.Fragment, { children: Array.from({ length: contentLength }, (_, index) => (0, import_jsx_runtime22.jsx)(MessagePartChildren, {
    index,
    children: (value) => children(value) ?? (0, import_jsx_runtime22.jsx)(DefaultPartFallback, {})
  }, index)) });
};
var MessagePrimitiveParts = ({ components, unstable_showEmptyOnNonTextEnd = true, children }) => {
  if (children) return (0, import_jsx_runtime22.jsx)(MessagePrimitivePartsInner, { children });
  return (0, import_jsx_runtime22.jsx)(MessagePrimitivePartsCompat, {
    components,
    unstable_showEmptyOnNonTextEnd
  });
};
MessagePrimitiveParts.displayName = "MessagePrimitive.Parts";
var MessagePrimitivePartsCompat = ({ components, unstable_showEmptyOnNonTextEnd }) => {
  const contentLength = useAuiState((s) => s.message.parts.length);
  const messageRanges = useMessagePartsGroups(!!components?.ChainOfThought);
  const partsElements = (0, import_react36.useMemo)(() => {
    if (contentLength === 0) return (0, import_jsx_runtime22.jsx)(EmptyParts, { components });
    return messageRanges.map((range) => {
      if (range.type === "single") return (0, import_jsx_runtime22.jsx)(MessagePrimitivePartByIndex, {
        index: range.index,
        components
      }, range.index);
      else if (range.type === "chainOfThoughtGroup") {
        const ChainOfThoughtComponent = components?.ChainOfThought;
        if (!ChainOfThoughtComponent) return null;
        return (0, import_jsx_runtime22.jsx)(ChainOfThoughtByIndicesProvider, {
          startIndex: range.startIndex,
          endIndex: range.endIndex,
          children: (0, import_jsx_runtime22.jsx)(ChainOfThoughtComponent, {})
        }, `chainOfThought-${range.startIndex}`);
      } else if (range.type === "toolGroup") return (0, import_jsx_runtime22.jsx)(components?.ToolGroup ?? defaultComponents.ToolGroup, {
        startIndex: range.startIndex,
        endIndex: range.endIndex,
        children: Array.from({ length: range.endIndex - range.startIndex + 1 }, (_, i) => {
          const partIndex = range.startIndex + i;
          return (0, import_jsx_runtime22.jsx)(MessagePrimitivePartByIndex, {
            index: partIndex,
            components
          }, `part-${partIndex}`);
        })
      }, `tool-${range.startIndex}`);
      else return (0, import_jsx_runtime22.jsx)(components?.ReasoningGroup ?? defaultComponents.ReasoningGroup, {
        startIndex: range.startIndex,
        endIndex: range.endIndex,
        children: Array.from({ length: range.endIndex - range.startIndex + 1 }, (_, i) => {
          const partIndex = range.startIndex + i;
          return (0, import_jsx_runtime22.jsx)(MessagePrimitivePartByIndex, {
            index: partIndex,
            components
          }, `part-${partIndex}`);
        })
      }, `reasoning-${range.startIndex}`);
    });
  }, [
    messageRanges,
    components,
    contentLength
  ]);
  return (0, import_jsx_runtime22.jsxs)(import_jsx_runtime22.Fragment, { children: [
    components?.Quote && (0, import_jsx_runtime22.jsx)(QuoteRenderer, { Quote: components.Quote }),
    partsElements,
    (0, import_jsx_runtime22.jsx)(ConditionalEmpty, {
      components,
      enabled: unstable_showEmptyOnNonTextEnd
    })
  ] });
};

// node_modules/@assistant-ui/core/dist/react/primitives/message/MessageGroupedParts.js
var import_react37 = __toESM(require_react(), 1);
var import_jsx_runtime23 = __toESM(require_jsx_runtime(), 1);
var COMPLETE_STATUS4 = Object.freeze({ type: "complete" });
var shouldShowIndicator = (mode, parts, isRunning) => {
  if (!isRunning) return false;
  switch (mode) {
    case "never":
      return false;
    case "always":
      return true;
    case "empty":
      return parts.length === 0;
    case "no-text": {
      const last = parts[parts.length - 1];
      return last !== void 0 && last.type !== "text" && last.type !== "reasoning";
    }
  }
};
var PartChildrenSentinel = () => {
  throw new Error("MessagePrimitive.GroupedParts: rendered `children` under a leaf part. `children` is only meaningful for `group-…` cases — add a matching case for the part type or return `null` to skip it.");
};
var renderNode2 = (node, parts, render) => {
  if (node.type === "part") return (0, import_jsx_runtime23.jsx)(MessagePartChildren, {
    index: node.index,
    children: ({ part }) => render({
      part,
      children: (0, import_jsx_runtime23.jsx)(PartChildrenSentinel, {})
    })
  }, `part-${node.index}`);
  const status = parts[node.indices.at(-1)]?.status ?? COMPLETE_STATUS4;
  return (0, import_jsx_runtime23.jsx)(import_react37.Fragment, { children: render({
    part: {
      type: node.key,
      status,
      indices: node.indices
    },
    children: (0, import_jsx_runtime23.jsx)(import_jsx_runtime23.Fragment, { children: node.children.map((child) => renderNode2(child, parts, render)) })
  }) }, node.nodeKey);
};
var MessagePrimitiveGroupedParts = ({ groupBy, indicator = "no-text", children }) => {
  const parts = useAuiState(useShallow((s) => s.message.parts));
  const toolUIs = useAuiState((s) => s.tools.toolUIs);
  const isRunning = useAuiState((s) => indicator === "never" ? false : s.message.status?.type === "running");
  return (0, import_jsx_runtime23.jsxs)(import_jsx_runtime23.Fragment, { children: [(0, import_react37.useMemo)(() => {
    const context2 = { toolUIs };
    return buildGroupTree(parts.map((part) => groupBy(part, context2) ?? []));
  }, [
    parts,
    groupBy[GROUPBY_MEMO_KEY] ?? groupBy,
    toolUIs
  ]).map((node) => renderNode2(node, parts, children)), shouldShowIndicator(indicator, parts, isRunning) && children({
    part: { type: "indicator" },
    children: (0, import_jsx_runtime23.jsx)(PartChildrenSentinel, {})
  })] });
};
MessagePrimitiveGroupedParts.displayName = "MessagePrimitive.GroupedParts";

// node_modules/@assistant-ui/core/dist/react/primitives/message/MessageQuote.js
var import_react38 = __toESM(require_react(), 1);
var import_jsx_runtime24 = __toESM(require_jsx_runtime(), 1);
var MessagePrimitiveQuoteImpl = ({ children }) => {
  const quoteInfo = useAuiState(getMessageQuote);
  if (!quoteInfo) return null;
  return (0, import_jsx_runtime24.jsx)(import_jsx_runtime24.Fragment, { children: children(quoteInfo) });
};
var MessagePrimitiveQuote = (0, import_react38.memo)(MessagePrimitiveQuoteImpl);
MessagePrimitiveQuote.displayName = "MessagePrimitive.Quote";

// node_modules/@assistant-ui/core/dist/react/primitives/message/MessageAttachments.js
var import_react39 = __toESM(require_react(), 1);
var import_jsx_runtime25 = __toESM(require_jsx_runtime(), 1);
var getComponent2 = (components, attachment) => {
  switch (attachment.type) {
    case "image":
      return components?.Image ?? components?.Attachment;
    case "document":
      return components?.Document ?? components?.Attachment;
    case "file":
      return components?.File ?? components?.Attachment;
    default:
      return components?.Attachment;
  }
};
var AttachmentComponent = ({ components }) => {
  const attachment = useAuiState((s) => s.attachment);
  if (!attachment) return null;
  const Component = getComponent2(components, attachment);
  if (!Component) return null;
  return (0, import_jsx_runtime25.jsx)(Component, {});
};
var MessagePrimitiveAttachmentByIndex = (0, import_react39.memo)(({ index, components }) => {
  return (0, import_jsx_runtime25.jsx)(MessageAttachmentByIndexProvider, {
    index,
    children: (0, import_jsx_runtime25.jsx)(AttachmentComponent, { components })
  });
}, (prev, next) => prev.index === next.index && prev.components?.Image === next.components?.Image && prev.components?.Document === next.components?.Document && prev.components?.File === next.components?.File && prev.components?.Attachment === next.components?.Attachment);
MessagePrimitiveAttachmentByIndex.displayName = "MessagePrimitive.AttachmentByIndex";
var MessagePrimitiveAttachmentsInner = ({ children }) => {
  const attachmentsCount = useAuiState((s) => {
    if (s.message.role !== "user") return 0;
    return (s.message.attachments ?? []).length;
  });
  return (0, import_react39.useMemo)(() => Array.from({ length: attachmentsCount }, (_, index) => (0, import_jsx_runtime25.jsx)(MessageAttachmentByIndexProvider, {
    index,
    children: (0, import_jsx_runtime25.jsx)(RenderChildrenWithAccessor, {
      getItemState: (aui) => aui.message().attachment({ index }).getState(),
      children: (getItem) => children({ get attachment() {
        return getItem();
      } })
    })
  }, index)), [attachmentsCount, children]);
};
var MessagePrimitiveAttachments = ({ components, children }) => {
  if (components) return (0, import_jsx_runtime25.jsx)(MessagePrimitiveAttachmentsInner, { children: ({ attachment }) => {
    const Component = getComponent2(components, attachment);
    if (!Component) return null;
    return (0, import_jsx_runtime25.jsx)(Component, {});
  } });
  return (0, import_jsx_runtime25.jsx)(MessagePrimitiveAttachmentsInner, { children });
};
MessagePrimitiveAttachments.displayName = "MessagePrimitive.Attachments";

// node_modules/@assistant-ui/core/dist/react/primitives/composer/ComposerAttachments.js
var import_react40 = __toESM(require_react(), 1);
var import_jsx_runtime26 = __toESM(require_jsx_runtime(), 1);
var getComponent3 = (components, attachment) => {
  switch (attachment.type) {
    case "image":
      return components?.Image ?? components?.Attachment;
    case "document":
      return components?.Document ?? components?.Attachment;
    case "file":
      return components?.File ?? components?.Attachment;
    default:
      return components?.Attachment;
  }
};
var AttachmentComponent2 = ({ components }) => {
  const attachment = useAuiState((s) => s.attachment);
  if (!attachment) return null;
  const Component = getComponent3(components, attachment);
  if (!Component) return null;
  return (0, import_jsx_runtime26.jsx)(Component, {});
};
var ComposerPrimitiveAttachmentByIndex = (0, import_react40.memo)(({ index, components }) => {
  return (0, import_jsx_runtime26.jsx)(ComposerAttachmentByIndexProvider, {
    index,
    children: (0, import_jsx_runtime26.jsx)(AttachmentComponent2, { components })
  });
}, (prev, next) => prev.index === next.index && prev.components?.Image === next.components?.Image && prev.components?.Document === next.components?.Document && prev.components?.File === next.components?.File && prev.components?.Attachment === next.components?.Attachment);
ComposerPrimitiveAttachmentByIndex.displayName = "ComposerPrimitive.AttachmentByIndex";
var ComposerPrimitiveAttachmentsInner = ({ children }) => {
  const attachmentsCount = useAuiState((s) => s.composer.attachments.length);
  return (0, import_react40.useMemo)(() => Array.from({ length: attachmentsCount }, (_, index) => (0, import_jsx_runtime26.jsx)(ComposerAttachmentByIndexProvider, {
    index,
    children: (0, import_jsx_runtime26.jsx)(RenderChildrenWithAccessor, {
      getItemState: (aui) => aui.composer().attachment({ index }).getState(),
      children: (getItem) => children({ get attachment() {
        return getItem();
      } })
    })
  }, index)), [attachmentsCount, children]);
};
var ComposerPrimitiveAttachments = ({ components, children }) => {
  if (components) return (0, import_jsx_runtime26.jsx)(ComposerPrimitiveAttachmentsInner, { children: ({ attachment }) => {
    const Component = getComponent3(components, attachment);
    if (!Component) return null;
    return (0, import_jsx_runtime26.jsx)(Component, {});
  } });
  return (0, import_jsx_runtime26.jsx)(ComposerPrimitiveAttachmentsInner, { children });
};
ComposerPrimitiveAttachments.displayName = "ComposerPrimitive.Attachments";

// node_modules/@assistant-ui/core/dist/react/primitives/composer/ComposerQueue.js
var import_react41 = __toESM(require_react(), 1);
var import_jsx_runtime27 = __toESM(require_jsx_runtime(), 1);
var ComposerPrimitiveQueueInner = ({ children }) => {
  const queue = useAuiState((s) => s.composer.queue.length);
  return (0, import_react41.useMemo)(() => Array.from({ length: queue }, (_, index) => (0, import_jsx_runtime27.jsx)(QueueItemByIndexProvider, {
    index,
    children: (0, import_jsx_runtime27.jsx)(RenderChildrenWithAccessor, {
      getItemState: (aui) => aui.composer().queueItem({ index }).getState(),
      children: (getItem) => children({ get queueItem() {
        return getItem();
      } })
    })
  }, index)), [queue, children]);
};
var ComposerPrimitiveQueue = (0, import_react41.memo)(ComposerPrimitiveQueueInner);
ComposerPrimitiveQueue.displayName = "ComposerPrimitive.Queue";

// node_modules/@assistant-ui/core/dist/react/primitives/threadList/ThreadListItems.js
var import_react42 = __toESM(require_react(), 1);
var import_jsx_runtime28 = __toESM(require_jsx_runtime(), 1);
var ThreadListPrimitiveItemByIndex = (0, import_react42.memo)(({ index, archived = false, components }) => {
  const ThreadListItemComponent = components.ThreadListItem;
  return (0, import_jsx_runtime28.jsx)(ThreadListItemByIndexProvider, {
    index,
    archived,
    children: (0, import_jsx_runtime28.jsx)(ThreadListItemComponent, {})
  });
}, (prev, next) => prev.index === next.index && prev.archived === next.archived && prev.components.ThreadListItem === next.components.ThreadListItem);
ThreadListPrimitiveItemByIndex.displayName = "ThreadListPrimitive.ItemByIndex";
var ThreadListPrimitiveItemsInner = ({ archived, children }) => {
  const contentLength = useAuiState((s) => archived ? s.threads.archivedThreadIds.length : s.threads.threadIds.length);
  return (0, import_react42.useMemo)(() => Array.from({ length: contentLength }, (_, index) => (0, import_jsx_runtime28.jsx)(ThreadListItemByIndexProvider, {
    index,
    archived,
    children: (0, import_jsx_runtime28.jsx)(RenderChildrenWithAccessor, {
      getItemState: (aui) => aui.threads().item({
        index,
        archived
      }).getState(),
      children: (getItem) => children({ get threadListItem() {
        return getItem();
      } })
    })
  }, index)), [
    contentLength,
    archived,
    children
  ]);
};
var ThreadListPrimitiveItems = ({ archived = false, components, children }) => {
  if (components) {
    const ThreadListItemComponent = components.ThreadListItem;
    return (0, import_jsx_runtime28.jsx)(ThreadListPrimitiveItemsInner, {
      archived,
      children: () => (0, import_jsx_runtime28.jsx)(ThreadListItemComponent, {})
    });
  }
  return (0, import_jsx_runtime28.jsx)(ThreadListPrimitiveItemsInner, {
    archived,
    children
  });
};
ThreadListPrimitiveItems.displayName = "ThreadListPrimitive.Items";

// node_modules/@assistant-ui/core/dist/react/primitives/chainOfThought/ChainOfThoughtParts.js
var import_react43 = __toESM(require_react(), 1);
var import_jsx_runtime29 = __toESM(require_jsx_runtime(), 1);
var ChainOfThoughtPrimitivePartsInner = ({ children }) => {
  const partsLength = useAuiState((s) => s.chainOfThought.parts.length);
  return (0, import_react43.useMemo)(() => Array.from({ length: partsLength }, (_, index) => (0, import_jsx_runtime29.jsx)(ChainOfThoughtPartByIndexProvider, {
    index,
    children: (0, import_jsx_runtime29.jsx)(RenderChildrenWithAccessor, {
      getItemState: (aui) => aui.chainOfThought().part({ index }).getState(),
      children: (getItem) => children({ get part() {
        return getItem();
      } })
    })
  }, index)), [partsLength, children]);
};
var ChainOfThoughtPrimitiveParts = ({ components, children }) => {
  if (children) return (0, import_jsx_runtime29.jsx)(ChainOfThoughtPrimitivePartsInner, { children });
  const messageComponents = (0, import_react43.useMemo)(() => ({
    Reasoning: components?.Reasoning,
    tools: { Fallback: components?.tools?.Fallback }
  }), [components?.Reasoning, components?.tools?.Fallback]);
  const Layout = components?.Layout;
  return (0, import_jsx_runtime29.jsx)(ChainOfThoughtPrimitivePartsInner, { children: () => Layout ? (0, import_jsx_runtime29.jsx)(Layout, { children: (0, import_jsx_runtime29.jsx)(MessagePartComponent, { components: messageComponents }) }) : (0, import_jsx_runtime29.jsx)(MessagePartComponent, { components: messageComponents }) });
};
ChainOfThoughtPrimitiveParts.displayName = "ChainOfThoughtPrimitive.Parts";

// node_modules/@assistant-ui/core/dist/react/primitives/part/PartMessages.js
var import_react44 = __toESM(require_react(), 1);
var import_jsx_runtime30 = __toESM(require_jsx_runtime(), 1);
var usePartMessages = () => {
  return useAuiState((s) => {
    const part = s.part;
    if (part.type !== "tool-call") return void 0;
    return "messages" in part ? part.messages : void 0;
  });
};
var PartPrimitiveMessagesImpl = ({ components, children }) => {
  const messages = usePartMessages();
  if (!messages?.length) return null;
  if (children) return (0, import_jsx_runtime30.jsx)(ReadonlyThreadProvider, {
    messages,
    children: (0, import_jsx_runtime30.jsx)(ThreadPrimitiveMessagesImpl, { children })
  });
  return (0, import_jsx_runtime30.jsx)(ReadonlyThreadProvider, {
    messages,
    children: (0, import_jsx_runtime30.jsx)(ThreadPrimitiveMessagesImpl, { components })
  });
};
PartPrimitiveMessagesImpl.displayName = "PartPrimitive.Messages";
var PartPrimitiveMessages = (0, import_react44.memo)(PartPrimitiveMessagesImpl);

// node_modules/@assistant-ui/core/dist/react/primitives/messagePart/MessagePartInProgress.js
var MessagePartPrimitiveInProgress = ({ children }) => {
  return useAuiState((s) => s.part.status.type === "running") ? children : null;
};
MessagePartPrimitiveInProgress.displayName = "MessagePartPrimitive.InProgress";

// node_modules/@assistant-ui/core/dist/react/primitives/threadListItem/ThreadListItemTitle.js
var import_jsx_runtime31 = __toESM(require_jsx_runtime(), 1);
var ThreadListItemPrimitiveTitle = ({ fallback }) => {
  return (0, import_jsx_runtime31.jsx)(import_jsx_runtime31.Fragment, { children: useAuiState((s) => s.threadListItem.title) || fallback });
};
ThreadListItemPrimitiveTitle.displayName = "ThreadListItemPrimitive.Title";

// node_modules/@assistant-ui/core/dist/react/primitives/thread/ThreadSuggestions.js
var import_react45 = __toESM(require_react(), 1);
var import_jsx_runtime32 = __toESM(require_jsx_runtime(), 1);
var SuggestionComponent = ({ components }) => {
  const Component = components.Suggestion;
  return (0, import_jsx_runtime32.jsx)(Component, {});
};
var ThreadPrimitiveSuggestionByIndex = (0, import_react45.memo)(({ index, components }) => {
  return (0, import_jsx_runtime32.jsx)(SuggestionByIndexProvider, {
    index,
    children: (0, import_jsx_runtime32.jsx)(SuggestionComponent, { components })
  });
}, (prev, next) => prev.index === next.index && prev.components.Suggestion === next.components.Suggestion);
ThreadPrimitiveSuggestionByIndex.displayName = "ThreadPrimitive.SuggestionByIndex";
var ThreadPrimitiveSuggestionsInner = ({ children }) => {
  const suggestionsLength = useAuiState((s) => s.suggestions.suggestions.length);
  return (0, import_react45.useMemo)(() => {
    if (suggestionsLength === 0) return null;
    return Array.from({ length: suggestionsLength }, (_, index) => (0, import_jsx_runtime32.jsx)(SuggestionByIndexProvider, {
      index,
      children: (0, import_jsx_runtime32.jsx)(RenderChildrenWithAccessor, {
        getItemState: (aui) => aui.suggestions().suggestion({ index }).getState(),
        children: (getItem) => children({ get suggestion() {
          return getItem();
        } })
      })
    }, index));
  }, [suggestionsLength, children]);
};
var ThreadPrimitiveSuggestionsImpl = ({ components, children }) => {
  if (components) return (0, import_jsx_runtime32.jsx)(ThreadPrimitiveSuggestionsInner, { children: () => (0, import_jsx_runtime32.jsx)(SuggestionComponent, { components }) });
  return (0, import_jsx_runtime32.jsx)(ThreadPrimitiveSuggestionsInner, { children });
};
ThreadPrimitiveSuggestionsImpl.displayName = "ThreadPrimitive.Suggestions";
var ThreadPrimitiveSuggestions = (0, import_react45.memo)(ThreadPrimitiveSuggestionsImpl, (prev, next) => {
  if (prev.children || next.children) return prev.children === next.children;
  return prev.components.Suggestion === next.components.Suggestion;
});

// node_modules/@assistant-ui/core/dist/react/primitives/composer/ComposerIf.js
var useComposerIf = (props) => {
  return useAuiState((s) => {
    if (props.editing === true && !s.composer.isEditing) return false;
    if (props.editing === false && s.composer.isEditing) return false;
    const isDictating = s.composer.dictation != null;
    if (props.dictation === true && !isDictating) return false;
    if (props.dictation === false && isDictating) return false;
    return true;
  });
};
var ComposerPrimitiveIf = ({ children, ...query }) => {
  return useComposerIf(query) ? children : null;
};
ComposerPrimitiveIf.displayName = "ComposerPrimitive.If";

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useComposerSend.js
var import_react46 = __toESM(require_react(), 1);
var useComposerSend = () => {
  const aui = useAui();
  const disabled = useAuiState((s) => !s.composer.canSend || s.thread.isRunning && !s.thread.capabilities.queue);
  return {
    send: (0, import_react46.useCallback)((opts) => {
      aui.composer().send(opts);
    }, [aui]),
    disabled
  };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useComposerCancel.js
var import_react47 = __toESM(require_react(), 1);
var useComposerCancel = () => {
  const aui = useAui();
  const disabled = useAuiState((s) => !s.composer.canCancel);
  return {
    cancel: (0, import_react47.useCallback)(() => {
      aui.composer().cancel();
    }, [aui]),
    disabled
  };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useComposerDictate.js
var import_react48 = __toESM(require_react(), 1);
var useComposerDictate = () => {
  const aui = useAui();
  const disabled = useAuiState((s) => s.composer.dictation != null || !s.thread.capabilities.dictation || !s.composer.isEditing);
  return {
    startDictation: (0, import_react48.useCallback)(() => {
      aui.composer().startDictation();
    }, [aui]),
    disabled
  };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useComposerAddAttachment.js
var import_react49 = __toESM(require_react(), 1);
var useComposerAddAttachment = () => {
  const aui = useAui();
  const disabled = useAuiState((s) => !s.composer.isEditing);
  return {
    addAttachment: (0, import_react49.useCallback)((file) => {
      return aui.composer().addAttachment(file);
    }, [aui]),
    disabled
  };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useMessageReload.js
var import_react50 = __toESM(require_react(), 1);

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useMessageBranching.js
var import_react51 = __toESM(require_react(), 1);

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useActionBarCopy.js
var import_react52 = __toESM(require_react(), 1);
var useActionBarCopy = ({ copiedDuration = 3e3, copyToClipboard } = {}) => {
  const aui = useAui();
  const disabled = useAuiState((s) => {
    return !((s.message.role !== "assistant" || s.message.status?.type !== "running") && s.message.parts.some((c) => c.type === "text" && c.text.length > 0));
  });
  const isCopied = useAuiState((s) => s.message.isCopied);
  const isEditing = useAuiState((s) => s.composer.isEditing);
  const composerValue = useAuiState((s) => s.composer.text);
  return {
    copy: (0, import_react52.useCallback)(() => {
      const valueToCopy = isEditing ? composerValue : aui.message().getCopyText();
      if (!valueToCopy) return;
      const write = copyToClipboard ?? (() => {
      });
      Promise.resolve(write(valueToCopy)).then(() => {
        aui.message().setIsCopied(true);
        setTimeout(() => aui.message().setIsCopied(false), copiedDuration);
      }, () => {
      });
    }, [
      aui,
      isEditing,
      composerValue,
      copiedDuration,
      copyToClipboard
    ]),
    disabled,
    isCopied
  };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useActionBarEdit.js
var import_react53 = __toESM(require_react(), 1);
var useActionBarEdit = () => {
  const aui = useAui();
  const disabled = useAuiState((s) => s.composer.isEditing);
  return {
    edit: (0, import_react53.useCallback)(() => {
      aui.composer().beginEdit();
    }, [aui]),
    disabled
  };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useActionBarReload.js
var import_react54 = __toESM(require_react(), 1);
var useActionBarReload = () => {
  const aui = useAui();
  const disabled = useAuiState((s) => s.thread.isRunning || s.thread.isDisabled || s.message.role !== "assistant");
  return {
    reload: (0, import_react54.useCallback)(() => {
      aui.message().reload();
    }, [aui]),
    disabled
  };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useActionBarFeedback.js
var import_react55 = __toESM(require_react(), 1);
var useActionBarFeedbackPositive = () => {
  const aui = useAui();
  const isSubmitted = useAuiState((s) => s.message.metadata.submittedFeedback?.type === "positive");
  return {
    submit: (0, import_react55.useCallback)(() => {
      aui.message().submitFeedback({ type: "positive" });
    }, [aui]),
    isSubmitted
  };
};
var useActionBarFeedbackNegative = () => {
  const aui = useAui();
  const isSubmitted = useAuiState((s) => s.message.metadata.submittedFeedback?.type === "negative");
  return {
    submit: (0, import_react55.useCallback)(() => {
      aui.message().submitFeedback({ type: "negative" });
    }, [aui]),
    isSubmitted
  };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useActionBarSpeak.js
var import_react56 = __toESM(require_react(), 1);
var useActionBarSpeak = () => {
  const aui = useAui();
  const disabled = useAuiState((s) => {
    return !((s.message.role !== "assistant" || s.message.status?.type !== "running") && s.message.parts.some((c) => c.type === "text" && c.text.length > 0));
  });
  return {
    speak: (0, import_react56.useCallback)(async () => {
      aui.message().speak();
    }, [aui]),
    disabled
  };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useActionBarStopSpeaking.js
var import_react57 = __toESM(require_react(), 1);
var useActionBarStopSpeaking = () => {
  const aui = useAui();
  const disabled = useAuiState((s) => s.message.speech == null);
  return {
    stopSpeaking: (0, import_react57.useCallback)(() => {
      aui.message().stopSpeaking();
    }, [aui]),
    disabled
  };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useBranchPickerNext.js
var import_react58 = __toESM(require_react(), 1);
var useBranchPickerNext = () => {
  const aui = useAui();
  const disabled = useAuiState((s) => {
    if (s.message.branchNumber >= s.message.branchCount) return true;
    if (s.thread.isRunning && !s.thread.capabilities.switchBranchDuringRun) return true;
    return false;
  });
  return {
    next: (0, import_react58.useCallback)(() => {
      aui.message().switchToBranch({ position: "next" });
    }, [aui]),
    disabled
  };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useBranchPickerPrevious.js
var import_react59 = __toESM(require_react(), 1);
var useBranchPickerPrevious = () => {
  const aui = useAui();
  const disabled = useAuiState((s) => {
    if (s.message.branchNumber <= 1) return true;
    if (s.thread.isRunning && !s.thread.capabilities.switchBranchDuringRun) return true;
    return false;
  });
  return {
    previous: (0, import_react59.useCallback)(() => {
      aui.message().switchToBranch({ position: "previous" });
    }, [aui]),
    disabled
  };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useSuggestionTrigger.js
var import_react60 = __toESM(require_react(), 1);
var useSuggestionTrigger = ({ prompt, send, clearComposer = true }) => {
  const aui = useAui();
  const disabled = useAuiState((s) => s.thread.isDisabled);
  const resolvedSend = send ?? false;
  return {
    trigger: (0, import_react60.useCallback)(() => {
      const isRunning = aui.thread().getState().isRunning;
      if (resolvedSend && !isRunning) {
        aui.thread().append({
          content: [{
            type: "text",
            text: prompt
          }],
          runConfig: aui.composer().getState().runConfig
        });
        if (clearComposer) aui.composer().setText("");
      } else if (clearComposer) aui.composer().setText(prompt);
      else {
        const currentText = aui.composer().getState().text;
        aui.composer().setText(currentText.trim() ? `${currentText} ${prompt}` : prompt);
      }
    }, [
      aui,
      resolvedSend,
      clearComposer,
      prompt
    ]),
    disabled
  };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useThreadListItemArchive.js
var import_react61 = __toESM(require_react(), 1);
var useThreadListItemArchive = () => {
  const aui = useAui();
  return { archive: (0, import_react61.useCallback)(() => {
    aui.threadListItem().archive();
  }, [aui]) };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useThreadListItemDelete.js
var import_react62 = __toESM(require_react(), 1);
var useThreadListItemDelete = () => {
  const aui = useAui();
  return { delete: (0, import_react62.useCallback)(() => {
    aui.threadListItem().delete();
  }, [aui]) };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useThreadListItemUnarchive.js
var import_react63 = __toESM(require_react(), 1);
var useThreadListItemUnarchive = () => {
  const aui = useAui();
  return { unarchive: (0, import_react63.useCallback)(() => {
    aui.threadListItem().unarchive();
  }, [aui]) };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useThreadListItemTrigger.js
var import_react64 = __toESM(require_react(), 1);
var useThreadListItemTrigger = () => {
  const aui = useAui();
  return { switchTo: (0, import_react64.useCallback)(() => {
    aui.threadListItem().switchTo();
  }, [aui]) };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useThreadListNew.js
var import_react65 = __toESM(require_react(), 1);
var useThreadListNew = () => {
  const aui = useAui();
  return { switchToNewThread: (0, import_react65.useCallback)(() => {
    aui.threads().switchToNewThread();
  }, [aui]) };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useThreadListLoadMore.js
var import_react66 = __toESM(require_react(), 1);
var useThreadListLoadMore = () => {
  const aui = useAui();
  const disabled = useAuiState((s) => !s.threads.hasMore || s.threads.isLoading || s.threads.isLoadingMore);
  return {
    loadMore: (0, import_react66.useCallback)(() => {
      aui.threads().loadMore();
    }, [aui]),
    disabled
  };
};

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useEditComposerCancel.js
var import_react67 = __toESM(require_react(), 1);

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useEditComposerSend.js
var import_react68 = __toESM(require_react(), 1);

// node_modules/@assistant-ui/core/dist/react/primitive-hooks/useMessageError.js
var useMessageError = () => {
  return useAuiState((s) => s.message.status?.type === "incomplete" && s.message.status.reason === "error" ? s.message.status.error ?? "An error occurred" : void 0);
};

export {
  resource,
  withKey,
  tapState,
  tapEffect,
  tapRef,
  tapConst,
  tapMemo,
  tapEffectEvent,
  tapResource,
  flushResourcesSync,
  DataRenderers,
  CompositeContextProvider,
  ModelContext,
  tapClientResource,
  AuiProvider,
  Derived,
  attachTransformScopes,
  useResource,
  useAui,
  useAuiState,
  AuiIf,
  useAuiEvent,
  tapClientLookup,
  Tools,
  Suggestions,
  AssistantProviderBase,
  useAssistantTool,
  makeAssistantTool,
  useAssistantToolUI,
  makeAssistantToolUI,
  useAssistantDataUI,
  makeAssistantDataUI,
  useAssistantInstructions,
  useAssistantContext,
  create,
  useInlineRender,
  defineToolkit,
  stubTool,
  useAuiToolOverrides,
  hitlTool,
  hitl,
  providerTool,
  defineMcpToolkit,
  useAssistantInteractable,
  useInteractableState,
  useToolArgsStatus,
  Interactables,
  MessageAttachmentByIndexProvider,
  ComposerAttachmentByIndexProvider,
  ThreadListItemRuntimeProvider,
  MessageByIndexProvider,
  PartByIndexProvider,
  TextMessagePartProvider,
  ChainOfThoughtClient,
  ChainOfThoughtByIndicesProvider,
  ThreadListItemByIndexProvider,
  SuggestionByIndexProvider,
  ThreadRuntimeImpl,
  ReadonlyThreadProvider,
  RuntimeAdapterProvider,
  useRuntimeAdapters,
  AssistantRuntimeImpl,
  BaseAssistantRuntimeCore,
  DefaultThreadComposerRuntimeCore,
  useExternalStoreRuntime,
  useExternalStoreSharedOptions,
  convertExternalMessages,
  useExternalMessageConverter,
  createMessageConverter,
  useRemoteThreadListRuntime,
  AssistantCloud,
  CloudFileAttachmentAdapter,
  useCloudThreadListAdapter,
  ThreadPrimitiveMessageByIndex,
  ThreadPrimitiveMessages,
  getMessageQuote,
  GenerativeUIRenderError,
  GenerativeUIRender,
  MessagePrimitiveGenerativeUI,
  defaultComponents,
  MessagePrimitivePartByIndex,
  MessagePrimitiveParts,
  groupPartByType,
  MessagePrimitiveGroupedParts,
  MessagePrimitiveQuote,
  MessagePrimitiveAttachmentByIndex,
  MessagePrimitiveAttachments,
  ComposerPrimitiveAttachmentByIndex,
  ComposerPrimitiveAttachments,
  ComposerPrimitiveQueue,
  ThreadListPrimitiveItemByIndex,
  ThreadListPrimitiveItems,
  ChainOfThoughtPrimitiveParts,
  PartPrimitiveMessages,
  MessagePartPrimitiveInProgress,
  ThreadListItemPrimitiveTitle,
  ThreadPrimitiveSuggestionByIndex,
  ThreadPrimitiveSuggestions,
  ComposerPrimitiveIf,
  useComposerSend,
  useComposerCancel,
  useComposerDictate,
  useComposerAddAttachment,
  useActionBarCopy,
  useActionBarEdit,
  useActionBarReload,
  useActionBarFeedbackPositive,
  useActionBarFeedbackNegative,
  useActionBarSpeak,
  useActionBarStopSpeaking,
  useVoiceState,
  useVoiceVolume,
  useVoiceControls,
  useBranchPickerNext,
  useBranchPickerPrevious,
  useSuggestionTrigger,
  useThreadListItemArchive,
  useThreadListItemDelete,
  useThreadListItemUnarchive,
  useThreadListItemTrigger,
  useThreadListNew,
  useThreadListLoadMore,
  useMessageError,
  splitLocalRuntimeOptions,
  useLocalRuntime
};
//# sourceMappingURL=chunk-OYIRJU3Y.js.map
